# V0-runde 6 — F28.25 Uke (B2) (2026-06-19)

**Chat:** https://v0.app/sivert-s-projects/chat/weather-app-improvements-hFdZTf6eG0J

## V0's score: **82/100**

«Solid struktur, riktig atmosphere-modell, god a11y-disiplin. Trekk for:
kolonnelås + vær-ikon ikke bekreftet implementert, og uavklart
interpolasjons-kilde (Q3).»

## V0's 2 P0-tiltak (det jeg ikke ser uten å scrolle)

1. **Kolonnelås:** temp/badge/vær-ikon må ligge på fast x-grid (CSS grid
   med faste kolonnebredder + `.tnum` på temp). Misalignment vises først
   nedover i lista, ikke i toppraden du ser i preview.

2. **Intern scroll-container bunn-padding/safe-area** så siste rad ikke
   ligger under bottom-nav (klippet rad = «halv-kuttet kort»-feilen fra
   B4 igjen).

## V0's atmosphere-svar (Q3)

- **«I dag»-state:** AKTIV stepper-time styrer (fokusert-rad-prinsippet
  i B6 → her er «fokus» = valgt time, ikke rad 0). Bakgrunnen skal følge
  det SR-en annonserer via aria-live.
- **«10 dager»-state:** ingen stepper → bruk FØRSTE rad (i dag/nærmeste)
  som t-kilde.
- **Begge:** kontinuerlig `mix()` cold→mild→warm, aldri tre faste klasser
  (kritisk regel linje 162), `transition:background var(--dur-slow)` så
  skiftet føles levende, ikke hopp.

> Konsekvent regel: fokusert tidspunkt styrer bakgrunn; mangler fokus,
> styrer nærmeste rad.

## Notis
V0 viste «Your credit balance is low. Recharge before you run out.» —
Sivert's V0-konto. Flagges til Sivert ved neste statussjekk.

## F28.26-plan

1. Kolonnelås: PlanScreenInstrument-rad-grid med faste kolonner
2. Bunn-safe-area-padding på scroll-container
3. Atmosphere-kilde: hook ny `useAtmosphereSource(currentStepperHour)` →
   feed inn i `applyAtmosphere(root, { temp })` med `transition: background`
