---
phase: 03-living-home-signature-transition
plan: "05"
type: execute
wave: 3
depends_on:
  - 02-09
  - 03-03
files_modified:
  - .planning/phases/03-living-home-signature-transition/evidence/03-05-PHASE2-GATE.md
  - src/lib/outfit-transition/phase2-adapter.ts
  - src/lib/outfit-transition/phase2-adapter.test.ts
  - .planning/phases/03-living-home-signature-transition/evidence/03-05-INDEPENDENT-REVIEW.md
autonomous: true
requirements:
  - MOTION-01
must_haves:
  truths:
    - "Integration begins only from the exact Phase 2 candidate SHA with recorded fresh-context independent PASS, per D-09 and D-12."
    - "The adapter imports Phase-2-owned `AvatarVisibleSlot`, `AvatarVisualCoverage`, `OutfitTruthSnapshotV1`, `OutfitBundleProducerResult`, `RegisterOutfitRow`, and `OutfitTransitionVisualState` instead of mirroring their structures."
    - "The intended transition set starts only from `base.avatar.visibleGarmentIds`; `base.garments` resolves those IDs exactly once but never independently selects candidates."
    - "`transitionVisualState` remains only the scalar `settled | landing` presentation input and never carries or selects visibility truth."
    - "Unsupported/unavailable bundles plus unknown, null, hidden, occluded, ambiguous, duplicate, or equipment entries remain complete static outcomes."
    - "Identity or anchor uncertainty produces static settlement, per D-04."
  artifacts:
    - path: .planning/phases/03-living-home-signature-transition/evidence/03-05-PHASE2-GATE.md
      provides: "Exact Phase 2 SHA, ancestry, interface, and independent-PASS evidence"
    - path: src/lib/outfit-transition/phase2-adapter.ts
      provides: "Sole adapter from verified Phase 2 public contracts to Phase 3 capture inputs"
  key_links:
    - from: src/lib/outfit-transition/phase2-adapter.ts
      to: src/lib/outfit/outfit-truth.ts
      via: "Type-only imports of `OutfitTruthSnapshotV1`, `AvatarVisibleSlot`, and `AvatarVisualCoverage`; exact resolution of avatar-visible IDs to `avatarCoverage`"
    - from: src/lib/outfit-transition/phase2-adapter.ts
      to: src/lib/outfit/outfit-bundle-producer.ts
      via: "Type-only `OutfitBundleProducerResult` provenance/container gate"
    - from: src/lib/outfit-transition/phase2-adapter.ts
      to: src/lib/outfit/outfit-transition-contract.ts
      via: "Type-only `RegisterOutfitRow` and visual-state contract"
---

<objective>
Bind Phase 3 to the exact independently passed Phase 2 public interface and create one truth-preserving adapter, implementing D-04, D-05, D-09, D-10, and D-12.

Purpose: Prevent motion code from guessing garment identity or coupling itself to Antrekkskart internals.
Output: A recorded exact-SHA Phase 2 gate, a typed public-contract adapter, adversarial tests, and an independently reviewed Phase 3 candidate.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-outfit-truth-antrekkskart/02-INTERFACE-CONTRACT.md
@.planning/phases/02-outfit-truth-antrekkskart/02-09-SUMMARY.md
@.planning/phases/03-living-home-signature-transition/03-CONTEXT.md
@.planning/phases/03-living-home-signature-transition/03-DEPENDENCY-CONTRACT.md
@.planning/phases/03-living-home-signature-transition/03-VALIDATION.md
@.planning/phases/03-living-home-signature-transition/03-03-SUMMARY.md
@src/lib/outfit/outfit-truth.ts
@src/lib/outfit/outfit-bundle-producer.ts
@src/lib/outfit/outfit-transition-contract.ts
@src/lib/outfit-transition/transition-snapshot.ts
@src/lib/outfit-transition/eligibility.ts
</context>

<interfaces>
The adapter must compile against type-only imports of the accepted Phase-2 exports: branded `OutfitItemId`, `AvatarVisibleSlot`, `AvatarVisualCoverage`, `OutfitTruthSnapshotV1`, `OutfitBundleProducerResult`, `RegisterOutfitRow`, and `OutfitTransitionVisualState`. A supported bundle's `base` is the imported snapshot. Candidate selection begins only at `base.avatar.visibleGarmentIds`; each listed ID is resolved exactly once in `base.garments` to validate `visibleOnAvatar === true`, non-null `avatarCoverage`, coverage slots/rank/occlusion, and non-equipment identity. `transitionVisualState` is only `"settled" | "landing"` presentation state. Phase 3 does not import or model Phase-2 alternative-option internals. Phase 2 registers only real semantic Outfit rows and owns the production screen. Phase 3 owns a separate internal `RegisterHomeAnchor`; there is no generic anchor-kind registrar.

The checked-in exports at the recorded Phase-2 SHA are the only compiler oracle. Any missing or incompatible export blocks integration. Never compensate by copying a planning-file type, reading React internals, selecting from `base.garments`, or using text labels, class names, CSS geometry, avatar assets, or DOM order.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Enforce the exact Phase 2 candidate gate</name>
  <files>.planning/phases/03-living-home-signature-transition/evidence/03-05-PHASE2-GATE.md</files>
  <action>
In the isolated Phase 3 worktree, verify `02-09-SUMMARY.md` through the checked-in fail-fast verifier planned by 03-03. Require exact `status: PASS`, full `phase2_candidate_sha`, `feature_flag: true`, commit existence/ancestry, and authoritative exported `AvatarVisibleSlot`, `AvatarVisualCoverage`, `OutfitTruthSnapshotV1`, `avatarCoverage`, `OutfitBundleProducerResult`, `RegisterOutfitRow`, and scalar `transitionVisualState` surfaces. Record the exact SHA and export sites. If any label or export fails, stop immediately; do not substitute branch head, a later unreviewed commit, a generic registrar, a local structural mirror, or a guessed interface.
  </action>
  <verify>
    <automated>node scripts/verify-phase3-exact-sha.mjs phase2-handoff --summary .planning/phases/02-outfit-truth-antrekkskart/02-09-SUMMARY.md --expected-feature-flag true --ancestor-of HEAD</automated>
  </verify>
  <done>The evidence file identifies one immutable, integrated Phase 2 implementation SHA and its fresh-context PASS; no floating branch reference remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adapt verified outfit truth and anchors</name>
  <files>src/lib/outfit-transition/phase2-adapter.ts, src/lib/outfit-transition/phase2-adapter.test.ts</files>
  <behavior>
    - "A supported bundle yields candidates only from `base.avatar.visibleGarmentIds`, preserving that authoritative ID order."
    - "Each visible ID resolves exactly once in `base.garments`; unlisted garments can never become candidates and garment-array reordering cannot change candidate order."
    - "Every resolved garment requires `visibleOnAvatar === true`, valid non-null `avatarCoverage`, valid `AvatarVisibleSlot` coverage, finite rank, explicit occlusion, and surviving visible slots."
    - "Missing/duplicate visible IDs or garment matches, hidden entries, null/empty/unknown coverage, rank ties, partial/contradictory occlusion, and duplicate IDs/slots settle statically."
    - "Equipment and unsupported/unavailable/absent bundles settle statically; equipment IDs never enter the motion set."
    - "Scalar `transitionVisualState` values `settled` and `landing` do not change identity or eligibility."
    - "Every accepted branded ID must own exactly one Phase-3 Home source and one Phase-2 real Outfit row; any missing, duplicate, mismatched, zero, or stale anchor settles statically."
    - "No label, category, avatar asset, array position, class name, CSS selector, DOM order, or Antrekkskart node can establish identity or visibility."
  </behavior>
  <action>
Write contract tests against real exported Phase-2 fixtures first. Add compile/source assertions proving the adapter uses type-only imports for `AvatarVisibleSlot`, `AvatarVisualCoverage`, `OutfitTruthSnapshotV1`, `OutfitBundleProducerResult`, `RegisterOutfitRow`, and `OutfitTransitionVisualState`, consumes `base.avatar.visibleGarmentIds`, `visibleOnAvatar`, and `avatarCoverage`, has no dependency on Phase-2 alternative-option internals, and never scans/maps/filters `base.garments` to independently select motion candidates.

Implement the sole adapter so `OutfitBundleProducerResult` gates provenance and supported/static branching and supported `base` is accepted only as imported `OutfitTruthSnapshotV1`. Start the ordered candidate set only from `base.avatar.visibleGarmentIds`. Build an exact-ID lookup over `base.garments` solely to resolve those already-selected IDs, reject duplicate garment IDs, and require exactly one garment match for each visible ID. Reject any selected ID present in equipment. Require `visibleOnAvatar === true` and non-null valid `AvatarVisualCoverage`; use `bodyCoverage`, `visibleSlots`, finite `visualLayerRank`, and explicit `occludesSlots` to reject hidden, tied, partial, contradictory, or otherwise ambiguous resolution. Combine only accepted IDs with Phase 3's Home registry and Phase 2's `RegisterOutfitRow` registry. Treat `transitionVisualState` strictly as scalar landing/settled presentation and never as a selector.

Use a table-driven matrix for every selection/visibility/occlusion edge: one and multiple avatar-visible IDs; one and multiple surviving slots; disjoint visible layers; explicit outer-over-inner occlusion; completely hidden and partially/unresolved occlusion; equal-rank/tie and contradictory resolution; null coverage; empty/duplicate/unknown slots; empty/duplicate/unknown/missing visible ID; duplicate/missing garment match; `visibleOnAvatar: false`; `base.garments` extra/missing/reordered mutations; equipment collision; unsupported-cardinality; unavailable/absent bundle; both scalar visual states; and missing/duplicate/zero/stale source or row geometry. Only a listed, exactly resolved, fully verified visible garment with complete registration can proceed; every other row returns a stable static reason. Never infer from labels, categories, asset manifests, render position, CSS, Antrekkskart layout, or private screen state.
  </action>
  <verify>
    <automated>cmd.exe /d /s /c "npm test -- src/lib/outfit-transition/phase2-adapter.test.ts src/lib/outfit-transition/transition-snapshot.test.ts src/lib/outfit-transition/eligibility.test.ts && npx tsc -b --pretty false"</automated>
  </verify>
  <done>The adapter imports exact Phase-2 types, starts only from `base.avatar.visibleGarmentIds`, uses `base.garments` only for exact resolution/validation, and fails closed across the complete visibility/occlusion matrix.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze and independently review the Phase 2 binding</name>
  <files>.planning/phases/03-living-home-signature-transition/evidence/03-05-INDEPENDENT-REVIEW.md</files>
  <action>
Run adapter and foundation suites, typecheck, lint, `git diff --check`, and unchanged-dependency checks. Commit the adapter/tests only after the Phase 2 gate evidence is complete. Record the exact labeled fields required by `03-VALIDATION.md`: `phase3_candidate_sha`, `code_security_sha`, `code_security_status`, `ui_accessibility_sha`, `ui_accessibility_status`, `ancestry_status`, and `clean_status`. Both reviewer SHAs must equal the candidate; all statuses must be PASS. Require explicit review of exact import ownership, avatar-visible-ID selection, exact garment resolution, scalar `transitionVisualState`, `visibleOnAvatar`/`avatarCoverage`, every slot/rank/occlusion edge, equipment exclusion, separate registrations, factory provenance, absence of independent `base.garments` selection, and absence of DOM/CSS coupling. Any code edit invalidates both verdicts.
  </action>
  <verify>
    <automated>cmd.exe /d /s /c "npm test -- src/lib/outfit-transition/phase2-adapter.test.ts src/lib/outfit-transition/transition-snapshot.test.ts src/lib/outfit-transition/eligibility.test.ts && npx tsc -b --pretty false && npm run lint && git diff --exit-code HEAD^ HEAD -- package.json package-lock.json npm-shrinkwrap.json yarn.lock pnpm-lock.yaml"</automated>
  </verify>
  <done>The exact adapter candidate has a fresh-context PASS against the exact Phase 2 implementation candidate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phase 2 branch/history to Phase 3 | A moving branch or unreviewed successor must not masquerade as the approved contract. |
| Phase-2 avatar truth to transition set | Ignoring `base.avatar.visibleGarmentIds` or independently selecting from garments could animate hidden, unknown, or equipment entries. |
| DOM registration to identity | Elements are mutable and cannot establish identity by appearance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-13 | Spoofing | Phase 2 candidate gate | high | mitigate | Resolve a labeled full SHA, verify commit existence/ancestry, and require recorded independent PASS. |
| T-03-14 | Tampering | visibility/occlusion adapter | high | mitigate | Exact imported exports plus exhaustive edge tests; selection starts only at `base.avatar.visibleGarmentIds`, with exact garment resolution and coverage validation. |
| T-03-15 | Spoofing | anchor identity | high | mitigate | Branded item IDs plus separate Phase-3 Home and Phase-2 row registrations; no label or selector fallback. |
| T-03-SC | Tampering | npm installs | high | mitigate | Install nothing and verify dependency files remain unchanged. |
</threat_model>

<verification>
Run the Plan 03-05 commands in `03-VALIDATION.md`, compare the recorded Phase 2 SHA with the reviewed summary, and use source search to prove the adapter has no class-name, text-label, or query-selector dependency.
</verification>

<success_criteria>
- Integration is pinned to one exact independently passed Phase 2 candidate.
- All Phase 3 motion identity starts only from Phase 2's authoritative `base.avatar.visibleGarmentIds` and registration APIs.
- Unknown, null, hidden, occluded, ambiguous, equipment, and unregistered entries remain static.
- The exact Phase 3 adapter candidate has a fresh-context independent PASS.
</success_criteria>

<failure_and_rollback>
If the Phase 2 exact gate fails, stop without code changes and leave the foundation candidates usable. If adapter review fails, revert only its isolated commit; do not modify Phase 2 to force compatibility from this plan.
</failure_and_rollback>

<output>
Create `.planning/phases/03-living-home-signature-transition/03-05-SUMMARY.md` with exact `phase2_candidate_sha` and `phase3_candidate_sha: <40hex>`, gate/review evidence, commands, and rollback note.
</output>
