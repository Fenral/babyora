---
phase: 03-living-home-signature-transition
plan: "06"
type: execute
wave: 4
depends_on:
  - 01-18
  - 03-02
  - 03-04
  - 03-05
files_modified:
  - src/lib/outfit-transition/coordinator.ts
  - src/lib/outfit-transition/coordinator.test.ts
  - src/hooks/useOutfitTransitionCoordinator.ts
  - src/hooks/__tests__/useOutfitTransitionCoordinator.test.tsx
  - src/screens/HjemScreen.tsx
  - src/App.tsx
  - e2e/fixtures/home-outfit-motion.ts
  - e2e/home-outfit-motion.ts
  - .planning/phases/03-living-home-signature-transition/evidence/03-06-INDEPENDENT-REVIEW.md
autonomous: true
requirements:
  - MOTION-01
must_haves:
  truths:
    - "A deliberate Home CTA activation captures source geometry and consumes replay for the exact identity before navigation."
    - "The existing native-dialog navigation, immediate Outfit semantics, focus behavior, close/back behavior, and haptics remain authoritative."
    - "Home registers only the adapter's Phase-2-verified visible transition entries while Outfit uses Phase 2's `RegisterOutfitRow`; unknown, null, hidden, occluded, and equipment entries remain static."
    - "App passes the frozen Phase-2 `OutfitBundleProducerResult`, row registrar, and scalar `transitionVisualState: settled` without editing the Phase-2 screen; candidate selection remains inside the 03-05 avatar-truth adapter."
    - "This is the sole post-gate plan that wires the new Living Home composition into `HjemScreen.tsx`."
    - "Double activation, missing anchors, backgrounding, resize, close, and unmount settle without trapping input or focus."
  artifacts:
    - path: src/lib/outfit-transition/coordinator.ts
      provides: "Pure lifecycle state machine for capture, readiness, playback, abort, and settlement"
    - path: src/hooks/useOutfitTransitionCoordinator.ts
      provides: "App-lifetime DOM registry and lifecycle adapter"
    - path: src/App.tsx
      provides: "Ephemeral coordinator ownership across mounted Home and Outfit dialog"
  key_links:
    - from: src/screens/HjemScreen.tsx
      to: src/hooks/useOutfitTransitionCoordinator.ts
      via: "CTA activation and Phase-3-owned Home registrations"
    - from: src/App.tsx
      to: src/lib/outfit/outfit-transition-contract.ts
      via: "Frozen `outfitBundle`, `RegisterOutfitRow`, and visual-state props"
---

<objective>
Wire exact-identity capture and lifecycle coordination across Home and Outfit while preserving the verified navigation/accessibility contract, implementing D-03, D-04, D-06, D-07, and D-10.

Purpose: Establish a safe orchestration seam before any decorative overlay plays.
Output: A tested coordinator state machine, App-lifetime hook, Home/App integration, browser lifecycle coverage, and an independently reviewed exact-SHA candidate.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/BABYORA-UX-MOTION-BIBLE.md
@docs/motion-system.md
@.planning/phases/02-outfit-truth-antrekkskart/02-INTERFACE-CONTRACT.md
@.planning/phases/03-living-home-signature-transition/03-CONTEXT.md
@.planning/phases/03-living-home-signature-transition/03-VALIDATION.md
@.planning/phases/03-living-home-signature-transition/03-02-SUMMARY.md
@.planning/phases/03-living-home-signature-transition/03-04-SUMMARY.md
@.planning/phases/03-living-home-signature-transition/03-05-SUMMARY.md
@.planning/phases/01-planlegg-dagslinjen/01-18-SUMMARY.md
@src/App.tsx
@src/screens/HjemScreen.tsx
@src/lib/outfit/outfit-transition-contract.ts
@src/lib/outfit-transition/phase2-adapter.ts
</context>

<interfaces>
Before editing shared files, parse exact `candidate_sha` from the independently passed Phase-1 summary, normalize it internally to `phase1CandidateSha`, and require that candidate plus the exact Phase-2 candidate gated by 03-05. `App` owns one coordinator and replay ledger. `HjemScreen` uses Phase 3's internal `RegisterHomeAnchor` only for IDs accepted by the adapter from `base.avatar.visibleGarmentIds` after exact garment/coverage validation, and captures before `openOutfit`. The frozen Phase-2 screen already accepts `OutfitBundleProducerResult`, `RegisterOutfitRow`, and scalar `transitionVisualState`; Phase 3 imports those exact surfaces and edits neither that screen nor its components.

The coordinator exposes serializable state for `idle`, `captured`, `ready`, `playing`, and `settled`, plus an `abort(reason)` path. Navigation and focus never wait for state changes. The overlay consumer is added only in 03-07.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the transition lifecycle state machine and hook</name>
  <files>src/lib/outfit-transition/coordinator.ts, src/lib/outfit-transition/coordinator.test.ts, src/hooks/useOutfitTransitionCoordinator.ts, src/hooks/__tests__/useOutfitTransitionCoordinator.test.tsx</files>
  <behavior>
    - "First deliberate activation atomically consumes replay, captures a valid source snapshot, and allows navigation."
    - "Repeated activation of the same triple settles statically and cannot recapture."
    - "Complete matching target registration advances captured to ready; partial or mismatched registration settles statically."
    - "Visibility loss, pagehide, resize/orientation change, close, identity refresh, and unmount settle and clear transient DOM references."
    - "Reduced motion and invalid capture preserve the semantic navigation path while skipping playback."
  </behavior>
  <action>
Write pure state-machine tests and hook lifecycle tests first, then implement its reducer/commands and a thin React hook around refs, lifecycle listeners, replay ledger, adapter, capture, and eligibility. Capture Home rectangles synchronously only for IDs emitted by the 03-05 adapter during deliberate CTA activation. The coordinator never independently enumerates `base.garments` or promotes a null/unknown/hidden/occluded/equipment entry. Consume the exact triple before choosing animated versus static behavior per D-07. Accept Phase-2 `RegisterOutfitRow` callbacks after navigation, evaluate the complete candidate only after semantic Outfit is mounted at T0, and expose settled output without controlling dialog visibility or focus. Abort idempotently on every lifecycle change and clear DOM references after settlement. Reuse the post-Phase-1 haptic path unchanged; reduced motion must not alter the verified haptic policy.
  </action>
  <verify>
    <automated>cmd.exe /d /s /c "npm test -- src/lib/outfit-transition/coordinator.test.ts src/hooks/__tests__/useOutfitTransitionCoordinator.test.tsx src/lib/outfit-transition/replay-policy.test.ts src/lib/outfit-transition/eligibility.test.ts && npx tsc -b --pretty false"</automated>
  </verify>
  <done>The coordinator is deterministic, one-attempt, interruptible, and unable to block navigation, focus, or semantic UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register Home sources and own the coordinator in App</name>
  <files>src/screens/HjemScreen.tsx, src/App.tsx, e2e/fixtures/home-outfit-motion.ts, e2e/home-outfit-motion.ts</files>
  <behavior>
    - "Home scene garment nodes register exact branded outfit item IDs through `RegisterHomeAnchor`."
    - "Living Home decoration is wired from the same current snapshot only after both exact upstream gates."
    - "CTA activation captures before the existing dialog opens, and Outfit semantics/focus appear without animation delay."
    - "A garment omitted from visible Home has no invented source or flight; uncertainty about whether it should be visible causes static settlement."
    - "Closing and reopening the same exact triple does not reattempt the explanation."
    - "Device/back-style close and rapid double activation leave the CTA and dialog operable."
  </behavior>
  <action>
As the sole Home wiring executor, render `LivingHomeAtmosphere` from the exact current recommendation snapshot without changing recommendation or CTA semantics. Carry only adapter-accepted branded IDs into transition-capable Home nodes and register them through `RegisterHomeAnchor`; never use labels or indexes as identity or select additional IDs from `base.garments`. In `App`, create one hook instance and pass the already-produced Phase-2 `outfitBundle`, `registerOutfitRow`, and `transitionVisualState: "settled"` through the frozen props while separately passing `registerHomeAnchor` to Home. Capture before the existing open handler and retain focus return. Do not edit `PaakledningScreen.tsx` or any Phase-2 component. Create the deterministic browser harness and cover 320/390, 200%, forced colors, theme/temperature combinations, semantic T0, landing-before-explanation, replay, missing source, every static visibility/occlusion outcome, and close/back recovery. Do not render the overlay in this plan.
  </action>
  <verify>
    <automated>cmd.exe /d /s /c "npm test -- src/lib/outfit-transition/coordinator.test.ts src/hooks/__tests__/useOutfitTransitionCoordinator.test.tsx && npx tsx e2e/home-outfit-motion.ts --case coordinator && npx tsc -b --pretty false"</automated>
  </verify>
  <done>Real source/target registrations reach one App-lifetime coordinator while existing navigation, focus, haptics, and recovery remain intact.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze and independently review coordinator integration</name>
  <files>.planning/phases/03-living-home-signature-transition/evidence/03-06-INDEPENDENT-REVIEW.md</files>
  <action>
Run coordinator/foundation suites, the focused browser lifecycle cases, `npm run e2e`, typecheck, lint, `git diff --check`, and dependency-file checks in the isolated worktree. Commit declared implementation paths and record the exact labeled fields required by `03-VALIDATION.md`: `phase3_candidate_sha`, `code_security_sha`, `code_security_status`, `ui_accessibility_sha`, `ui_accessibility_status`, `ancestry_status`, and `clean_status`. Both reviewer SHAs must equal the candidate; all statuses must be PASS. Review capture timing, replay, branded IDs, separate registrars, Home same-snapshot wiring, lifecycle cleanup, semantic T0, landing-before-explanation, focus, haptics, and static fallback using only text/DOM/ARIA/rectangle/timing logs. Any edit invalidates both verdicts.
  </action>
  <verify>
    <automated>cmd.exe /d /s /c "npm test -- src/lib/outfit-transition/coordinator.test.ts src/hooks/__tests__/useOutfitTransitionCoordinator.test.tsx src/lib/outfit-transition/replay-policy.test.ts src/lib/outfit-transition/eligibility.test.ts && npx tsx e2e/home-outfit-motion.ts --case coordinator && npm run e2e && npx tsc -b --pretty false && npm run lint && git diff --check"</automated>
  </verify>
  <done>The exact coordinator integration candidate has a fresh-context independent PASS with no navigation, focus, replay, or lifecycle regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User activation to source capture | DOM and recommendation state can change between pointer/keyboard activation and navigation. |
| Home registrations to Outfit registrations | Elements live in different semantic surfaces and mount at different times. |
| Browser lifecycle to coordinator | Background, resize, close, and unmount can interrupt any state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-16 | Tampering | capture timing | high | mitigate | Synchronous pre-navigation normalized capture tied to the exact identity triple. |
| T-03-17 | Spoofing | DOM registration | high | mitigate | Branded IDs plus separate Phase-3 Home and Phase-2 row registrars; cleanup on element replacement/unmount. |
| T-03-18 | Denial of Service | coordinator lifecycle | high | mitigate | Idempotent abort/settlement, single active candidate, bounded listeners, and rapid-action tests. |
| T-03-19 | Repudiation | fallback outcomes | low | mitigate | Stable reason codes retained in test/debug state without logging user data. |
| T-03-SC | Tampering | npm installs | high | mitigate | No installs; dependency files remain unchanged. |
</threat_model>

<verification>
Run the Plan 03-06 commands in `03-VALIDATION.md`; inspect listener cleanup and prove the dialog open/focus call path has no await on coordinator or animation state.
</verification>

<success_criteria>
- Source geometry is captured before navigation for one exact triple.
- Target readiness uses only Phase 2 public registration.
- Replay, reduced motion, missing geometry, lifecycle interruption, close/back, and rapid activation settle safely.
- Existing dialog, focus, CTA, and haptic behavior remain authoritative.
- The exact candidate SHA has a fresh-context independent PASS.
</success_criteria>

<failure_and_rollback>
If coordinator wiring regresses navigation, focus, haptics, or cleanup, revert the isolated candidate to the independently passed 03-04/03-05 bases. Home and Outfit remain fully usable without the coordinator or overlay.
</failure_and_rollback>

<output>
Create `.planning/phases/03-living-home-signature-transition/03-06-SUMMARY.md` with exact `phase3_candidate_sha: <40hex>`, dependency SHAs, lifecycle matrix, review evidence, and rollback note.
</output>
