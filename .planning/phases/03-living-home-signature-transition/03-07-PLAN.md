---
phase: 03-living-home-signature-transition
plan: "07"
type: execute
wave: 5
depends_on:
  - 03-06
files_modified:
  - src/components/outfit-transition/OutfitTransitionOverlay.tsx
  - src/components/outfit-transition/OutfitTransitionOverlay.css
  - src/components/outfit-transition/OutfitTransitionOverlay.test.tsx
  - src/styles/motion-grammar.ts
  - src/App.tsx
  - e2e/home-outfit-motion.ts
  - .planning/phases/03-living-home-signature-transition/evidence/03-07-INDEPENDENT-REVIEW.md
autonomous: true
requirements:
  - MOTION-01
must_haves:
  truths:
    - "On one eligible first activation, only IDs selected from `base.avatar.visibleGarmentIds` and accepted by the 03-05 adapter travel into their exact Outfit rows."
    - "The semantic Outfit screen, heading, rows, controls, and focus are present at navigation time zero; the overlay is decorative and pointer-inert, per D-03."
    - "Unknown/null/missing/duplicate/hidden/occluded entries and equipment remain static; unlisted `base.garments` entries never become candidates, per D-05."
    - "Reduced motion, unsupported animation, stale geometry, interruption, and replay all show the settled Outfit immediately."
    - "No legacy multi-second takeover, delayed chip reveal, or persistent replay flag remains in the current Home-to-Outfit path, per D-06 and D-07."
  artifacts:
    - path: src/components/outfit-transition/OutfitTransitionOverlay.tsx
      provides: "Semantic-hidden, finite FLIP-style garment explanation"
    - path: src/components/outfit-transition/OutfitTransitionOverlay.css
      provides: "Transform/opacity-only bounded visual treatment"
    - path: src/App.tsx
      provides: "Frozen Phase-2 props plus overlay state orchestration"
  key_links:
    - from: src/App.tsx
      to: src/components/outfit-transition/OutfitTransitionOverlay.tsx
      via: "Validated ready coordinator snapshot and terminal callbacks"
    - from: src/components/outfit-transition/OutfitTransitionOverlay.tsx
      to: src/lib/outfit-transition/timeline.ts
      via: "Single bounded duration and overlapping per-item offsets"
    - from: src/App.tsx
      to: src/lib/outfit/outfit-transition-contract.ts
      via: "Pass frozen bundle, row registrar, and visual state without editing Phase 2"
---

<objective>
Deliver the semantic-first signature garment transition from Home into Outfit, implementing D-03 through D-07 while preserving all verified Phase 1 and Phase 2 behavior.

Purpose: Explain the recommendation spatially without turning animation into a navigation gate or changing outfit truth.
Output: A bounded transform/opacity overlay, immediate Outfit integration, lifecycle cleanup, browser proof, and an independently reviewed exact-SHA candidate.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/BABYORA-UX-MOTION-BIBLE.md
@docs/motion-system.md
@docs/INSTRUMENT-DESIGN-SYSTEM.md
@docs/signature.md
@.planning/phases/02-outfit-truth-antrekkskart/02-INTERFACE-CONTRACT.md
@.planning/phases/03-living-home-signature-transition/03-CONTEXT.md
@.planning/phases/03-living-home-signature-transition/03-VALIDATION.md
@.planning/phases/03-living-home-signature-transition/03-06-SUMMARY.md
@src/App.tsx
@src/lib/outfit-transition/timeline.ts
@src/hooks/useOutfitTransitionCoordinator.ts
</context>

<interfaces>
`OutfitTransitionOverlay` receives only an already validated immutable candidate, garment presentation data from the Phase 2 adapter, the finite Phase 3 timeline, and `onFinish`/`onAbort` callbacks. It uses the existing `motion/react` API and never reads live DOM, weather, storage, or navigation state.

The frozen Phase-2 screen already accepts `OutfitBundleProducerResult`, optional `RegisterOutfitRow`, and scalar `transitionVisualState: "settled" | "landing"`. Phase 3 changes only `App`: it passes `"landing"` during the overlay and `"settled"` after completion/cancellation. The overlay consumes only the 03-05 adapter output selected from `base.avatar.visibleGarmentIds`; it never independently selects from `base.garments`. Unsupported/unavailable bundles plus unknown, null, missing, duplicate, hidden, occluded, ambiguous, or equipment entries settle statically. Phase 2 guarantees semantic mount, heading focus, content, controls, and row registration at T0.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render a finite semantic-hidden garment overlay</name>
  <files>src/components/outfit-transition/OutfitTransitionOverlay.tsx, src/components/outfit-transition/OutfitTransitionOverlay.css, src/components/outfit-transition/OutfitTransitionOverlay.test.tsx, src/styles/motion-grammar.ts</files>
  <behavior>
    - "Each validated garment produces one aria-hidden pointer-inert visual clone positioned from its source rectangle."
    - "Each clone reaches its exact target rectangle using transform and opacity within the shared bounded timeline."
    - "Per-item offsets overlap and total completion never exceeds 1400 ms."
    - "Motion rendering unavailability, reduced motion, cancel, or unmount synchronously invokes terminal settlement once."
    - "Equipment, obscured inner/middle garments, and any item outside the explicit visible validated garment set never render in the overlay."
  </behavior>
  <action>
Write component/motion contract tests first using controllable Motion props and callbacks. Extend the active `motion-grammar.ts` with canonical 220 ms normal-interaction and 1250 ms explanatory values, then pass that contract into the pure 03-02 timeline; do not create another timing table. Implement a fixed-position portal overlay with `motion.div`, `MotionConfig`, and `AnimatePresence`. Render one token-driven clone for every validated traveling garment; mark the root `aria-hidden`, pointer-inert, and non-focusable. Start from immutable source rectangles and animate transform/scale/opacity toward immutable target rectangles with Motion and overlapping offsets under the one total clock. Obscured inner/middle garments and equipment remain static semantic Outfit content. Avoid layout-property animation, layout reads during playback, or meaning attached to clone text. Complete exactly once through Motion completion/cancel/unmount state and remove portal DOM. If Motion rendering or required geometry is unavailable, settle statically immediately.
  </action>
  <verify>
    <automated>cmd.exe /d /s /c "npm test -- src/components/outfit-transition/OutfitTransitionOverlay.test.tsx src/lib/outfit-transition/timeline.test.ts src/lib/outfit-transition/eligibility.test.ts && npx tsc -b --pretty false"</automated>
  </verify>
  <done>The overlay is finite, exact-item, transform/opacity-only, semantic-hidden, pointer-inert, and leak-free across completion and abort.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate semantic-first playback into Outfit</name>
  <files>src/App.tsx, e2e/home-outfit-motion.ts</files>
  <behavior>
    - "Eligible first activation mounts and focuses semantic Outfit before the overlay begins."
    - "Visual garment clones converge into exact registered rows, then the settled Outfit remains unchanged."
    - "Same-triple reopen, reduced motion, missing target, resize, backgrounding, close, and device-back paths skip or abort to the same settled Outfit."
    - "Keyboard activation and rapid double activation remain operable and create no duplicate overlay."
    - "Outfit controls are usable immediately and remain usable after interruption."
  </behavior>
  <action>
Render the overlay from `App` only when the coordinator reports one fully validated adapter candidate; navigation and focus never await playback. Pass the frozen Phase-2 `outfitBundle` and `registerOutfitRow` unchanged, and toggle only scalar `transitionVisualState` from `"landing"` to `"settled"` around Motion completion/cancellation. Do not edit `PaakledningScreen.tsx` or any Phase-2 file. Remove any App-owned superseded replay/delay path, retain 180–250 ms normal feedback, and extend `--case signature` checks for exact convergence, duration, replay, keyboard, reduced motion, missing anchors, lifecycle aborts, semantic T0, landing-before-explanation, focus, post-abort interaction, every visibility/occlusion static edge, and proof that unlisted/reordered `base.garments` cannot replace avatar-visible-ID selection/order.
  </action>
  <verify>
    <automated>cmd.exe /d /s /c "npm test -- src/components/outfit-transition/OutfitTransitionOverlay.test.tsx src/lib/outfit-transition/coordinator.test.ts && npx tsx e2e/home-outfit-motion.ts --case signature && npx tsc -b --pretty false"</automated>
  </verify>
  <done>The one eligible first activation explains exact garments within the locked duration while every fallback and interruption exposes the same immediate semantic Outfit.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze and independently review the signature transition</name>
  <files>.planning/phases/03-living-home-signature-transition/evidence/03-07-INDEPENDENT-REVIEW.md</files>
  <action>
Run all transition unit/component suites, the focused deterministic browser cases, `npm run e2e`, typecheck, lint, build, `git diff --check`, and unchanged-dependency checks in the isolated worktree. Commit declared implementation/test paths and record the exact labeled fields required by `03-VALIDATION.md`: `phase3_candidate_sha`, `code_security_sha`, `code_security_status`, `ui_accessibility_sha`, `ui_accessibility_status`, `ancestry_status`, and `clean_status`. Both reviewer SHAs must equal the candidate; all statuses must be PASS. Require text/DOM/ARIA/rectangle/timing-log evidence for semantic T0, landing-before-explanation, exact destinations, equipment exclusion, duration, reduced motion, replay, cleanup, keyboard/focus, and superseded-takeover absence. Any implementation correction invalidates both verdicts.
  </action>
  <verify>
    <automated>cmd.exe /d /s /c "npm test -- src/lib/outfit-transition src/components/outfit-transition && npx tsx e2e/home-outfit-motion.ts --case all && npm run e2e && npx tsc -b --pretty false && npm run lint && npm run build && git diff --check"</automated>
  </verify>
  <done>The exact signature-transition candidate has a fresh-context independent PASS with no unresolved critical/high finding or semantic-navigation regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Validated snapshot to decorative portal | Overlay content is visual duplication and must never acquire semantics or input authority. |
| Animation lifecycle to dialog lifecycle | Close, back, visibility, resize, and unmount can happen while animations exist. |
| Phase 3 presentation to Phase 2 truth | Motion must preserve garment/equipment and exact-row meaning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-20 | Spoofing | visual destination | high | mitigate | Render only the fully validated exact-ID candidate and its immutable target rectangles. |
| T-03-21 | Elevation of Privilege | overlay DOM | high | mitigate | `aria-hidden`, no focusable nodes, pointer-events disabled, and no navigation/control callbacks. |
| T-03-22 | Denial of Service | animation objects/portal | high | mitigate | One active candidate, finite API animations, idempotent terminal callback, and full cleanup on every lifecycle exit. |
| T-03-23 | Tampering | semantic Outfit visual state | medium | mitigate | Explicit prop/state; never hide, inert, or remove semantic content during decoration. |
| T-03-SC | Tampering | npm installs | high | mitigate | Use browser primitives only; verify dependency files remain unchanged. |
</threat_model>

<verification>
Run the Plan 03-07 commands in `03-VALIDATION.md`; inspect browser timing artifacts and accessibility snapshots, and verify the final Outfit DOM/value state is identical after animated and every static/aborted path.
</verification>

<success_criteria>
- MOTION-01 is visible on one eligible first exact-triple activation.
- Garments land in exact Phase 2 rows; equipment and unverified items never masquerade as garments.
- Semantic navigation, heading focus, controls, and accessibility are immediate.
- Total explanation is 900-1400 ms; normal feedback is 180-250 ms.
- All fallback/interruption paths settle cleanly, and the exact SHA has an independent PASS.
</success_criteria>

<failure_and_rollback>
If exact mapping, accessibility, timing, or cleanup fails, disable promotion and revert the isolated overlay/integration commit to the verified 03-06 candidate. Home-to-Outfit navigation and the Phase 2 Outfit screen remain fully functional through static settlement.
</failure_and_rollback>

<output>
Create `.planning/phases/03-living-home-signature-transition/03-07-SUMMARY.md` with exact `phase3_candidate_sha: <40hex>`, dependency SHAs, timing/accessibility logs, independent review, and rollback note.
</output>
