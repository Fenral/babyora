---
phase: 03-living-home-signature-transition
plan: "03"
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - src/lib/outfit-transition/transition-snapshot.ts
  - src/lib/outfit-transition/transition-snapshot.test.ts
  - src/lib/outfit-transition/eligibility.ts
  - src/lib/outfit-transition/eligibility.test.ts
  - scripts/verify-phase3-exact-sha.mjs
  - scripts/__tests__/verify-phase3-exact-sha.test.ts
  - .planning/phases/03-living-home-signature-transition/evidence/03-03-INDEPENDENT-REVIEW.md
autonomous: true
requirements:
  - MOTION-01
must_haves:
  truths:
    - "A transition candidate is immutable and tied to one exact weather, recommendation, and outfit identity triple."
    - "Animation is allowed only when identities, source geometry, target geometry, viewport state, and motion preference are all verified, per D-04."
    - "Any invalid or stale condition returns an explicit static-settlement reason instead of guessing."
    - "Eligibility decisions are pure and independent of Phase 2 implementation names, per D-09."
  artifacts:
    - path: src/lib/outfit-transition/transition-snapshot.ts
      provides: "Serializable immutable identity and geometry capture contract"
    - path: src/lib/outfit-transition/eligibility.ts
      provides: "Pure fail-closed animation decision"
    - path: scripts/verify-phase3-exact-sha.mjs
      provides: "Tested exact-SHA verifier with external-evidence path confinement"
  key_links:
    - from: src/lib/outfit-transition/transition-snapshot.ts
      to: src/lib/outfit-transition/eligibility.ts
      via: "Validated capture is the only geometry accepted by eligibility"
    - from: scripts/verify-phase3-exact-sha.mjs
      to: BABYORA_PHASE3_EVIDENCE_ROOT
      via: "Argument-array reads of one external candidate record, two external review records, and the external validation bundle"
---

<objective>
Create the Phase-2-independent snapshot and fail-closed eligibility core required by D-03, D-04, and D-09.

Purpose: Prevent visually polished motion from ever explaining the wrong recommendation or relying on stale geometry.
Output: Immutable transition capture types, strict geometry validation, a pure eligibility decision, tests, and an independently reviewed exact-SHA candidate.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/BABYORA-UX-MOTION-BIBLE.md
@docs/motion-system.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-living-home-signature-transition/03-CONTEXT.md
@.planning/phases/03-living-home-signature-transition/03-RESEARCH.md
@.planning/phases/03-living-home-signature-transition/03-DEPENDENCY-CONTRACT.md
@.planning/phases/03-living-home-signature-transition/03-VALIDATION.md
</context>

<interfaces>
Use local structural types in this foundation plan: an ordered three-string transition identity, normalized viewport metadata, item IDs, and plain finite rectangles. Do not import Phase 2 or duplicate its domain model. Plan 03-05 will be the sole adapter from the exact verified Phase 2 public contract.

Eligibility returns a discriminated decision with either `animate` and a validated immutable snapshot, or `settle-static` and a stable reason code suitable for diagnostics and tests.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture immutable identity and source geometry</name>
  <files>src/lib/outfit-transition/transition-snapshot.ts, src/lib/outfit-transition/transition-snapshot.test.ts</files>
  <behavior>
    - "A complete exact identity triple and finite positive rectangles produce a deeply immutable capture."
    - "Zero-size, non-finite, off-contract, duplicate-item, or missing rectangles are rejected."
    - "Viewport dimensions and scroll coordinates are captured with geometry so later comparison can detect staleness."
    - "Caller-owned arrays, maps, and rectangles cannot mutate a completed capture."
  </behavior>
  <action>
Write tests first, then create serializable capture types and pure constructors for identity, viewport, and item rectangles. Normalize DOM-like rectangles into plain numeric records and deep-copy ordered item entries. Reject incomplete identity fields, duplicate item IDs, non-finite values, non-positive dimensions, and absent viewport dimensions. Preserve capture order for deterministic item staggering but never derive semantic garment truth here. The browser coordinator will read DOM rectangles once at deliberate activation and pass only normalized values into this module.
  </action>
  <verify>
    <automated>npm test -- src/lib/outfit-transition/transition-snapshot.test.ts</automated>
  </verify>
  <done>A valid capture cannot be mutated after construction, and every malformed geometry/identity case fails with a typed reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Decide animation eligibility conservatively</name>
  <files>src/lib/outfit-transition/eligibility.ts, src/lib/outfit-transition/eligibility.test.ts</files>
  <behavior>
    - "Exact identity, matching item set, verified source and target geometry, visible document, stable viewport, and motion allowed yields animate."
    - "Any identity mismatch yields static settlement."
    - "Any missing, duplicate, stale, zero-size, or non-finite anchor yields static settlement."
    - "Reduced motion, hidden document, aborted lifecycle, or changed viewport yields static settlement."
    - "An empty garment set never starts decorative motion."
  </behavior>
  <action>
Write a table-driven test matrix first, then implement one pure function that validates the complete candidate just before playback. Compare all three identity components exactly, compare expected garment IDs as sets while preserving source order for playback, require both source and target rectangles for every expected garment, and compare captured/current viewport facts. Treat reduced motion, background state, lifecycle abort, and empty sets as safe static settlement. Return stable reason codes for automated evidence. Do not partially animate a subset, infer missing anchors, query DOM, or accept timestamps as identity. This implements D-04.
  </action>
  <verify>
    <automated>npm test -- src/lib/outfit-transition/eligibility.test.ts</automated>
  </verify>
  <done>Every unverified or stale state fails closed with a deterministic reason; only a completely verified candidate can animate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the fail-fast exact-SHA verifier and review the core</name>
  <files>scripts/verify-phase3-exact-sha.mjs, scripts/__tests__/verify-phase3-exact-sha.test.ts, .planning/phases/03-living-home-signature-transition/evidence/03-03-INDEPENDENT-REVIEW.md</files>
  <behavior>
    - "Phase-2 handoff mode requires labeled status PASS, feature_flag true, a full candidate SHA, commit existence, and ancestry."
    - "Candidate mode directly requires detached HEAD equal to the labeled candidate and an empty porcelain status."
    - "Candidate mode runs merge-base ancestry for every supplied dependency and rejects zero or omitted expected dependencies when a count is declared."
    - "Candidate mode requires an absolute `BABYORA_PHASE3_EVIDENCE_ROOT` that resolves outside the detached candidate checkout and reads one external candidate record plus two separate external reviewer records and one external validation bundle."
    - "The Phase-1 summary is parsed only from exact `candidate_sha`, then normalized to an internal `phase1CandidateSha`; alternate aliases are rejected."
    - "Two external reviewer records require distinct reviewer IDs and session IDs, fork_turns none, fresh_context true, the same candidate SHA, the same computed evidence SHA-256, and PASS."
    - "Every Git/process/read/hash failure exits nonzero immediately; recorded ancestry/clean booleans are not trusted as proof."
  </behavior>
  <action>
Write verifier tests first using a temporary parent directory containing a detached Git candidate and a distinct sibling evidence directory whose path includes spaces. Cover missing/empty/relative `BABYORA_PHASE3_EVIDENCE_ROOT`, evidence root equal to or nested under the checkout, symlink/junction resolution back into the checkout, missing or aliased record paths, a combined review record, dirty tracked/untracked state, wrong HEAD, attached HEAD, wrong dependency count, nonancestor dependency, malformed exact labels, duplicate reviewer/session IDs, inherited context, mismatched SHA/hash/verdict, and any record/bundle inside the candidate. Test exact Phase-1 `candidate_sha` acceptance plus rejection of alternate or duplicate aliases.

Implement a Node ESM CLI using `execFileSync` argument arrays and explicit nonzero propagation, never shell interpolation. Provide `phase2-handoff` and `candidate` modes. Candidate mode requires `BABYORA_PHASE3_EVIDENCE_ROOT`, resolves it with filesystem-realpath semantics, proves it is absolute and outside the detached repository/worktree, and requires each supplied candidate/review/bundle path to resolve beneath that external root. It reads separate candidate, code/security-review, and UI/accessibility-review JSON files. It must compare `git rev-parse HEAD` directly with the candidate record, require detached HEAD, require empty `git status --porcelain`, run `git diff-tree --check`, run `git merge-base --is-ancestor` for every declared dependency, parse Phase 1 only from exact `candidate_sha` and normalize it internally to `phase1CandidateSha`, compute SHA-256 only over the external deterministic validation bundle, and validate distinct labeled reviewer/session/fresh-context records including `fork_turns: none`. The candidate record and reviews are inputs to verification but are not part of the Git candidate; the summary is never an input to candidate or evidence hashing. Export pure parsers/path guards for unit tests. Then freeze/review the 03-03 candidate using an orchestrator-created external evidence root and the exact fields in `03-VALIDATION.md`; any correction invalidates both verdicts.
  </action>
  <verify>
    <automated>cmd.exe /d /s /c "npm test -- src/lib/outfit-transition/transition-snapshot.test.ts src/lib/outfit-transition/eligibility.test.ts scripts/__tests__/verify-phase3-exact-sha.test.ts && npx tsc -b --pretty false && git diff --exit-code HEAD^ HEAD -- package.json package-lock.json npm-shrinkwrap.json yarn.lock pnpm-lock.yaml"</automated>
  </verify>
  <done>The exact candidate SHA has a fresh-context PASS demonstrating that animation cannot proceed on partial or mismatched truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| DOM geometry to immutable capture | Layout values can be stale, zero, non-finite, or detached. |
| Source capture to target eligibility | Navigation and data refresh can change identities, viewport, or item membership. |
| Repository to dependency registry | No package-manager install is authorized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-07 | Tampering | identity and geometry capture | high | mitigate | Deep-copy, validate finite rectangles, reject duplicates, and expose immutable records. |
| T-03-08 | Spoofing | identity comparison | high | mitigate | Exact three-part comparison with no timestamp, label, or positional substitution. |
| T-03-09 | Denial of Service | eligibility/item set | medium | mitigate | Linear bounded validation and rejection of empty or malformed collections before animation. |
| T-03-SC | Tampering | npm installs | high | mitigate | Install nothing and verify dependency files remain unchanged. |
</threat_model>

<verification>
Run the Plan 03-03 commands in `03-VALIDATION.md`, inspect mutation attempts in tests, and confirm every ineligible branch produces semantic UI settlement rather than an exception requirement.
</verification>

<success_criteria>
- Snapshot and eligibility contracts are deterministic and independent of Phase 2.
- Only a fully matched identity and geometry set can animate.
- Reduced motion and lifecycle disruptions are first-class static outcomes.
- The exact candidate SHA has a fresh-context independent PASS.
</success_criteria>

<failure_and_rollback>
If any fail-closed case is ambiguous or accepts partial truth, reject the candidate. Revert its isolated commit; the foundation is unwired and has no runtime or persistent side effects.
</failure_and_rollback>

<output>
Create `.planning/phases/03-living-home-signature-transition/03-03-SUMMARY.md` with exact `phase3_candidate_sha: <40hex>`, verifier test evidence, review evidence, commands, and rollback note.
</output>
