---
phase: 03-living-home-signature-transition
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/outfit-transition/timeline.ts
  - src/lib/outfit-transition/timeline.test.ts
  - src/lib/outfit-transition/replay-policy.ts
  - src/lib/outfit-transition/replay-policy.test.ts
  - .planning/phases/03-living-home-signature-transition/evidence/03-02-INDEPENDENT-REVIEW.md
autonomous: true
requirements:
  - MOTION-01
must_haves:
  truths:
    - "The explanatory transition accepts the active motion grammar's 1250 ms default and enforces one finite 900-1400 ms timeline, per D-06."
    - "Ordinary interface feedback uses the 180-250 ms band and is never coupled to the explanatory sequence."
    - "Reduced motion, backgrounding, cancellation, and replay exhaustion resolve immediately to settled semantic UI."
    - "One exact recommendation identity triple can trigger at most one attempt during an App lifetime, per D-07."
  artifacts:
    - path: src/lib/outfit-transition/timeline.ts
      provides: "Deterministic finite timing and progress model parameterized by the active motion grammar"
    - path: src/lib/outfit-transition/replay-policy.ts
      provides: "In-memory exact-triple attempt ledger"
  key_links:
    - from: src/lib/outfit-transition/replay-policy.ts
      to: src/lib/outfit-transition/timeline.ts
      via: "Eligibility consumes an identity before either animated or static settlement begins"
---

<objective>
Define the Phase-2-independent timing and replay contracts for the Home-to-Outfit explanation, implementing D-06, D-07, and D-09.

Purpose: Make the signature motion finite, interruptible, testable, and unable to surprise a user with repeated playback.
Output: Pure timeline and in-memory replay-policy modules with deterministic tests and an independently reviewed exact-SHA candidate.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/BABYORA-UX-MOTION-BIBLE.md
@docs/motion-system.md
@docs/INSTRUMENT-DESIGN-SYSTEM.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-living-home-signature-transition/03-CONTEXT.md
@.planning/phases/03-living-home-signature-transition/03-RESEARCH.md
@.planning/phases/03-living-home-signature-transition/03-VALIDATION.md
@src/styles/motion-grammar.ts
</context>

<interfaces>
The timeline module owns constants and pure phase calculations only. Default explanatory duration is 1250 ms and must remain inside 900-1400 ms for supported item counts. Normal interface feedback uses 220 ms unless an existing active token within 180-250 ms is more appropriate.

The replay ledger key is the exact ordered Phase 2 identity triple `snapshotId + recommendationFingerprint + transitionContextId`. It is in-memory for the mounted App lifetime. The first deliberate Home activation consumes the key whether animation is eligible or must settle statically; a different exact triple is independently eligible once.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Model one bounded and cancellable explanatory timeline</name>
  <files>src/lib/outfit-transition/timeline.ts, src/lib/outfit-transition/timeline.test.ts</files>
  <behavior>
    - "One through ten garment moves fit within a 1250 ms default total."
    - "Every supported explanatory total remains between 900 and 1400 ms."
    - "Item offsets overlap and converge rather than adding serial seconds."
    - "Cancel, visibility loss, and reduced motion settle progress at the final state."
    - "Normal UI duration is 220 ms and stays between 180 and 250 ms."
  </behavior>
  <action>
Write tests first, then implement pure functions for overall progress, per-item delay/progress, and terminal settlement. Require callers to supply the canonical timing contract and validate that normal and explanatory values remain within D-06; use 220 ms and 1250 ms in the locked contract tests. Use a single clock and overlapping offsets so item count cannot stretch the sequence. Do not create a third timing-token source or mutate the shared motion grammar in this independent foundation candidate; Plan 03-07 will add the two canonical values to the active grammar after shared-file gates clear. Model cancellation and reduced motion as terminal settlement, not reverse playback or a second timeline. Do not add timers, animation libraries, or compatibility choreography for the existing multi-second takeover.
  </action>
  <verify>
    <automated>npm test -- src/lib/outfit-transition/timeline.test.ts</automated>
  </verify>
  <done>The timeline is pure, finite, interruption-safe, and exhaustively bounded for supported garment counts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce one attempt per exact identity triple</name>
  <files>src/lib/outfit-transition/replay-policy.ts, src/lib/outfit-transition/replay-policy.test.ts</files>
  <behavior>
    - "A never-seen exact triple can be consumed once."
    - "The same triple cannot be consumed twice even if the first attempt uses static fallback or is interrupted."
    - "Changing any one of the three identities creates a distinct eligible key."
    - "Malformed or incomplete identity does not animate and cannot collide with a valid key."
    - "The ledger requires no persistent storage and can be scoped to one App lifetime."
  </behavior>
  <action>
Write tests first, then implement canonical tuple-key construction and an injectable in-memory attempt ledger. Make consumption atomic from the coordinator's perspective: the deliberate activation checks and consumes once before animation selection. Return explicit decisions for `animate`, `settle-static`, and invalid identity without persisting timestamps, weather content, garment content, or user data. Do not use localStorage, sessionStorage, cookies, database state, or a module-global singleton; the App will own one ledger instance per mounted lifetime. This implements the replay meaning chosen in D-07.
  </action>
  <verify>
    <automated>npm test -- src/lib/outfit-transition/replay-policy.test.ts</automated>
  </verify>
  <done>Replay is deterministic, privacy-preserving, App-lifetime scoped, and conservative across fallback and interruption paths.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze and independently review timing and replay</name>
  <files>.planning/phases/03-living-home-signature-transition/evidence/03-02-INDEPENDENT-REVIEW.md</files>
  <action>
Use the isolated Phase 3 worktree per D-10. Run both focused suites, typecheck, `git diff --check`, scope, and unchanged-dependency checks, then commit only declared paths. Record the exact labeled fields required by `03-VALIDATION.md`: `phase3_candidate_sha`, `code_security_sha`, `code_security_status`, `ui_accessibility_sha`, `ui_accessibility_status`, `ancestry_status`, and `clean_status`. Both reviewer SHAs must equal the candidate and all statuses must be PASS. Challenge the full item-count range, interruption settlement, identity collisions, persistence absence, and the 1400 ms ceiling. Any code edit invalidates both verdicts.
  </action>
  <verify>
    <automated>cmd.exe /d /s /c "npm test -- src/lib/outfit-transition/timeline.test.ts src/lib/outfit-transition/replay-policy.test.ts && npx tsc -b --pretty false && git diff --exit-code HEAD^ HEAD -- package.json package-lock.json npm-shrinkwrap.json yarn.lock pnpm-lock.yaml"</automated>
  </verify>
  <done>The immutable timing/replay candidate has a fresh-context PASS with no unresolved critical or high finding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Recommendation identities to replay ledger | Untrusted, missing, or adversarially similar strings determine replay eligibility. |
| Browser lifecycle to transition timeline | Visibility, cancellation, unmount, and reduced-motion changes can occur at any point. |
| Repository to dependency registry | No package-manager install is authorized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-04 | Spoofing | tuple-key construction | medium | mitigate | Length-safe canonical serialization and complete-triple validation prevent delimiter collisions. |
| T-03-05 | Denial of Service | item scheduling | medium | mitigate | Clamp supported item count and overlap offsets under a hard total-duration bound. |
| T-03-06 | Information Disclosure | replay ledger | low | mitigate | Store only canonical opaque identities in memory; clear naturally on App unmount. |
| T-03-SC | Tampering | npm installs | high | mitigate | Install nothing and verify package manifests and lockfiles are unchanged. |
</threat_model>

<verification>
Run the Plan 03-02 commands in `03-VALIDATION.md`; additionally search production code for persistence APIs and prove no timeline constant or derived schedule exceeds 1400 ms.
</verification>

<success_criteria>
- The explanatory sequence is one bounded, cancellable timeline.
- Normal UI and explanatory timings are explicitly separated.
- Same-triple replay is impossible after any first attempt.
- No persistence, package, external service, or user setup is introduced.
- The exact candidate SHA has a fresh-context independent PASS.
</success_criteria>

<failure_and_rollback>
If any timing bound, collision, or interruption test fails, do not integrate the primitives. Revert the isolated candidate commit; because the modules are not yet wired and own no persistent state, rollback is complete.
</failure_and_rollback>

<output>
Create `.planning/phases/03-living-home-signature-transition/03-02-SUMMARY.md` with exact `phase3_candidate_sha: <40hex>`, review evidence path, commands, outcomes, and rollback note.
</output>
