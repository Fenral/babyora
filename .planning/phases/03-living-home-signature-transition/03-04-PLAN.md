---
phase: 03-living-home-signature-transition
plan: "04"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/LivingHomeAtmosphere.tsx
  - src/components/LivingHomeAtmosphere.css
  - src/components/__tests__/LivingHomeAtmosphere.test.tsx
  - .planning/phases/03-living-home-signature-transition/evidence/03-04-INDEPENDENT-REVIEW.md
autonomous: true
requirements:
  - HOME-01
must_haves:
  truths:
    - "A reusable Living Home composition derives decoration synchronously from one caller-provided current snapshot, per D-01."
    - "Recommendation content and navigation remain outside the decorative component and can never wait on it."
    - "The composition uses only new Phase-3 files, existing tokens, CSS/DOM, and the 03-01 primitives, per D-02 and D-09."
    - "Neutral input, reduced motion, and forced colors retain a calm static decorative result."
  artifacts:
    - path: src/components/LivingHomeAtmosphere.tsx
      provides: "Same-snapshot atmosphere composition ready for serialized Home wiring"
    - path: src/components/LivingHomeAtmosphere.css
      provides: "New-file-only responsive and forced-colors treatment using existing tokens"
    - path: src/components/__tests__/LivingHomeAtmosphere.test.tsx
      provides: "SSR/DOM contract tests without shared-screen integration"
  key_links:
    - from: src/components/LivingHomeAtmosphere.tsx
      to: src/lib/home-atmosphere.ts
      via: "One synchronous resolver call from caller-provided values"
    - from: src/components/LivingHomeAtmosphere.tsx
      to: src/components/LivingHomeBackground.tsx
      via: "Resolved decorative model"
---

<objective>
Complete the Living Home new-file foundation without touching `HjemScreen.tsx`, `App.tsx`, shared tokens, or Phase 1/2 files, implementing D-01, D-02, D-03, and D-09.

Purpose: Give the later sole integration executor one tested composition instead of asking it to design atmosphere while editing shared navigation code.
Output: A same-snapshot Living Home composition, its new stylesheet and focused tests, plus exact-SHA autonomous review evidence.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/BABYORA-UX-MOTION-BIBLE.md
@docs/INSTRUMENT-DESIGN-SYSTEM.md
@docs/COLOR-SYSTEM.md
@.planning/phases/03-living-home-signature-transition/03-CONTEXT.md
@.planning/phases/03-living-home-signature-transition/03-RESEARCH.md
@.planning/phases/03-living-home-signature-transition/03-VALIDATION.md
@.planning/phases/03-living-home-signature-transition/03-01-SUMMARY.md
@src/lib/home-atmosphere.ts
@src/components/LivingHomeBackground.tsx
@src/styles/design-tokens.css
</context>

<interfaces>
`LivingHomeAtmosphere` accepts only the weather fields already available in the current Home recommendation snapshot: actual temperature, perceived temperature, normalized/provider condition, and explicit daylight evidence. It owns no recommendation text, CTA, navigation, fetching, storage, time inference, or shared-screen state.

This plan is an independent foundation candidate. Every production/test path it creates is new. The sole `HjemScreen.tsx` wiring occurs in 03-06 after the exact Phase-1 and Phase-2 gates.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Compose the same-snapshot Living Home decoration</name>
  <files>src/components/LivingHomeAtmosphere.tsx, src/components/LivingHomeAtmosphere.css, src/components/__tests__/LivingHomeAtmosphere.test.tsx</files>
  <behavior>
    - "One caller input resolves once and renders the 03-01 decorative background."
    - "Cold/mild/warm, explicit day/night/polar twilight, representative conditions, and neutral input produce deterministic data attributes."
    - "The subtree is aria-hidden, non-focusable, pointer-transparent, and contains no recommendation or control semantics."
    - "Reduced motion renders the settled static state."
    - "Forced-colors mode removes decorative ambiguity while preserving no authored semantic dependency."
  </behavior>
  <action>
Write the SSR/component contract tests first. Create a thin `LivingHomeAtmosphere` composition that calls `resolveHomeAtmosphere` once and passes its model to `LivingHomeBackground`. Keep all props readonly and structural so the later Home integration can pass the exact current snapshot fields without an adapter-side fetch or clock read. Add a new component-local stylesheet that consumes existing design tokens, includes a forced-colors branch, remains pointer-transparent, and never owns content visibility. Do not edit `HjemScreen.tsx`, `App.tsx`, `design-tokens.css`, `motion-grammar.ts`, or any Phase-1/2 file in this plan.
  </action>
  <verify>
    <automated>cmd.exe /d /s /c "npm test -- src/lib/__tests__/home-atmosphere.test.ts src/components/__tests__/LivingHomeBackground.test.tsx src/components/__tests__/LivingHomeAtmosphere.test.tsx && npx tsc -b --pretty false"</automated>
  </verify>
  <done>The new-file composition is deterministic, static-equivalent, forced-colors safe, and ready for post-gate Home wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze and independently review the new-file foundation</name>
  <files>.planning/phases/03-living-home-signature-transition/evidence/03-04-INDEPENDENT-REVIEW.md</files>
  <action>
Run the focused tests, typecheck, lint, `git diff --check`, scope scan, and dependency-file scan in the isolated worktree. Commit only the three new implementation/test paths and capture a full immutable SHA. Use the exact labeled review record required by `03-VALIDATION.md`: `phase3_candidate_sha`, `code_security_sha`, `code_security_status`, `ui_accessibility_sha`, `ui_accessibility_status`, `ancestry_status`, and `clean_status`. Both reviewer SHAs must equal the candidate; all statuses must be PASS. Review evidence is text/DOM/ARIA output only. Any implementation edit invalidates both verdicts and requires a new candidate.
  </action>
  <verify>
    <automated>cmd.exe /d /s /c "npm test -- src/lib/__tests__/home-atmosphere.test.ts src/components/__tests__/LivingHomeBackground.test.tsx src/components/__tests__/LivingHomeAtmosphere.test.tsx && npx tsc -b --pretty false && npm run lint && git diff --check"</automated>
  </verify>
  <done>The exact new-file foundation candidate has matching autonomous code/security and UI/accessibility PASS verdicts and no shared-file edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Caller snapshot to decoration | Fields can be missing or unfamiliar but cannot control semantics/navigation. |
| Decoration to Home content | The future caller must remain sole owner of recommendation and CTA. |
| Foundation branch to shared files | Early work must not edit Phase-1/2/shared integration files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-10 | Tampering | atmosphere input | medium | mitigate | Pure resolver, closed normalization, neutral fallback, readonly props. |
| T-03-11 | Elevation of Privilege | decorative DOM | high | mitigate | Entire subtree aria-hidden, non-focusable, pointer-transparent, no control callbacks. |
| T-03-12 | Tampering | shared-file scope | high | mitigate | Exact declared new paths plus scope scan before candidate promotion. |
| T-03-SC | Tampering | dependency files | high | mitigate | No install task; manifests and lockfiles must be unchanged. |
</threat_model>

<verification>
Run the 03-04 commands from `03-VALIDATION.md`; confirm the candidate diff contains only the declared new files and the review record contains exact labeled SHA equality/status fields.
</verification>

<success_criteria>
- HOME-01 has a reusable same-snapshot composition without shared-screen wiring.
- No Phase-1/2/shared file is edited.
- Reduced motion, neutral input, semantic hiding, and forced colors are tested.
- Both autonomous reviewer roles PASS the exact immutable candidate.
</success_criteria>

<failure_and_rollback>
If the composition or scope gate fails, do not promote the candidate. Revert its isolated commit; no route, screen, shared token, storage, dependency, or external state was changed.
</failure_and_rollback>

<output>
Create `.planning/phases/03-living-home-signature-transition/03-04-SUMMARY.md` with exact `phase3_candidate_sha: <40hex>`, labeled gate fields, commands, findings, and rollback note.
</output>
