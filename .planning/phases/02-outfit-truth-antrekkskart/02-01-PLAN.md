---
phase: 02-outfit-truth-antrekkskart
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/outfit/inventory-v1.ts
  - scripts/outfit/__tests__/inventory-v1.test.ts
  - src/lib/outfit/body-anchor-catalog.ts
  - src/lib/outfit/__tests__/body-anchor-coverage.test.ts
  - src/lib/outfit/avatar-visibility-catalog.ts
  - src/lib/outfit/outfit-truth.ts
  - src/lib/outfit/outfit-avatar-truth.ts
  - src/lib/outfit/__tests__/outfit-avatar-truth.test.ts
  - src/lib/outfit/__tests__/outfit-truth.test.ts
  - src/lib/outfit/outfit-transition-contract.ts
  - src/lib/outfit/__tests__/outfit-transition-contract.test.ts
autonomous: true
risk_lane: high
minimum_implementation_capability: high-implementation
minimum_reviewer_capability: high-verification
requirements:
  - OUTFIT-01
  - OUTFIT-02
must_haves:
  truths:
    - "D-01/D-02: semantic equipment is removed through engine-`utstyr` OR canonical catalog-`utstyr`; every supported result then has 1-10 ordered garment occurrences and no equipment body connection"
    - "Measured 11-garment output is preserved as complete list-only unsupported-cardinality truth; it is never truncated or normalized"
    - "D-03/D-08: immutable outfit truth contains stable occurrence identity, ordering, semantic anchors, snapshot identity, recommendation fingerprint, and transition identity, but no responsive node geometry"
    - "The canonical builder consumes the full finalized Recommendation with original layer categories and finalizer fields; a flattened ordered-garment/equipment projection is never sufficient input"
    - "D-05: canonical per-item body/visible-slot coverage, visual layer rank, outer precedence, and explicit occlusion derive one complete visible set; any ambiguity or non-exact manifest relation is neutral"
  artifacts:
    - path: src/lib/outfit/outfit-truth.ts
      provides: canonical recursively frozen OutfitTruthSnapshotV1 contract and factory
    - path: src/lib/outfit/body-anchor-catalog.ts
      provides: exhaustive semantic equipment/body classification independent of layout
    - path: scripts/outfit/inventory-v1.ts
      provides: candidate-versioned exhaustive finite-domain inventory invoked from the reviewed worktree
    - path: src/lib/outfit/avatar-visibility-catalog.ts
      provides: canonical body/visible-slot coverage, layer rank, and explicit occlusion per catalog item
    - path: src/lib/outfit/outfit-avatar-truth.ts
      provides: exact public manifest matcher over pose plus complete visible outer/accessory set
    - path: src/lib/outfit/outfit-transition-contract.ts
      provides: Phase-2 Outfit-row target registration and exact-identity readiness contract consumed by Phase 3
  key_links:
    - from: src/lib/wool-layers/types.ts
      to: src/lib/outfit/outfit-truth.ts
      via: finalized Recommendation category and order are converted once into canonical occurrences
    - from: public/avatars/verified/index.json
      to: src/lib/outfit/outfit-avatar-truth.ts
      via: derived complete visible occurrence set must exactly equal one manifest row at the exact pose
    - from: src/lib/outfit/outfit-truth.ts
      to: src/lib/outfit/outfit-transition-contract.ts
      via: snapshotId, recommendationFingerprint, transitionContextId, and occurrence itemId
---

<objective>
Freeze the canonical Phase 2 outfit-truth boundary before any visual integration, implementing D-01, D-02, D-03, D-05, and D-08.

Purpose: Give every later map, row, alternative, and Phase 3 transition one immutable source of truth instead of re-deriving identity from display strings.
Output: Exhaustive semantic anchor coverage, a versioned recursively frozen snapshot factory, and the DOM-registration/readiness interface Phase 3 can consume.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/CLAUDE-START-HERE.md
@docs/DECISION-LOG.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-outfit-truth-antrekkskart/02-RESEARCH.md
@.planning/phases/02-outfit-truth-antrekkskart/02-INTERFACE-CONTRACT.md
@.planning/phases/03-living-home-signature-transition/03-DEPENDENCY-CONTRACT.md
@src/lib/wool-layers/types.ts
@src/lib/wool-layers/recommend.ts
@src/data/garment-illustrations.ts
@src/data/garment-category.ts
@src/lib/recommendation/verified-avatar.ts
@public/avatars/verified/index.json
</context>

<worktree_contract>
Execute in an isolated worktree created exactly from foundation SHA `807bf66e11cdf255db99e1f19269545bedd6209c`. This plan owns only the eleven files in `files_modified`; remaining Phase 1 work stays outside this worktree. Create the candidate-versioned inventory script and its test inside this worktree before invoking it. The planning-only `02-INVENTORY.mts` is historical research evidence and must never be executed as implementation evidence. Record the starting SHA and produced commit SHA in `02-01-SUMMARY.md`. No package install, network service, screenshot, video, or paid action is authorized. Cost is NOK 0; if a required action could exceed NOK 1000, stop as BLOCKED without incurring it per D-12.
</worktree_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove exhaustive semantic anchor coverage</name>
  <files>scripts/outfit/inventory-v1.ts, scripts/outfit/__tests__/inventory-v1.test.ts, src/lib/outfit/body-anchor-catalog.ts, src/lib/outfit/__tests__/body-anchor-coverage.test.ts</files>
  <behavior>
    - The finite supported engine matrix across every valid age band, temperature band, activity, stroller, wind, rain, and weather-symbol input is enumerated through existing public engine types/tables, not a hand-picked sample.
    - Semantic equipment is the union of engine category `utstyr` and `garmentIdFor(label)` → `categoryFor(id) === 'utstyr'`; varmepose, saueskinn and every emitted sovepose regression are equipment with no anchor.
    - Every remaining source label maps to a body region and normalized semantic anchor through the canonical catalog.
    - Duplicate garment labels remain distinct occurrences, and any newly emitted unmapped label fails the coverage test instead of being guessed.
    - The reviewed candidate contains `inventory-v1.ts` plus a test that reproduces 2,036,160 branch-representative cases, 70/70 catalog coverage, 57/57 body coverage, max 11 garments, 6 equipment, and 12,960 results above ten.
    - Counts 1-10 are supported map truth; the representative 11-item result returns complete ordered list-only unsupported cardinality without map/avatar/alternative/motion eligibility.
  </behavior>
  <action>First create `scripts/outfit/inventory-v1.ts` and its test inside the isolated candidate worktree, using only the checked-in engine public types/tables and catalog as inputs. The script emits `inventoryVersion: 1`, deterministic metrics and the exact 11-item fixture; its test locks the finite dimensions and expected result. Only after both files exist may this plan invoke `npx tsx scripts/outfit/inventory-v1.ts --assert`. Create a typed semantic classifier that resolves `garmentIdFor(sourceLabel)` and `categoryFor(catalogId)`, then classifies equipment when either engine or catalog category is `utstyr`. Assert varmepose, saueskinn, sovepose, regnponcho and ansiktskrem are equipment even when legacy says `ekstra`. Map every remaining catalog identity to `BodyRegion` and normalized anchors. Do not import or execute the planning-only inventory script, assert a false max-ten engine invariant, or accept metrics from another dirty workspace.</action>
  <verify>
    <automated>cmd.exe /d /s /c "npx vitest run scripts/outfit/__tests__/inventory-v1.test.ts src/lib/outfit/__tests__/body-anchor-coverage.test.ts &amp;&amp; npx tsx scripts/outfit/inventory-v1.ts --assert"</automated>
  </verify>
  <done>The measured domain and semantic classifier are reproducible; catalog equipment is anchorless/excluded from count, 1-10 is the honest supported map range, and 11 remains complete list-only truth without normalization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build immutable occurrence-level outfit truth</name>
  <files>src/lib/outfit/avatar-visibility-catalog.ts, src/lib/outfit/outfit-truth.ts, src/lib/outfit/outfit-avatar-truth.ts, src/lib/outfit/__tests__/outfit-avatar-truth.test.ts, src/lib/outfit/__tests__/outfit-truth.test.ts</files>
  <behavior>
    - Exact identical finalized input produces the same snapshotId, recommendationFingerprint, transitionContextId, order, and occurrence itemIds.
    - Repeated labels receive different stable itemIds while preserving finalized engine order.
    - Mutation attempts at the root, arrays, garment/equipment records, avatar record, and nested anchors cannot change snapshot data.
    - Unsupported labels remain available as ordered text truth but make graphical-map eligibility false; they are never assigned a guessed anchor.
    - Every garment has canonical `bodyCoverage`, `visibleSlots`, `visualLayerRank`, and explicit `occludesSlots`, or a null/invalid coverage that forces neutral avatar truth.
    - Unique highest `visualLayerRank` per covered slot establishes outer precedence; ties, ambiguous/partial occlusion, or duplicate visible catalog IDs that the manifest cannot represent force neutral.
    - Avatar resolution matches every row of `public/avatars/verified/index.json` only when pose and the deterministically derived complete visible catalog-ID set are exactly equal; missing/extra/different items are neutral with empty visible ids.
    - A supported build contains exactly 1-10 semantic garments; the measured 11-item fixture produces `unsupported-cardinality` with all ordered labels/equipment and no snapshot claims.
    - A flat string projection, missing categorized layer data, or a Recommendation whose activity/provenance does not match the normalized RecommendInput is rejected before snapshot construction.
  </behavior>
  <action>Define branded IDs, semantic truth records, and `OutfitTruthBuildResultV1 = supported | unsupported-cardinality`. Make the builder require the normalized RecommendInput plus the complete finalized Recommendation and preserve its original layer category/order; reject flat arrays, missing required layer/note structure or malformed optional safety/finalizer fields rather than reconstructing a Recommendation. Add canonical visibility records and derive visibility slot by slot: unique highest rank wins; explicit occlusion only; ties/missing/null/contradictory coverage return neutral. Require exact manifest pose/set equality. Test all 24 rows, hidden inner layer, duplicate/rank/null/unknown/ambiguous/missing/extra/pose cases, optional safety-field presence/absence, flat-projection rejection and input/activity mismatch. Do not call the old level resolver, synthesize a path, partially match, activate Avatar State V2, or stage hidden layers.</action>
  <verify>
    <automated>npx vitest run src/lib/outfit/__tests__/outfit-avatar-truth.test.ts src/lib/outfit/__tests__/outfit-truth.test.ts src/lib/outfit/__tests__/body-anchor-coverage.test.ts</automated>
  </verify>
  <done>Canonical truth is deterministic, occurrence-safe, recursively immutable, strict about provenance, equipment-separated, layout-free, and conservative about avatar visibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Freeze Phase 3 registration and readiness semantics</name>
  <files>src/lib/outfit/outfit-transition-contract.ts, src/lib/outfit/__tests__/outfit-transition-contract.test.ts</files>
  <behavior>
    - `RegisterOutfitRow` accepts an existing occurrence itemId and Element-or-null lifecycle value; Phase 2 exports/calls no Home source registration from Outfit.
    - Phase-2 readiness validates snapshotId, recommendationFingerprint, transitionContextId and unique target rows; Phase 3 separately owns Home sources and the combined source/target predicate.
    - Missing, duplicate or stale target rows return a typed fail-closed reason; unsupported cardinality and reduced motion are static-only.
    - Eligibility reads identity/registration only and never imports layout coordinates into truth.
  </behavior>
  <action>Implement the Phase-2-owned dependency surface as `RegisterOutfitRow(itemId, element)` plus exact identity/target-row validation. Outfit components register only real ordered rows; Antrekkskart nodes never register as `home-node`. Phase 3 owns the distinct `RegisterHomeAnchor` type and callback, Hjem source lifecycle, finite source rectangles and the combined readiness predicate. Keep DOM values runtime-only and static Outfit complete on failure. Cite D-08 and align the adapter boundary with `.planning/phases/03-living-home-signature-transition/03-DEPENDENCY-CONTRACT.md`.</action>
  <verify>
    <automated>npx vitest run src/lib/outfit/__tests__/outfit-transition-contract.test.ts</automated>
  </verify>
  <done>Phase 3 has a stable, tested contract for exact identity, occurrence registration, reduced motion, and fail-closed motion eligibility without inheriting layout geometry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| finalized engine recommendation → truth factory | Internal data may contain duplicate or newly introduced labels |
| truth snapshot → UI/Phase 3 | Consumers must not mutate, guess, or mix identities |
| verified asset catalog → avatar claim | A visual asset can overstate what is actually verified |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Tampering | outfit-truth.ts | high | mitigate | clone, validate, recursively freeze, brand, and provenance-check every accepted snapshot |
| T-02-02 | Spoofing | occurrence identity | high | mitigate | deterministic occurrence ids include order/category/duplicate ordinal and exact triple matching |
| T-02-03 | Spoofing | avatar visibility/occlusion | critical | mitigate | canonical coverage/slots/rank/explicit occlusion, exact same-pose manifest set, neutral on every ambiguity |
| T-02-04 | Denial of Service | anchor/readiness evaluation | low | mitigate | bounded 1-10 arrays, pure finite validation, typed early failure |
| T-02-SC | Tampering | npm supply chain | high | mitigate | install no packages; use the existing lockfile and reject package-manifest/lockfile drift |
</threat_model>

<failure_and_rollback>
The measured 11-item case is expected unsupported-cardinality, not a test failure: preserve all rows/equipment and close map/avatar/alternative/motion. A future unknown label or a count outside the measured contract also fails closed and reports its exact input. Do not modify Motor/legacy thresholds. Rollback reverts only new modules.
</failure_and_rollback>

<verification>
Run the candidate-local inventory assertion and four focused suites, then `npm run lint`, `npm run build`, and `git diff --check`. Prove the invoked inventory script/test blobs are tracked by and read from the candidate worktree; reject the planning-workspace path. Confirm `package.json`, the lockfile, Motor/legacy recommendation files, and media directories are unchanged. Bind an archive or clean worktree to the produced exact commit SHA and obtain two fresh-context independent reviews: one contract/security/inventory-provenance review and one outfit-safety/avatar-visibility/occlusion review. Both must return PASS with no unresolved P0/P1 before Wave 2; record reviewer identities, SHA, commands, and findings in the summary.
</verification>

<success_criteria>
- All supported 1-10 semantic garment occurrences are ordered and uniquely addressable; engine/catalog equipment is separate, and 11 is complete list-only.
- Semantic anchors cover every current supported engine output and unknowns fail closed.
- Snapshot identity and Phase 3 registration contracts are deterministic, strict, immutable, and layout-independent.
- Avatar truth derives visibility from canonical slot/rank/explicit-occlusion data, matches every manifest row exactly, and otherwise stays neutral.
- Exact-SHA two-key review passes at NOK 0 with no app media.
</success_criteria>

<output>
Create `.planning/phases/02-outfit-truth-antrekkskart/02-01-SUMMARY.md` with machine-readable `status: PASS`, `plan_id: 02-01`, 40-hex `candidate_sha`, start/dependency SHA, tracked inventory script/test blob IDs and exact metrics, all-manifest-row avatar/occlusion results, risk findings, two distinct reviewer identity/session/capability/fresh-context/PASS records, and rollback note.
</output>
