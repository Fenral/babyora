---
phase: 02-outfit-truth-antrekkskart
plan: 05
type: execute
wave: 4
depends_on:
  - 02-04
  - 01-18
files_modified:
  - src/lib/planning/planned-outfit-context.ts
  - src/lib/planning/__tests__/planned-outfit-context.test.ts
  - src/screens/HjemScreen.tsx
  - src/screens/UkeScreen.tsx
autonomous: true
risk_lane: high
minimum_implementation_capability: high-implementation
minimum_reviewer_capability: high-verification
requirements:
  - OUTFIT-01
  - OUTFIT-02
must_haves:
  truths:
    - "Integration starts only after a raw-frontmatter preflight accepts exactly one Phase-1 `candidate_sha` plus exact contract/pack hashes, rejects every candidate alias/duplicate/ambiguity, normalizes the accepted value internally to `phase1_candidate_sha`, proves commit existence, and later proves ancestry"
    - "D-08: every exact current/planned context carries one factory-owned `OutfitBundleProducerSeedV1` containing the complete normalized RecommendInput and complete finalized Recommendation, including categorized layers and finalizer data, with matching stable provenance"
    - "Untrusted, stale, mutable, duplicate, layout-bearing, or identity-mismatched context fails closed"
    - "Phase 1 context remains a separately identified migration input and is not accepted by the Phase 2 canonical route"
  artifacts:
    - path: src/lib/planning/planned-outfit-context.ts
      provides: strict exact-context boundary containing the immutable producer seed derived from already-validated context input
    - path: src/lib/planning/__tests__/planned-outfit-context.test.ts
      provides: identity, provenance, serialization, and rejection coverage
    - path: src/screens/HjemScreen.tsx
      provides: current context call preserves the exact existing engineInput and resolved full Recommendation
    - path: src/screens/UkeScreen.tsx
      provides: planned context calls preserve each exact phase engineInput and resolved full Recommendation
  key_links:
    - from: src/lib/outfit/outfit-truth.ts
      to: src/lib/planning/planned-outfit-context.ts
      via: exact validated immutable producer seed, never a screen reconstruction
    - from: src/lib/outfit/alternative-options.ts
      to: src/lib/planning/planned-outfit-context.ts
      via: options must belong to the base transition and source occurrences
    - from: src/screens/HjemScreen.tsx
      to: src/lib/planning/planned-outfit-context.ts
      via: pass existing engineInput/resolvedRecommendation source objects; factory constructs the seed
    - from: src/screens/UkeScreen.tsx
      to: src/lib/planning/planned-outfit-context.ts
      via: pass existing phase.engineInput/phase.recommendation source objects; factory constructs the seed
---

<objective>
Make the exact planned-outfit context carry the complete Phase-2 producer seed per D-08, while preserving a buildable atomic migration boundary.

Purpose: Preserve the full objects already present at current/planned producers so serialized App integration can call the pure producer without recomputation, category reconstruction or flattened-truth substitution.
Output: Strict context creator/guard/serializer tests plus serialized Hjem/Uke call-site preservation of the complete normalized input and finalized Recommendation.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-planlegg-dagslinjen/01-18-SUMMARY.md
@.planning/phases/02-outfit-truth-antrekkskart/02-04-SUMMARY.md
@.planning/phases/02-outfit-truth-antrekkskart/02-INTERFACE-CONTRACT.md
@src/lib/planning/planned-outfit-context.ts
@src/lib/planning/__tests__/planned-outfit-context.test.ts
@src/lib/wool-layers/types.ts
@src/screens/HjemScreen.tsx
@src/screens/UkeScreen.tsx
</context>

<worktree_contract>
This is the first cross-phase integration plan. Before any source edit, read the raw YAML frontmatter of `.planning/phases/01-planlegg-dagslinjen/01-18-SUMMARY.md` and fail closed unless it has exact `status: PASS`, exactly one scalar `candidate_sha` containing 40 hex, exactly one scalar `contract_sha256` and `pack_sha256` containing 64 hex each, and two distinct fresh-context reviewer identities with PASS on that same tuple. The upstream Phase-1 source field is only `candidate_sha`: `phase1_candidate_sha`, `final_candidate_sha`, `commit`, Markdown `**Commit:**`, duplicate `candidate_sha` keys, YAML aliases/anchors, alternate casing/spelling, or multiple values are ambiguity and must fail before YAML parsing can collapse duplicates. After validation only, normalize that value into the Phase-2 evidence key `phase1_candidate_sha`; never accept that internal name as an upstream alias. Require `git cat-file -e <candidate_sha>^{commit}`. Apply the separate Phase-2 `candidate_sha`/review checks to `02-04-SUMMARY.md`. Create a clean isolated worktree exactly at the accepted Phase-1 `candidate_sha`, then merge the exact 02-04 candidate commit without squashing or rewriting so 02-01 through 02-04 and Phase 1 remain ancestors. Run `git merge-base --is-ancestor <phase1_candidate_sha> HEAD` and one check for every Phase-2 dependency SHA; any nonzero result is BLOCKED. Only after those gates may this serialized plan edit the four declared paths, including the Hjem/Uke context-producing calls finalized by Phase-1 01-18. Those screen edits are limited to preserving and passing already-computed objects; no recommendation, UI, navigation, weather, swap or access behavior changes. Phase 3 may extend Hjem/App only after 02-09. Do not cherry-pick reviewed changes, copy a dirty tree, or perform Phase 1 implementation. Record `phase1_source_field: candidate_sha`, the normalized exact Phase-1 tuple, all dependency SHAs, merge SHA, serialized ownership and screen-call-site audit. Cost NOK 0; D-12 blocks actions over NOK 1000.
</worktree_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the strict outfit-truth context shape</name>
  <files>src/lib/planning/planned-outfit-context.ts, src/lib/planning/__tests__/planned-outfit-context.test.ts, src/screens/HjemScreen.tsx, src/screens/UkeScreen.tsx</files>
  <behavior>
    - Creation accepts the already-computed full `RecommendInput` and already-finalized full `Recommendation` as source objects, then constructs the seed internally; no caller can supply a branded seed, seed ID or provenance.
    - The seed retains every normalized optional/nested input field and every Recommendation field, including activity, tempBand, categorized layers, notes, structuredNotes, summary, safetyFlags and severity.
    - Context identity, producer-seed sourceContextId/recommendationId/recommendationFingerprint/transitionContextId agree and remain stable through serialization; the full Recommendation's exact category-preserving garment/equipment flattening equals the existing display projection.
    - Every nested array/record is own data and recursively frozen; getters, prototype pollution, forged brands, and post-creation mutation are rejected.
    - Responsive layout boxes/connectors, DOM elements, and selection state are absent from serialized context.
    - A Phase 1 string-only context remains explicitly distinguishable during producer migration and cannot satisfy the outfit-truth guard.
  </behavior>
  <action>Write rejection/round-trip tests first. Extend the context factory input with source fields such as `recommendInput` and `finalizedRecommendation`, but never an injectable `outfitProducerSeed`. Validate both as plain own data, reject accessors/custom prototypes/cycles/non-finite values, prove `finalizedRecommendation.activity === recommendInput.activity`, and derive its category-preserving non-utstyr/utstyr flattening. Require that flattening to equal the existing recommendation orderedGarments/equipment projection and fingerprint basis. Deep-clone and recursively freeze the complete normalized input and full finalized Recommendation, including categorized layers and all note/safety/finalizer fields, then derive seed version, sourceContextId, recommendationId/fingerprint and transitionContextId from factory-owned context identity. In Hjem, pass the existing `engineInput` and `resolvedRecommendation`; in Uke, pass each existing fact/phase `engineInput` and resolved `recommendation`. Do not rerun `recommend`, reconstruct categories, change swaps, alter UI, or let source objects escape mutable. Keep the Phase-1 reader separately identifiable.</action>
  <verify>
    <automated>npx vitest run src/lib/planning/__tests__/planned-outfit-context.test.ts src/lib/outfit/__tests__/outfit-truth.test.ts src/lib/outfit/__tests__/alternative-options.test.ts</automated>
  </verify>
  <done>Both current and planned exact contexts round-trip one factory-owned deeply immutable full-input/full-Recommendation seed, reject flat/forged/mismatched data, and preserve existing route behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Prove integration-base and no-regression boundaries</name>
  <files>src/lib/planning/__tests__/planned-outfit-context.test.ts</files>
  <action>Per D-09, assert every valid factory-created context has exactly one derived seed, callers cannot inject/omit/flatten its content, post-creation mutations of input/layers/notes/flags cannot alter it, and existing Phase-1 current/planned/timezone/source/serialization behavior still passes. Add source-contract assertions that Hjem/Uke pass their existing full objects and never call `recommend` a second time for the handoff. Prove source kind is not inferred from access capability; App supplies it later from explicit route state. Run the focused Phase-1 suite, build both screens, then repeat the raw-frontmatter/commit/reviewer/ancestry/clean-tree gate. Capture exact results, `phase1_source_field: candidate_sha`, normalized tuple and Hjem/Uke preservation signatures.</action>
  <verify>
    <automated>npx vitest run src/lib/planning/__tests__/planned-outfit-context.test.ts src/lib/planning/__tests__/planned-outfit-resolver.test.ts</automated>
  </verify>
  <done>The new contract is present without regressing the accepted Phase 1 metadata boundary, and the implementation is demonstrably based on the immutable final Phase 1 candidate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| serialized/navigation input → planned context | Plain objects may be forged, stale, accessor-backed, or identity-mixed |
| Phase 1 branch → Phase 2 integration | Concurrent work can be accidentally merged from an unreviewed tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-16 | Spoofing | planned context identity | critical | mitigate | exact triple match, strict branded guards, deterministic context identity |
| T-02-17 | Tampering | serialized snapshot/options | high | mitigate | plain own-data validation, recursive clone/freeze, option ownership checks |
| T-02-18 | Elevation of Privilege | integration base | high | mitigate | exact PASS tuple, commit-existence check, isolated worktree, non-squashed merge and ancestry proof |
| T-02-19 | Information Disclosure | runtime fields | medium | mitigate | forbid DOM nodes, layout output, store state, and prototype/accessor data in serialization |
| T-02-19A | Tampering | Hjem/Uke producer handoff | critical | mitigate | serialize after Phase-1 01-18; preserve existing full objects; exact projection/category/finalizer checks; no rerun |
| T-02-SC | Tampering | npm supply chain | high | mitigate | no installs; verify package/lockfile unchanged |
</threat_model>

<failure_and_rollback>
If the final Phase 1 Git/contract/pack tuple, either independent PASS, or a Phase 2 dependency PASS SHA is unavailable or malformed, stop BLOCKED without editing integration files. A missing commit, dirty source, summary ambiguity, tuple mismatch, or failed ancestry check is equally blocking. Any flattened, incomplete, mutable or mismatched full input/Recommendation pair is rejected; no string-only fallback is Phase-2 truth. If either screen cannot preserve its already-computed full objects without rerunning recommendation logic, stop BLOCKED. Rollback reverts the four-path integration commit; no storage/schema migration exists.
</failure_and_rollback>

<verification>
From the current candidate worktree, run the tracked `npx tsx scripts/outfit/inventory-v1.ts --assert`, focused suites, `npm test`, `npm run lint`, `npm run build`, and `git diff --check`; never execute/import planning inventory. Require clean status. Verify Phase 1 and 02-01..02-04 ancestry, accepted tuple, four-path scope, exact Hjem/Uke source-object preservation, no second `recommend` call, full layer/note/safety-field retention, recursive freeze, stable IDs, no package/media/UI/navigation/weather changes, and no layout/runtime data in serialization. Bind the exact SHA and obtain two distinct high-verification fresh reviews: full engine/finalizer/provenance safety and serialized screen-ownership/tuple correctness.
</verification>

<success_criteria>
- Exact context transports one canonical seed with the complete normalized RecommendInput and complete finalized Recommendation, matching stable identity and no responsive/runtime state.
- Hjem/Uke preserve their already-computed full objects only after Phase-1 01-18; no rerun or category reconstruction occurs.
- Invalid or legacy-only data cannot satisfy the Phase 2 guard.
- Phase 1 exact-context behavior remains green on its final reviewed base.
- Exact-SHA two-key review and ancestry evidence pass.
</success_criteria>

<output>
Create `.planning/phases/02-outfit-truth-antrekkskart/02-05-SUMMARY.md` with machine-readable `status: PASS`, `plan_id: 02-05`, 40-hex Phase-2 `candidate_sha`, `phase1_source_field: candidate_sha`, normalized accepted Phase-1 tuple, dependency ancestry, tracked inventory blobs, full seed type/signature and retained-field matrix, Hjem/Uke source-object/no-rerun audit, exact tests, two distinct qualified PASS reviews, and rollback note.
</output>
