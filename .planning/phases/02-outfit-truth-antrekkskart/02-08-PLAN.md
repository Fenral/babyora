---
phase: 02-outfit-truth-antrekkskart
plan: 08
type: execute
wave: 7
depends_on:
  - 02-04
  - 02-05
  - 02-06
  - 02-07
files_modified:
  - src/lib/outfit/feature-flags.ts
  - src/components/outfit/OutfitTruthPanel.tsx
  - src/components/outfit/VerifiedAvatarComposite.tsx
  - src/components/outfit/__tests__/OutfitTruthPanel.test.tsx
autonomous: true
risk_lane: high
minimum_implementation_capability: high-implementation
minimum_reviewer_capability: high-verification
requirements:
  - OUTFIT-01
  - OUTFIT-02
must_haves:
  truths:
    - "The Phase-2 integration flag is false with no runtime bypass and remains false after this plan and after the final Phase-2 gate"
    - "A reusable panel accepts only the pure producer result: supported truth renders the full experience; unsupported cardinality renders every garment/equipment item as list-only truth; invalid input is neutral"
    - "D-05: avatar rendering uses only the snapshot's manifest-exact pose + complete visible outer/accessory set result, otherwise neutral"
    - "The panel accepts `RegisterOutfitRow`, semantic-preserving transition visual state, and guide callback; serialized 02-09 alone wires it into Phase-2-owned Paakledning and the minimal App bundle bootstrap, while Phase 3 later owns Hjem and post-handoff App transition extensions"
  artifacts:
    - path: src/components/outfit/OutfitTruthPanel.tsx
      provides: reusable supported/list-only/unavailable outfit presentation
    - path: src/components/outfit/VerifiedAvatarComposite.tsx
      provides: exact verified asset rendering with neutral fallback
    - path: src/lib/outfit/feature-flags.ts
      provides: false-only Phase-2 integration readiness boundary for the later serialized owner
  key_links:
    - from: src/lib/outfit/outfit-bundle-producer.ts
      to: src/components/outfit/OutfitTruthPanel.tsx
      via: typed supported, unsupported-cardinality, or unavailable result
    - from: src/components/outfit/OutfitTruthPanel.tsx
      to: src/components/outfit/OutfitExperience.tsx
      via: supported snapshot/options session only
    - from: src/components/outfit/OutfitTruthPanel.tsx
      to: src/lib/outfit/outfit-transition-contract.ts
      via: injected `RegisterOutfitRow` reaches real ordered rows only
---

<objective>
Assemble the reusable Phase-2 outfit presentation without activating or owning any application route.

Purpose: Give the later serialized Phase-3 integrator a complete, exact, fail-closed component that needs no truth reconstruction.
Output: False readiness flag, reusable panel, manifest-exact avatar consumer, and component contract tests.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/BABYORA-UX-MOTION-BIBLE.md
@docs/INSTRUMENT-DESIGN-SYSTEM.md
@.planning/phases/02-outfit-truth-antrekkskart/02-INTERFACE-CONTRACT.md
@.planning/phases/02-outfit-truth-antrekkskart/02-04-SUMMARY.md
@.planning/phases/02-outfit-truth-antrekkskart/02-07-SUMMARY.md
@.planning/phases/03-living-home-signature-transition/03-DEPENDENCY-CONTRACT.md
@src/lib/temp-axis.ts
@src/components/outfit/VerifiedAvatarComposite.tsx
</context>

<worktree_contract>
Continue from the exact PASS integration SHA of 02-07 and verify the exact 02-04 component candidate remains an ancestor. This plan owns only the four files above. It must not edit `src/App.tsx`, `src/screens/HjemScreen.tsx`, `src/screens/UkeScreen.tsx`, `src/screens/PaakledningScreen.tsx`, navigation, or any Home source registration. Phase-2 Plan 02-09 alone wires and activates the panel in Paakledning and installs the minimal current/planned App bundle bootstrap. Phase 3 later owns Hjem/Home sources/transition and may extend App only after the 02-09 exact-SHA gate. Leave the flag false. No package, engine, global-token, media, screenshot, or video change. Cost NOK 0; D-12 blocks actions over NOK 1000.
</worktree_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze the false integration and fail-closed component boundary</name>
  <files>src/lib/outfit/feature-flags.ts, src/components/outfit/OutfitTruthPanel.tsx, src/components/outfit/__tests__/OutfitTruthPanel.test.tsx</files>
  <behavior>
    - `OUTFIT_TRUTH_V1_AVAILABLE` is compile-time false and has no query, storage, environment-at-runtime, or prop override.
    - The panel accepts a validated `OutfitBundleProducerResult` plus optional `RegisterOutfitRow`, `transitionVisualState`, and `onOpenWarmColdGuide` inputs.
    - `unavailable` renders only the established neutral unavailable content and no garment advice.
    - `unsupported-cardinality` renders the exact complete inner-first ordered garments and separate equipment, with no map, connectors, avatar claim, alternatives, registration, or motion eligibility.
    - No Phase-2 source imports App, Hjem, Uke, Paakledning screen wrapper, or navigation.
  </behavior>
  <action>Write component/source tests before implementation. Create the false compile-time readiness constant with no bypass. Define `OutfitTruthPanel` over the exact public producer union: invalid/unavailable is neutral; the recorded 11-garment result uses the exact heading `Ta på innerst først` and preserves all 11 rows plus equipment in normal flow while declining every graphical/interactive truth claim. Accept `"settled" | "landing"` as a visual-only state defaulting to settled; both states mount identical semantic content, controls, focusability and row lifecycle, and landing may not use hidden/display-none/visibility-hidden/inert. Keep callbacks dependency-injected and unused when ineligible. Do not add a route, wrapper, App guard, Home producer, or Uke producer.</action>
  <verify>
    <automated>npx vitest run src/components/outfit/__tests__/OutfitTruthPanel.test.tsx src/lib/outfit/__tests__/outfit-bundle-producer.test.ts</automated>
  </verify>
  <done>The reusable boundary is false-by-default, accepts only the producer union, tells the full 11-item truth, and owns no application wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose the supported exact-truth experience</name>
  <files>src/components/outfit/OutfitTruthPanel.tsx, src/components/outfit/__tests__/OutfitTruthPanel.test.tsx</files>
  <behavior>
    - Supported current/planned input renders one exact map, `Ta på innerst først` ordered rows, separate equipment, finalized option actions, reset, and unchanged recovery copy.
    - The root uses `.ba-temp-root` and `data-temp="kald|mild|varm"` from existing `tempAxisFor(feelsLikeC, tempC)` values in the exact context; it adds no threshold.
    - Counts 1-4 use spacious content, 5-10 compact rails; map failure retains the complete normal-flow list/equipment and omits connectors.
    - Every row forwards only `RegisterOutfitRow`; the panel never registers a Home source or controls navigation/focus outside itself.
    - The guide action calls only the injected callback and does not synthesize guidance.
  </behavior>
  <action>Compose `OutfitExperience` from the supported context's immutable base/options. Derive the temperature presentation with the existing `tempAxisFor` contract, apply existing light/dark temperature tokens, render separate equipment and byte-identical recovery copy, and invoke the injected guide callback. Preserve the three-field selected/focus/hover behavior, persistent `aria-pressed`, normal-flow active caption, density modes, and row-only registration from 02-04. On layout failure keep all text truth; never recompute, stage hidden layers, truncate, use `+N`, or call static alternatives/finalization inside the component.</action>
  <verify>
    <automated>npx vitest run src/components/outfit/__tests__/OutfitTruthPanel.test.tsx src/components/outfit/__tests__/OutfitExperience.test.tsx src/lib/copy/__tests__/warm-cold-recovery.test.ts</automated>
  </verify>
  <done>The supported panel is complete and exact, while list-only/unavailable behavior and cross-phase ownership remain fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Consume only manifest-exact avatar truth</name>
  <files>src/components/outfit/VerifiedAvatarComposite.tsx, src/components/outfit/OutfitTruthPanel.tsx, src/components/outfit/__tests__/OutfitTruthPanel.test.tsx</files>
  <behavior>
    - A supported snapshot renders an asset only when `verifiedAssetPath` and all `visibleGarmentIds` came from canonical body/visible slots, unique outer-rank precedence and explicit occlusion followed by exact manifest pose/set equality.
    - Null/unknown/ambiguous coverage, rank tie, unresolved occlusion, duplicate unrepresentable visible ID, missing/extra item, pose mismatch, or unsupported cardinality renders the established neutral avatar with no claimed garment layers.
    - Selecting a finalized outcome consumes that outcome's independently resolved avatar truth.
    - Hidden inner/middle garments are never animated, composited, or revealed through outerwear.
  </behavior>
  <action>Make `VerifiedAvatarComposite` accept only trusted `OutfitAvatarTruth` from a supported snapshot and retain the neutral fallback. Do not reconstruct coverage, occlusion or a path from category, garment level, outerwear, headwear or filename; do not call the old level-based `verifiedAvatarAsset`; do not partially match the manifest; do not activate Avatar State V2. Consumer tests must cover all 24 trusted manifest results plus hidden-inner and every neutral diagnostic produced by the canonical resolver.</action>
  <verify>
    <automated>npx vitest run src/components/outfit/__tests__/OutfitTruthPanel.test.tsx src/lib/outfit/__tests__/outfit-truth.test.ts</automated>
  </verify>
  <done>The reusable component shows the exact verified manifest state or neutral and cannot visually claim unsupported layers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| producer result → reusable panel | Invalid or list-only truth must not enter map/avatar/alternatives |
| snapshot → avatar | Partial manifest matching can visually claim the wrong accessories |
| Phase-2 component → serialized application wiring | A reusable component must not silently seize the 02-09 App bootstrap or Phase-3 Hjem/transition ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-27 | Elevation of Privilege | integration readiness | critical | mitigate | compile-time false with no bypass; only Phase 2 Plan 02-09 may activate after full gates |
| T-02-28 | Spoofing | panel result branch | critical | mitigate | validated discriminated union; unsupported/unavailable never enter graphical path |
| T-02-29 | Spoofing | verified avatar | critical | mitigate | exact manifest pose + complete visible-set match or neutral |
| T-02-30 | Tampering | screen ownership | high | mitigate | strict four-file scope and source-import gate |
| T-02-SC | Tampering | npm supply chain | high | mitigate | no installs and unchanged package/lockfile |
</threat_model>

<failure_and_rollback>
Any invalid producer result is neutral; unsupported cardinality remains complete list-only truth; valid truth with map failure keeps all ordered rows/equipment. The flag remains false on every outcome. If this reusable-panel plan needs App/Hjem/Uke/Paakledning wrapper edits, stop; serialized Plan 02-09 owns App/Paakledning bootstrap, while Phase 3 later owns Hjem and post-handoff App transition extensions. Rollback reverts only the four listed files.
</failure_and_rollback>

<verification>
From the current candidate worktree, run the tracked `npx tsx scripts/outfit/inventory-v1.ts --assert`, focused component/avatar/producer tests, `npm test`, `npm run lint`, `npm run build`, source ownership scans, and `git diff --check` while the flag is false; never execute or import `.planning/.../02-INVENTORY.mts`. Prove the inventory script/test blobs exist in the candidate. Use DOM/source fixtures only; create no screenshots/video per D-11. Assert App/Hjem/Uke/Paakledning wrapper, package, engine and media paths are unchanged. Bind a clean exact-SHA archive and obtain two distinct fresh-context reviews with the minimum high-verification capability: presentation/accessibility/design-system/ownership and outfit-safety/avatar/fail-closed. The implementer may not self-PASS; both must PASS the same SHA with no unresolved P0/P1; record the inventory blob IDs.
</verification>

<success_criteria>
- Phase 2 supplies a complete reusable panel for serialized Paakledning plus App production bootstrap in 02-09; Hjem and post-handoff App transition extensions remain Phase 3-owned.
- Supported truth renders exact map/list/equipment/options/recovery; 11 garments remain complete list-only truth.
- Exact heading, density, normal-flow caption, temperature theme, three-state interaction, and row-only registration are preserved.
- Avatar is coverage/occlusion-derived and manifest-exact or neutral; this plan leaves the flag false for 02-09's gated activation.
</success_criteria>

<output>
Create `.planning/phases/02-outfit-truth-antrekkskart/02-08-SUMMARY.md` with machine-readable `status: PASS`, `plan_id: 02-08`, 40-hex `candidate_sha`, dependency ancestry, tracked inventory script/test blob IDs, `feature_flag: false`, supported/list-only/unavailable matrices, manifest-avatar cases, ownership audit, commands, and two distinct reviewer identity/capability/fresh-context/PASS records.
</output>
