---
phase: 02-outfit-truth-antrekkskart
plan: 06
type: execute
wave: 5
depends_on:
  - 02-05
files_modified:
  - src/lib/outfit/outfit-bundle-producer.ts
  - src/lib/outfit/__tests__/outfit-bundle-producer.test.ts
autonomous: true
risk_lane: high
minimum_implementation_capability: high-implementation
minimum_reviewer_capability: high-verification
requirements:
  - OUTFIT-01
  - OUTFIT-02
must_haves:
  truths:
    - "D-06/D-08: one pure producer accepts the factory-owned seed's complete normalized RecommendInput and complete finalized Recommendation plus an explicit App-route source discriminant, then emits one canonical result without calling `recommend`, reconstructing categories, or dropping finalizer data"
    - "The exact public `OutfitBundleProducerResult` distinguishes supported base/options/source/weather, complete unsupported-cardinality list truth, and unavailable failure"
    - "This pure-library plan edits no Hjem, Uke, App, or route wiring; 02-05 already serialized the Hjem/Uke full-object handoff, 02-09 later owns the minimal App current/planned production bootstrap, and Phase 3 owns post-02-09 Hjem/App transition extensions"
  artifacts:
    - path: src/lib/outfit/outfit-bundle-producer.ts
      provides: pure current/planned exact-result factory and typed supported/list-only/unavailable output
    - path: src/lib/outfit/__tests__/outfit-bundle-producer.test.ts
      provides: current/planned/interval identity, no-recompute, and unsupported-cardinality regression coverage
  key_links:
    - from: src/lib/outfit/outfit-bundle-producer.ts
      to: src/lib/outfit/outfit-truth.ts
      via: exact full seed input and category-preserving finalized Recommendation produce one semantic-equipment-aware build result
    - from: src/lib/outfit/outfit-bundle-producer.ts
      to: src/lib/planning/planned-outfit-context.ts
      via: only supported truth plus its finalized options becomes a strict canonical context
---

<objective>
Build the pure adapter that serialized Plan 02-09 can call at both real App current/planned recommendation boundaries, without assigning this library plan ownership of Hjem, Uke, App, or navigation.

Purpose: Make current/planned truth construction deterministic and reusable while eliminating cross-phase file conflicts and downstream recomputation.
Output: A side-effect-free producer and exhaustive contract tests for supported, list-only, and unavailable outcomes.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-outfit-truth-antrekkskart/02-INTERFACE-CONTRACT.md
@.planning/phases/02-outfit-truth-antrekkskart/02-05-SUMMARY.md
@.planning/phases/03-living-home-signature-transition/03-DEPENDENCY-CONTRACT.md
@src/lib/wool-layers/recommend.ts
@src/lib/wool-layers/finalize-safety.ts
@src/lib/planning/planned-outfit-context.ts
</context>

<worktree_contract>
Continue only from the exact PASS integration SHA in 02-05 after verifying its candidate, dependency ancestry, two-key review tuple, and Hjem/Uke full-object preservation audit. This plan owns only the two library/test paths above. It must not edit or import screen/App implementation details or claim route activation. Plan 02-09 is the next and sole serialized App bootstrap owner. Phase 3 may extend App and Hjem only after accepting 02-09; it preserves the seed contract, while Uke needs no further integration edit. No media, package, engine-threshold, or global-token changes. Cost NOK 0; D-12 blocks actions over NOK 1000.
</worktree_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify one pure exact-boundary producer</name>
  <files>src/lib/outfit/__tests__/outfit-bundle-producer.test.ts</files>
  <behavior>
    - Input requires exactly one factory-owned `OutfitBundleProducerSeedV1` whose recursively immutable payload retains the full normalized RecommendInput and full finalized Recommendation, plus explicit provenance distinguishing current versus planned and planned interval identity.
    - Identical seed/source yields byte-equivalent identity and output; different current/planned/interval provenance cannot reuse snapshot, transition, or option ownership.
    - A supported 1-10 semantic-garment result yields `bundleVersion: 1`, exact current/planned source metadata, finite temp/feels-like weather, one base snapshot and finalized options belonging only to that base.
    - The candidate-local inventory's exact 11-garment case yields the public `unsupported-cardinality` bundle with complete ordered garments/equipment and no snapshot, map, avatar claim, alternatives, or transition eligibility.
    - A flattened projection, missing required layer/note field, malformed optional safety/finalizer field, category/input mismatch, mutable/caller-forged seed, or invalid source yields typed `unavailable`; no prior or approximate result is substituted.
  </behavior>
  <action>Write the table-driven contract first using factory-created current/planned full seeds, duplicate-label, semantic-equipment, interval-change, optional RecommendInput fields, complete notes/structuredNotes/safetyFlags/severity, malformed-source, flattened/forged/mismatched seed, and the tracked 11-garment fixture. Assert the exact public union and reason codes. Prove purity: no store, DOM, navigation, screen, hook, `recommend`, raw swap, browser storage, clock or planning inventory. Assert the producer passes the preserved full Recommendation directly to the canonical truth and alternative builders; it cannot regenerate categorized layers from ordered strings or infer source from access capability.</action>
  <verify>
    <automated>npx vitest run src/lib/outfit/__tests__/outfit-bundle-producer.test.ts src/lib/outfit/__tests__/outfit-truth.test.ts src/lib/outfit/__tests__/alternative-options.test.ts src/lib/planning/__tests__/planned-outfit-context.test.ts</automated>
  </verify>
  <done>The failing suite fixes exact inputs, provenance, identity isolation, semantic equipment, and honest 11-garment list-only behavior without involving any screen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the typed producer and freeze the handoff</name>
  <files>src/lib/outfit/outfit-bundle-producer.ts, src/lib/outfit/__tests__/outfit-bundle-producer.test.ts</files>
  <behavior>
    - `produceOutfitBundle` returns `supported`, `unsupported-cardinality`, or `unavailable` as a recursively immutable discriminated result.
    - Supported output contains exact `bundleVersion`, `source`, `weather`, `base` and `options`; unsupported output contains the same version/source/weather plus complete typed truth; unavailable contains only version and a frozen reason-code union.
    - No branch invokes `recommend`, changes finalizer thresholds, applies a raw label-key swap, reads global state, infers source from access capability, or serializes DOM/layout/selection state.
    - The public types are sufficient for 02-09 to wire both App boundaries and for Phase 3 to add Hjem/Home transition orchestration afterward; Uke already carries the full seed from 02-05 and needs no later producer wiring.
  </behavior>
  <action>Implement the exact producer union frozen in `02-INTERFACE-CONTRACT.md`. Validate factory ownership, recursive immutability, complete normalized input, full finalized Recommendation and explicit source: sourceContextId must match, planned metadata exists only for planned, activity/projection/fingerprint/category provenance must agree, and source kind never comes from capability/access. Pass the full preserved input/Recommendation to the canonical truth builder and finalized alternative builder once; branch explicitly and propagate complete list-only truth. Never normalize 11, discard equipment/finalizer fields, call `recommend`, rebuild Recommendation from strings, manufacture screen identity or add undocumented fields. Add import-boundary tests.</action>
  <verify>
    <automated>cmd.exe /d /s /c "npx vitest run src/lib/outfit/__tests__/outfit-bundle-producer.test.ts src/lib/planning/__tests__/planned-outfit-context.test.ts &amp;&amp; npm run lint &amp;&amp; npm run build"</automated>
  </verify>
  <done>Plan 02-09 and later serialized integrators have one deterministic producer for exact current/planned boundaries, while this plan has not modified or claimed any screen or route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| exact input/finalized result → pure producer | A mismatched result can masquerade as the current recommendation |
| producer → future screen integration | A screen could recompute, reuse another interval, or mistake list-only truth for map support |
| serialized App ownership | Parallel 02-09/Phase-3 edits to App would invalidate isolated-worktree review |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-20 | Spoofing | exact input/result provenance | critical | mitigate | strict full-input/result/provenance validation and deterministic identity tests |
| T-02-21 | Tampering | unsupported cardinality | critical | mitigate | preserve all 11 garments as typed list-only output; no normalization or alternative/map/avatar construction |
| T-02-22 | Repudiation | current/planned/interval identity | high | mitigate | explicit immutable metadata and equivalence-class tests |
| T-02-23 | Elevation of Privilege | screen ownership | high | mitigate | library-only file scope here; 02-09 sole App bootstrap; Phase 3 Hjem/post-handoff App extension |
| T-02-SC | Tampering | npm supply chain | high | mitigate | no installs; unchanged manifests and lockfile |
</threat_model>

<failure_and_rollback>
Invalid or mismatched producer input yields `unavailable`; 11+ garments yield complete list-only truth. Neither result permits map, verified-avatar, alternatives, motion, or route claims. If the pure boundary cannot be implemented without editing a screen or recomputing recommendation truth, stop BLOCKED and leave 02-05 intact. Rollback reverts only the two new library/test files.
</failure_and_rollback>

<verification>
From the current candidate worktree, run the tracked `npx tsx scripts/outfit/inventory-v1.ts --assert`, focused suites, `npm test`, `npm run lint`, `npm run build`, source-import/scope scans, `git diff --check`, and a nonzero-on-dirty clean-tree assertion; never execute or import `.planning/.../02-INVENTORY.mts`. Prove the inventory script/test blobs exist in the candidate. Confirm no Hjem/Uke/App/Paakledning, engine, package, or media path changed. Bind one exact 40-hex candidate and obtain two distinct fresh-context reviews with the minimum high-verification capability: exact recommendation/provenance safety and cross-phase ownership/public-adapter sufficiency. The implementer may not self-PASS; both reviews must PASS the same SHA with no unresolved P0/P1; record the inventory blob IDs.
</verification>

<success_criteria>
- One pure function consumes the exact factory-owned full-input/full-Recommendation seed plus explicit route provenance and never calls `recommend` or reconstructs categories.
- Supported and list-only outputs remain explicitly different; the 11-garment case is complete and honest.
- Current/planned/interval identities cannot cross-contaminate.
- This plan changes no Hjem, Uke, App, or route wiring; 02-09's bootstrap and Phase 3's later extension are explicitly serialized.
</success_criteria>

<output>
Create `.planning/phases/02-outfit-truth-antrekkskart/02-06-SUMMARY.md` with machine-readable `status: PASS`, `plan_id: 02-06`, 40-hex `candidate_sha`, 02-05 dependency SHA/ancestry, tracked inventory script/test blob IDs, producer result matrix, screen-import/scope audit, commands, and two distinct reviewer identity/capability/fresh-context/PASS records.
</output>
