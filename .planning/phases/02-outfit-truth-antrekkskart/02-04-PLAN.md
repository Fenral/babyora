---
phase: 02-outfit-truth-antrekkskart
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified:
  - src/components/outfit/Antrekkskart.tsx
  - src/components/outfit/Antrekkskart.css
  - src/components/outfit/OutfitGarmentList.tsx
  - src/components/outfit/OutfitExperience.tsx
  - src/components/outfit/__tests__/OutfitExperience.test.tsx
autonomous: true
risk_lane: high
minimum_implementation_capability: high-implementation
minimum_reviewer_capability: high-verification
requirements:
  - OUTFIT-01
  - OUTFIT-02
must_haves:
  truths:
    - "D-01/D-02: the map renders every garment as a numbered node with its connector while equipment remains in a distinct unconnected list"
    - "D-04: selectedId, focusId, and hoverId remain independent; transient hover/focus and persistent aria-pressed selection cross-highlight both representations without stealing focus"
    - "D-06: comparison and selection operate only on finalized options and always show the resulting complete outfit with deterministic reset"
    - "D-10: the experience evolves existing tokens, typography, motion grammar, and native controls without a new visual direction"
    - "The executable presentation contract is exact: `Ta på innerst først`, 1-4 spacious, 5-10 compact rails, active caption in normal flow, and existing kald/mild/varm background tokens"
  artifacts:
    - path: src/components/outfit/Antrekkskart.tsx
      provides: accessible graphical garment nodes and connectors
    - path: src/components/outfit/OutfitGarmentList.tsx
      provides: ordered text truth and Phase 3 row registrations
    - path: src/components/outfit/OutfitExperience.tsx
      provides: shared interaction state, comparison, swap, reset, and text fallback
    - path: src/components/outfit/Antrekkskart.css
      provides: responsive, forced-colors, 200%, and reduced-motion styling
  key_links:
    - from: src/lib/outfit/outfit-map-layout.ts
      to: src/components/outfit/Antrekkskart.tsx
      via: layout model drives visual boxes/connectors without mutating truth
    - from: src/components/outfit/OutfitExperience.tsx
      to: src/state/outfit-selection-store.ts
      via: whole-snapshot select/reset actions
    - from: src/components/outfit/OutfitGarmentList.tsx
      to: src/lib/outfit/outfit-transition-contract.ts
      via: `RegisterOutfitRow(itemId, element-or-null)` registers only real ordered rows
---

<objective>
Build the reusable Antrekkskart experience that presents complete truth, paired interaction, and finalized alternatives per D-01, D-02, D-04, D-06, and D-10.

Purpose: Turn the frozen data contracts into an accessible, calm, responsive interface without yet changing canonical application routes.
Output: Map, ordered list, shared interaction/comparison orchestrator, styles, and component contract tests.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/INSTRUMENT-DESIGN-SYSTEM.md
@docs/COLOR-SYSTEM.md
@docs/motion-system.md
@src/styles/motion-grammar.ts
@.planning/phases/02-outfit-truth-antrekkskart/02-RESEARCH.md
@.planning/phases/02-outfit-truth-antrekkskart/02-INTERFACE-CONTRACT.md
@.planning/phases/02-outfit-truth-antrekkskart/02-02-SUMMARY.md
@.planning/phases/02-outfit-truth-antrekkskart/02-03-SUMMARY.md
</context>

<worktree_contract>
Create an isolated worktree from the exact 02-01 PASS SHA and merge the exact reviewed 02-02 and 02-03 candidate commits without squashing, rewriting, or copying files, so both reviewed commits remain ancestors of the assembled candidate. Fail before editing if either summary lacks a PASS verdict, a 40-hex candidate SHA, distinct required review evidence, commit existence, or 02-01 ancestry. Own only the five component/test files listed. Do not integrate App/screens, create media, add packages, or modify global design tokens. Cost is NOK 0 and D-12 blocks actions over NOK 1000.
</worktree_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render every garment node and truthful connector</name>
  <files>src/components/outfit/Antrekkskart.tsx, src/components/outfit/Antrekkskart.css, src/components/outfit/__tests__/OutfitExperience.test.tsx</files>
  <behavior>
    - For each eligible 1-10 snapshot, markup contains exactly one native button and ordinal label per garment plus one aria-hidden visual connector per garment.
    - The full accessible node name contains ordinal, garment label, category, and body region; connector graphics do not create duplicate screen-reader announcements.
    - Equipment never renders as a body node or connector.
    - Ineligible geometry renders no misleading map and leaves the full ordered text truth available.
    - Inactive connectors compute to at least 3:1 against their actual adjacent background across light/dark and kald/mild/varm; active connectors additionally differ by width and dash/pattern, and forced colors maps them to system colors.
  </behavior>
  <action>Write server-rendered and CSS-contract tests first, then implement `Antrekkskart` from the pure layout output. Use native buttons, stable itemId keys/test ids, an SVG connector layer marked decorative, and explicit map status. Render every node; do not slice, aggregate, overflow-hide, or replace entries with a count. Antrekkskart map nodes expose no Phase-3 registration callback: Phase 2 registers only ordered Outfit rows, while Phase 3 alone owns Home source nodes. Style with existing design tokens and logical properties; include 44px targets, visible focus, width containment, no information-bearing motion, contrast-safe connector variables for all six light/dark × temperature combinations, active stroke-width plus dash/pattern redundancy, and a forced-colors system-color override. Keep computed-style matrix validation for 02-09 in addition to these source/markup contracts.</action>
  <verify>
    <automated>npx vitest run src/components/outfit/__tests__/OutfitExperience.test.tsx src/lib/outfit/__tests__/outfit-map-layout.test.ts</automated>
  </verify>
  <done>Every eligible garment is a visible, numbered, accessible node with the correct connector; equipment is absent from the body map and invalid geometry shows text truth only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pair nodes and ordered rows with one accessible state</name>
  <files>src/components/outfit/OutfitGarmentList.tsx, src/components/outfit/OutfitExperience.tsx, src/components/outfit/Antrekkskart.css, src/components/outfit/__tests__/OutfitExperience.test.tsx</files>
  <behavior>
    - Pointer enter/leave updates only `hoverId`; focus/blur updates only `focusId`; neither changes persistent `selectedId`.
    - Click, Enter, and Space update `selectedId`; both paired controls expose `aria-pressed` from that persistent state, while `highlightedId = focusId ?? hoverId ?? selectedId`.
    - Pair highlighting never programmatically focuses the other representation; Escape clears only transient state and preserves committed selection.
    - The list heading is exactly `Ta på innerst først`; the active full caption remains in normal document flow and is programmatically associated with both paired controls.
    - Moving selection never changes node geometry or row order.
    - Every real ordered row calls `RegisterOutfitRow(itemId, element)` on mount and `RegisterOutfitRow(itemId, null)` on unmount; map nodes never register.
    - The root consumes an already derived `kald|mild|varm` value, applies `.ba-temp-root` and `data-temp`, and remains usable at 200% text and reduced motion.
  </behavior>
  <action>Implement `OutfitGarmentList` and an explicit three-field pair reducer in `OutfitExperience`: persistent `selectedId`, transient `focusId`, and transient `hoverId`. Use native button keyboard activation, `aria-pressed={selectedId === itemId}` on both paired controls, and highlight priority `focusId ?? hoverId ?? selectedId`. Pointer/focus events must never commit selection, activation must never call `.focus()` on the paired element, and Escape must not erase committed state. Render the exact heading `Ta på innerst først`; keep the selected/highlighted full caption in normal flow outside absolute/SVG geometry and associate it with both controls. Accept a `kald|mild|varm` presentation value already derived by the screen from existing `tempAxisFor`, then apply `.ba-temp-root`/`data-temp` without new thresholds. Forward only optional `RegisterOutfitRow` lifecycle registration for every ordered row. CSS must add text/ordinal, outline/shape, connector width/pattern, and color cues; stable geometry; `prefers-reduced-motion`; forced colors; and wrapping that preserves all content at 200%.</action>
  <verify>
    <automated>npx vitest run src/components/outfit/__tests__/OutfitExperience.test.tsx</automated>
  </verify>
  <done>Nodes and rows share deterministic transient highlighting and persistent pressed selection without focus stealing, while exact heading, normal-flow caption, temperature theme, row-only registration, and non-color cues remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Compare, select, and reset only complete finalized outcomes</name>
  <files>src/components/outfit/OutfitExperience.tsx, src/components/outfit/__tests__/OutfitExperience.test.tsx</files>
  <behavior>
    - `Se alternativ` appears only for a garment with at least one finalized option.
    - The modal/non-modal native dialog names source and target, advantages/tradeoffs, and the full resulting ordered outfit before confirmation.
    - Confirm atomically switches map, rows, equipment, avatar input, and identity to the option outcome; Reset restores the exact base.
    - Empty, stale, or rejected options show no selectable substitute and cannot mutate state.
  </behavior>
  <action>Wire the exact selection store into `OutfitExperience`. Present comparison using a native dialog or established accessible overlay primitives, keep focus/escape/close behavior deterministic, and show the entire post-finalization outfit before selection. On selection render the option's full snapshot rather than editing a row in place; on reset use the stored immutable base. Reserve a normal-flow recovery-copy slot for 02-08. Never call the static alternative catalog or finalizer from the component.</action>
  <verify>
    <automated>npx vitest run src/components/outfit/__tests__/OutfitExperience.test.tsx src/state/__tests__/outfit-selection-store.test.ts</automated>
  </verify>
  <done>The UI exposes only pre-finalized comparisons, swaps the complete truth atomically, resets exactly, and offers no fake action for static-only alternatives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| canonical snapshot/layout → DOM | Rendering can omit, reorder, or mislabel safety-relevant garments |
| pointer/keyboard/AT → shared selection | Modalities must not diverge or trap focus |
| finalized options → confirmation | UI must not invent or partially apply an alternative |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-12 | Spoofing | Antrekkskart connector/node labels | high | mitigate | exact itemId mapping, one node/connector per garment, semantic contract tests |
| T-02-13 | Denial of Service | keyboard/dialog flow | medium | mitigate | native controls, deterministic focus/escape, 44px targets, reduced-motion path |
| T-02-14 | Tampering | alternative confirmation | high | mitigate | UI accepts only validated complete outcome snapshots from the store |
| T-02-15 | Information Disclosure | accessible labels | low | accept | garment/category/body-region truth is intended user-visible content; connectors remain decorative |
| T-02-SC | Tampering | npm supply chain | high | mitigate | no new dependency; reject package/lockfile drift |
</threat_model>

<failure_and_rollback>
If geometry is ineligible, render the complete ordered rows/equipment and no connectors. If an option is invalid, omit its action and preserve the base. Component or accessibility review failure blocks route integration. Rollback reverts only these new files; no state persists across close/reload.
</failure_and_rollback>

<verification>
From the current candidate worktree, run the tracked `npx tsx scripts/outfit/inventory-v1.ts --assert`, focused component/domain tests, `npm run lint`, `npm run build`, and `git diff --check`; never execute or import `.planning/.../02-INVENTORY.mts`. Prove the inventory script/test blobs exist in the candidate. Use only DOM/markup/layout fixtures—no screenshots or video per D-11. Confirm global tokens, package files, screens, and media are unchanged. Review a clean exact-SHA archive in two distinct fresh contexts with the minimum high-verification capability: one owns semantics/three-field keyboard-pointer state/row-only registration, and one owns responsive CSS/connector contrast/forced colors/design-system/fake-alternative risk. The high-risk implementer may not self-PASS. Both must PASS the same candidate with no unresolved P0/P1; record candidate SHA, inventory blob IDs, dependency ancestor SHAs, reviewer/session/capability/fresh-context markers and evidence in the summary.
</verification>

<success_criteria>
- Complete 1-10 map/list rendering and equipment separation are encoded in markup contracts.
- Cross-highlighting uses distinct selected/focus/hover state with persistent `aria-pressed`, no focus stealing, and non-color, keyboard, screen-reader, 200%, forced-colors, and reduced-motion affordances.
- Exact heading, density modes, normal-flow active caption, perceived-temperature theme input, and row-only registration are encoded in executable tests.
- Alternatives are full finalized outcomes with working comparison, swap, and reset.
- Exact-SHA review passes without app-route or media changes.
</success_criteria>

<output>
Create `.planning/phases/02-outfit-truth-antrekkskart/02-04-SUMMARY.md` with machine-readable `status: PASS`, `plan_id: 02-04`, 40-hex `candidate_sha`, assembled 02-01..03 dependency SHAs/ancestry, tracked inventory script/test blob IDs, component matrices, commands, two distinct reviewer identity/session/capability/fresh-context/PASS records, findings, and rollback note.
</output>
