---
phase: 02-outfit-truth-antrekkskart
plan: 07
type: execute
wave: 6
depends_on:
  - 02-06
files_modified:
  - src/lib/copy/warm-cold-recovery.ts
  - src/lib/copy/__tests__/warm-cold-recovery.test.ts
  - src/screens/VarmEllerKaldScreen.tsx
  - src/components/PlaggDetailSheet.tsx
autonomous: true
risk_lane: high
minimum_implementation_capability: high-implementation
minimum_reviewer_capability: high-verification
requirements:
  - OUTFIT-02
must_haves:
  truths:
    - "D-07: Phase 2 can reuse the existing cautious warm/cold guidance from one shared source without changing wording, thresholds, Motor, or Avatar State V2"
    - "D-06: generic garment surfaces cannot write a raw swap or present static catalog candidates as a working outfit replacement; legacy consumer/store deletion waits for the serialized screen integrator"
    - "No new health claim or human copy-review port is introduced"
  artifacts:
    - path: src/lib/copy/warm-cold-recovery.ts
      provides: unchanged shared neck-check/status/action copy
    - path: src/screens/VarmEllerKaldScreen.tsx
      provides: existing guide rendered from shared copy with byte-equivalent output
    - path: src/components/PlaggDetailSheet.tsx
      provides: catalog information without a false selectable swap
  key_links:
    - from: src/lib/copy/warm-cold-recovery.ts
      to: src/screens/VarmEllerKaldScreen.tsx
      via: shared cautious copy constants
    - from: src/lib/wool-layers/alternatives.ts
      to: src/components/PlaggDetailSheet.tsx
      via: static entries remain informational and have no selection side effect
---

<objective>
Centralize the already-approved warm/cold recovery wording and remove the last deceptive raw-swap entry point per D-06 and D-07.

Purpose: Let the canonical outfit route point to proven cautious guidance while ensuring generic catalog UI cannot pretend a static candidate was safely applied.
Output: Shared unchanged copy and informational garment detail behavior, without claiming ownership of legacy producer/store removal.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@docs/DECISION-LOG.md
@.planning/phases/02-outfit-truth-antrekkskart/02-06-SUMMARY.md
@src/screens/VarmEllerKaldScreen.tsx
@src/components/PlaggDetailSheet.tsx
@src/state/swap-override-store.ts
</context>

<worktree_contract>
Continue from the exact PASS SHA of 02-06. This plan alone owns the four listed paths. Do not edit Hjem, Uke, App, Paakledning, or the legacy store: its remaining consumers and final deletion belong to the later serialized integration owner. Do not alter recommendation logic, thresholds, weather logic, Avatar State V2, packages, or media. Cost NOK 0; actions above NOK 1000 are BLOCKED per D-12.
</worktree_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract byte-equivalent cautious recovery copy</name>
  <files>src/lib/copy/warm-cold-recovery.ts, src/lib/copy/__tests__/warm-cold-recovery.test.ts, src/screens/VarmEllerKaldScreen.tsx</files>
  <behavior>
    - Rendered guide text remains exactly `Kjenn nakken`, the existing two-finger instruction, and the three existing status/signal/action tuples for warm, comfortable, and cold.
    - The existing accessible labels for adding/removing/keeping a layer remain unchanged.
    - The shared module contains copy only; it computes no temperature threshold, diagnosis, recommendation, or health state.
  </behavior>
  <action>Snapshot the current literal tuple in a failing characterization test before extraction. Move those exact Norwegian strings and accessible labels into a typed immutable copy module, then make `VarmEllerKaldScreen` render the module without changing visible or accessible output. Do not paraphrase, add a medical claim, introduce a reviewer checkpoint, or port threshold/engine logic. The later outfit route will import the same copy and link to this existing guide.</action>
  <verify>
    <automated>npx vitest run src/lib/copy/__tests__/warm-cold-recovery.test.ts</automated>
  </verify>
  <done>The established cautious wording and accessibility labels have one shared source and render byte-equivalently, with no new claim or recommendation logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove deceptive catalog swap behavior from generic detail</name>
  <files>src/components/PlaggDetailSheet.tsx, src/lib/copy/__tests__/warm-cold-recovery.test.ts</files>
  <behavior>
    - Generic garment detail may describe catalog alternatives and pros/cons but exposes no action that claims to replace the current outfit.
    - Opening/closing generic detail cannot mutate outfit selection.
    - The only production `Se alternativ` action is the canonical `OutfitExperience` path backed by finalized options.
    - Existing legacy producer/store imports are not expanded or declared canonical; their serialized removal is explicitly deferred.
  </behavior>
  <action>Add source/render contract assertions, then remove `useSwapOverride` and the kebab-key “Bytte til” side effect from `PlaggDetailSheet`. If catalog comparisons remain, label them explicitly as information and provide no success toast or outfit-mutation affordance. Do not edit or delete `src/state/swap-override-store.ts` while unchanged Hjem/Uke consumers still exist; record those imports as deferred work for the single serialized screen integrator. Preserve the detail sheet's other consumers and close/focus behavior.</action>
  <verify>
    <automated>npx vitest run src/lib/copy/__tests__/warm-cold-recovery.test.ts src/lib/outfit/__tests__/alternative-options.test.ts src/state/__tests__/outfit-selection-store.test.ts</automated>
  </verify>
  <done>Static catalog data is informational only in generic detail, while engine-backed OutfitExperience is the only new working alternative path and legacy store cleanup is honestly deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| shared guidance copy → user | Wording changes can become unintended health/safety claims |
| static catalog → generic detail | Informational candidates can be mistaken for finalized advice |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-24 | Tampering | warm-cold copy | high | mitigate | byte-equivalent characterization test and immutable shared constants |
| T-02-25 | Spoofing | PlaggDetailSheet alternative action | high | mitigate | remove mutation/success semantics; engine-backed path is the only working action |
| T-02-26 | Repudiation | legacy store boundary | medium | mitigate | no new writer; remaining legacy imports are recorded for the serialized integration owner |
| T-02-SC | Tampering | npm supply chain | high | mitigate | no installs; package files remain unchanged |
</threat_model>

<failure_and_rollback>
If characterization output differs, keep the prior guide implementation and block 02-08; do not approve a wording change implicitly. Do not delete the legacy store while unchanged screen consumers remain. Rollback restores these four paths from the pre-plan exact SHA; no persisted store data is migrated or retained.
</failure_and_rollback>

<verification>
From the current candidate worktree, run the tracked `npx tsx scripts/outfit/inventory-v1.ts --assert`, focused tests, `npm test`, `npm run lint`, `npm run build`, and `git diff --check`; never execute or import `.planning/.../02-INVENTORY.mts`. Prove the inventory script/test blobs exist in the candidate. Confirm generic detail has no raw-store writer, remaining legacy imports are unchanged and explicitly deferred, no new health text, and no engine/package/media edits. Bind a clean archive to the exact commit SHA and obtain two independent fresh reviews with the minimum high-verification capability: one copy/safety regression review and one deceptive-interaction/ownership-boundary review. Both must PASS without unresolved P0/P1; record the inventory blob IDs.
</verification>

<success_criteria>
- Existing warm/cold wording and accessible labels are unchanged and shared.
- Generic detail has no pretend swap action or selection side effect.
- Generic detail has no raw-swap writer; final legacy screen/store removal is owned by the later serialized integrator.
- Exact-SHA safety and interaction reviews pass at NOK 0 with no media.
</success_criteria>

<output>
Create `.planning/phases/02-outfit-truth-antrekkskart/02-07-SUMMARY.md` with machine-readable `status: PASS`, `plan_id: 02-07`, 40-hex `candidate_sha`, dependency ancestry, tracked inventory script/test blob IDs, before/after copy tuple, generic-detail writer audit, deferred legacy-import list, tests, and two distinct reviewer identity/capability/fresh-context/PASS records.
</output>
