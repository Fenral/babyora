---
phase: 02-outfit-truth-antrekkskart
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/lib/outfit/finalized-outfit-swap.ts
  - src/lib/outfit/alternative-options.ts
  - src/lib/outfit/__tests__/alternative-options.test.ts
  - src/state/outfit-selection-store.ts
  - src/state/__tests__/outfit-selection-store.test.ts
autonomous: true
risk_lane: high
minimum_implementation_capability: high-implementation
minimum_reviewer_capability: high-verification
requirements:
  - OUTFIT-02
must_haves:
  truths:
    - "D-06: Se alternativ exists only for a supported 1-10-garment snapshot and exposes occurrence-specific candidates whose complete outcome survived the existing safety finalizer"
    - "D-06/D-08: choosing an alternative replaces the entire exact snapshot identity; reset deterministically restores the immutable base snapshot"
    - "Equipment and unsupported-cardinality results never expose alternatives; stale, ambiguous, invalid, or unfinalizable candidates fail closed"
    - "Alternative finalization requires the complete normalized RecommendInput and complete finalized Recommendation with original categorized layers/notes/safety data; a flattened projection is rejected"
  artifacts:
    - path: src/lib/outfit/finalized-outfit-swap.ts
      provides: occurrence-aware clone, substitute, finalize, and survival verification
    - path: src/lib/outfit/alternative-options.ts
      provides: immutable engine-backed comparisons and complete outcome snapshots
    - path: src/state/outfit-selection-store.ts
      provides: ephemeral exact-snapshot selection state with deterministic reset
  key_links:
    - from: src/lib/wool-layers/alternatives.ts
      to: src/lib/outfit/alternative-options.ts
      via: catalog entries nominate candidates only
    - from: src/lib/outfit/finalized-outfit-swap.ts
      to: src/lib/wool-layers/finalize-safety.ts
      via: every substituted recommendation is finalized and checked before exposure
    - from: src/state/outfit-selection-store.ts
      to: src/lib/outfit/outfit-truth.ts
      via: selection stores whole validated snapshot outcomes keyed to the exact base triple
---

<objective>
Repair the alternative identity/store/finalizer boundary and make every selectable comparison an engine-backed, finalized full-outfit outcome per D-06 and D-08.

Purpose: Prevent static catalog suggestions, label-key collisions, and stale global overrides from changing safety-sensitive outfit advice.
Output: An occurrence-aware finalization adapter, immutable alternative-option builder, and exact-snapshot selection/reset store.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-outfit-truth-antrekkskart/02-RESEARCH.md
@.planning/phases/02-outfit-truth-antrekkskart/02-INTERFACE-CONTRACT.md
@.planning/phases/02-outfit-truth-antrekkskart/02-01-SUMMARY.md
@src/lib/wool-layers/alternatives.ts
@src/lib/wool-layers/finalize-safety.ts
@src/state/swap-override-store.ts
</context>

<worktree_contract>
Continue the isolated foundation worktree at the exact PASS SHA recorded by `02-01-SUMMARY.md`. This plan may run in parallel with 02-03 and owns only its five listed files. It must not touch Hjem, Uke, Påkledning, the legacy store, Motor thresholds, package manifests, or media. Cost remains NOK 0 with the D-12 hard stop above NOK 1000.
</worktree_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize occurrence-specific alternative outcomes</name>
  <files>src/lib/outfit/finalized-outfit-swap.ts, src/lib/outfit/alternative-options.ts, src/lib/outfit/__tests__/alternative-options.test.ts</files>
  <behavior>
    - A candidate targets exactly one source occurrence by itemId/order/category/source label, including duplicate-label outfits.
    - Candidate nomination alone is never selectable; the base and outcome must both be `supported`, the cloned recommendation must pass existing `finalizeSafety`, retain the target, satisfy the semantic-equipment and 1-10 invariants, and produce a valid complete outcome snapshot.
    - Semantic equipment occurrences and `unsupported-cardinality` build results yield no options and retain their complete ordered/equipment truth for list-only presentation.
    - Failed, removed, ambiguous, unknown, or identity-mismatched candidates are omitted with a typed diagnostic.
    - Options contain comparison text plus immutable full outcome snapshot; optionId and outcome identity are deterministic for identical input.
    - The base Recommendation's categorized layers, notes, structuredNotes, summary, safetyFlags and severity remain available to the existing finalizer; missing/flattened input cannot nominate an option.
  </behavior>
  <action>Write failing tests for kebab/raw-label mismatch, duplicate-label ambiguity, semantic-equipment rejection, the tracked 11-garment fixture, and flattened/missing full-Recommendation fields. Build an adapter that accepts the complete normalized RecommendInput and full finalized Recommendation, selects the occurrence before mutation, deep-clones all categorized layers plus notes/flags/finalizer data, substitutes the exact occurrence, calls the existing `finalizeSafety`, verifies target survival, and asks the canonical builder for a complete result. Static catalog data only nominates candidates; expose an option only when both full base and finalized outcome are supported. Never reconstruct categories from ordered strings, drop safety data, hand-author safety, or expose partial truth. Keep one transitionContextId while generating the outcome's own fingerprint/snapshotId.</action>
  <verify>
    <automated>npx vitest run src/lib/outfit/__tests__/alternative-options.test.ts src/lib/wool-layers/__tests__/finalize-safety.test.ts</automated>
  </verify>
  <done>Every selectable alternative is occurrence-correct, finalized, deterministic, and represented by a complete valid snapshot; static-only candidates and failures are unavailable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace raw global swaps with exact snapshot selection</name>
  <files>src/state/outfit-selection-store.ts, src/state/__tests__/outfit-selection-store.test.ts</files>
  <behavior>
    - Opening or setting a new base snapshot selects that exact immutable base and clears any outcome from another identity triple.
    - Selecting accepts only an option belonging to the current base transition and replaces the whole current snapshot atomically.
    - Reset returns byte-for-byte equivalent base truth; close clears base, options, selection, and diagnostics.
    - Forged, stale, partial, or mutable objects are rejected without changing current state.
  </behavior>
  <action>Create a small in-memory store whose state is `closed` or an exact base/options/current selection session. Validate with `isOutfitTruthSnapshot`, check option ownership and the full identity relationship, and keep snapshot objects immutable. Never persist advice to localStorage/sessionStorage and never key selection by display label. Include explicit `open`, `select`, `reset`, and `close` actions so route changes and repeated dialog opens are deterministic. This store is new; leave the legacy store untouched until its consumers are migrated in 02-06/02-07.</action>
  <verify>
    <automated>npx vitest run src/state/__tests__/outfit-selection-store.test.ts</automated>
  </verify>
  <done>Alternative state is occurrence-safe, exact-context-bound, ephemeral, atomic, and deterministically resettable with no stale raw swaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| static catalog → selectable option | Informational data is not safety validation |
| chosen occurrence → finalizer | Label collisions or index drift can replace the wrong layer |
| UI/store → exact context | Stale outcomes can cross scenario boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-05 | Tampering | finalized-outfit-swap.ts | critical | mitigate | exact occurrence selection, full clone, existing finalizer, post-finalization survival/invariant checks |
| T-02-06 | Spoofing | alternative option identity | high | mitigate | deterministic option/source/transition identity and strict snapshot validation |
| T-02-07 | Repudiation | rejected candidates | medium | mitigate | typed deterministic diagnostics covered by tests and recorded without exposing them as UI advice |
| T-02-08 | Information Disclosure | selection persistence | medium | mitigate | memory-only store; clear on close/base change; no browser storage |
| T-02-SC | Tampering | npm supply chain | high | mitigate | install no packages and reject manifest/lockfile changes |
</threat_model>

<failure_and_rollback>
Any candidate that cannot be proven to survive finalization as a supported 1-10-garment outcome remains unavailable; the base outfit remains unchanged. Equipment and 11+ garment outcomes remain complete list-only truth and never become map/alternative inputs. If identity tests or safety review fail, block all dependents rather than falling back to the static catalog or old raw swap store. Rollback is a commit revert of these new files; there is no persistent user data migration.
</failure_and_rollback>

<verification>
From the current candidate worktree, run the tracked `npx tsx scripts/outfit/inventory-v1.ts --assert` first, then focused tests, `npm run lint`, `npm run build`, and `git diff --check`; never execute or import `.planning/.../02-INVENTORY.mts`. Confirm the inventory script/test blobs exist in the candidate and no Motor/threshold, package, media, browser-storage, Hjem/Uke/Paakledning, or legacy-store change occurred. Review a clean archive at the produced exact SHA with two independent fresh contexts: one checks occurrence/finalizer safety and one checks identity/state/privacy. Both must PASS without unresolved P0/P1; record exact SHA, inventory blob IDs, and evidence in the summary.
</verification>

<success_criteria>
- The original label-key identity defect is captured by a regression test and cannot recur.
- “Se alternativ” data is selectable only when the existing engine/finalizer produced a full supported 1-10-garment outcome; equipment and list-only cardinalities expose no action.
- Select/reset/close behavior is deterministic and stale-context-safe.
- No legacy thresholds, package state, persistent storage, or media change.
</success_criteria>

<output>
Create `.planning/phases/02-outfit-truth-antrekkskart/02-02-SUMMARY.md` with machine-readable `status: PASS`, `plan_id: 02-02`, 40-hex `candidate_sha`, 02-01 dependency SHA/ancestry, tracked inventory script/test blob IDs, regression cases, omitted-candidate diagnostics, commands, two distinct reviewer identity/session/capability/fresh-context/PASS records, and rollback note.
</output>
