---
phase: 02-outfit-truth-antrekkskart
plan: 03
type: tdd
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/lib/outfit/outfit-map-layout.ts
  - src/lib/outfit/__tests__/outfit-map-layout.test.ts
autonomous: true
risk_lane: standard
minimum_implementation_capability: standard
minimum_reviewer_capability: standard
requirements:
  - OUTFIT-01
must_haves:
  truths:
    - "D-01: layouts for 1-10 garments contain one numbered visible node and one connector per garment, with no +N aggregation, hiding, or overlap"
    - "D-02: connectors terminate at the garment's semantic body anchor and equipment never enters body-connected layout"
    - "D-03: layout is a pure responsive rendering output; selection state cannot change geometry and no coordinate returns to canonical truth"
    - "The measured 11-garment equivalence class is map-ineligible and remains complete list-only truth; Phase 2 does not invent a third density mode"
  artifacts:
    - path: src/lib/outfit/outfit-map-layout.ts
      provides: deterministic spacious and compact-rail layout engine
    - path: src/lib/outfit/__tests__/outfit-map-layout.test.ts
      provides: exhaustive count/width/geometry invariants
  key_links:
    - from: src/lib/outfit/outfit-truth.ts
      to: src/lib/outfit/outfit-map-layout.ts
      via: ordered anchored garment occurrences are the only layout input
---

<objective>
Implement the pure responsive Antrekkskart geometry contract for D-01, D-02, and D-03 using test-first development.

Purpose: Make all canonical garments legible and connected at constrained widths without turning viewport-dependent positions into recommendation truth.
Output: A deterministic layout function and exhaustive geometry tests for 1-10 garments.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/02-outfit-truth-antrekkskart/02-RESEARCH.md
@.planning/phases/02-outfit-truth-antrekkskart/02-INTERFACE-CONTRACT.md
@.planning/phases/02-outfit-truth-antrekkskart/02-01-SUMMARY.md
</context>

<worktree_contract>
Run in parallel with 02-02 from the exact PASS SHA of 02-01 in a separate isolated worktree. This plan exclusively owns the two files above and introduces no UI, engine, integration, package, or media edits. Cost is NOK 0; D-12 blocks any action above NOK 1000.
</worktree_contract>

<tasks>

<task type="auto">
  <name>Task 1: RED — specify every responsive geometry invariant</name>
  <files>src/lib/outfit/__tests__/outfit-map-layout.test.ts</files>
  <action>Write a table-driven failing suite for counts 1 through 10 at widths 320, 390, and 560 CSS pixels plus a 200%-text constraint model. Require `spacious` mode for 1-4 and exactly two ordered compact rails of at most five nodes each for 5-10. Assert every occurrence appears exactly once, ordinal labels are 1-based inner-first, boxes remain finite and inside bounds, node boxes do not intersect, connector routes do not cross node boxes or each other, and connector endpoints equal semantic body anchors within tolerance. Assert semantic equipment is rejected/excluded, invalid counts or missing anchors return typed map-ineligible results, and layout output is deeply equal across every possible selected itemId. Include the exact 11-garment fixture from the reviewed candidate's `scripts/outfit/inventory-v1.ts`: it must return `unsupported-cardinality`/map-ineligible and preserve the complete ordered list outside layout; no planning-workspace oracle or third layout mode is accepted.</action>
  <verify>
    <automated>npx vitest run src/lib/outfit/__tests__/outfit-map-layout.test.ts</automated>
  </verify>
  <done>The suite fails for the missing implementation and encodes all D-01/D-02/D-03 count, order, width, connector, equipment, and selection-independence constraints.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN/REFACTOR — implement deterministic spacious and rail layouts</name>
  <files>src/lib/outfit/outfit-map-layout.ts, src/lib/outfit/__tests__/outfit-map-layout.test.ts</files>
  <action>Implement `layoutOutfitMap(snapshot, width)` as a pure bounded function accepting only a `supported` 1-10-garment snapshot and returning typed `spacious` or `compact-rails` node boxes and routed connector polylines. Allocate by canonical order, use deterministic rail slots rather than collision iteration, and maintain stable ordering as width changes. Use semantic normalized anchor points from truth only as connector targets; all node boxes and routes stay in this output. Reject `unsupported-cardinality`, invalid truth, count, width, anchor, or geometry with typed ineligibility rather than truncating, overlapping, estimating, or adding another density mode. Refactor only after the full test table passes.</action>
  <verify>
    <automated>npx vitest run src/lib/outfit/__tests__/outfit-map-layout.test.ts src/lib/outfit/__tests__/outfit-truth.test.ts</automated>
  </verify>
  <done>All 1-10 counts produce deterministic, bounded, non-overlapping, correctly anchored layouts at required widths; equipment and invalid inputs fail closed, and truth remains coordinate-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| immutable truth → geometry | Bad dimensions or missing anchors must not create deceptive connections |
| geometry → renderer | NaN, overlap, clipping, or reordering could hide outfit advice |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-09 | Tampering | layout input | medium | mitigate | accept only validated snapshots, finite widths, 1-10 anchored garments |
| T-02-10 | Spoofing | connector routing | high | mitigate | endpoints derive from explicit garment anchor and geometry invariants cover every count |
| T-02-11 | Denial of Service | responsive layout | low | mitigate | bounded deterministic slots with no unbounded collision loop |
| T-02-SC | Tampering | npm supply chain | high | mitigate | no installs; existing lockfile only |
</threat_model>

<failure_and_rollback>
Any unsatisfied geometry invariant or 11+ garment result produces a typed map-ineligible result so later UI can render the complete ordered text fallback; never hide, merge, aggregate, or reorder nodes. Roll back by reverting the two-file commit. Do not “fix” a layout failure by editing truth coordinates, engine output, or adding an unapproved third layout mode.
</failure_and_rollback>

<verification>
Complete RED/GREEN evidence, then run the current candidate's tracked `npx tsx scripts/outfit/inventory-v1.ts --assert`, `npm run lint`, `npm run build`, and `git diff --check`; never execute or import `.planning/.../02-INVENTORY.mts`. Confirm the inventory script/test blobs exist in the candidate and truth, engine, package, and media files are unchanged. Bind a clean archive to the exact commit SHA and obtain one fresh-context independent geometry/accessibility review with PASS and no unresolved P0/P1 before 02-04. Record SHA, inventory blob IDs, count/width matrix, and reviewer findings in the summary.
</verification>

<success_criteria>
- Counts 1-10 pass every width, ordering, visibility, overlap, and connector assertion.
- The measured 11-garment case is complete list-only truth and never enters map layout.
- Equipment has no body-connected layout.
- Selection does not move nodes and no responsive coordinates enter truth.
- Exact-SHA independent review passes with no media or package change.
</success_criteria>

<output>
Create `.planning/phases/02-outfit-truth-antrekkskart/02-03-SUMMARY.md` with machine-readable `status: PASS`, `plan_id: 02-03`, 40-hex `candidate_sha`, 02-01 dependency SHA/ancestry, tracked inventory script/test blob IDs, RED/GREEN commits, 1–10 and 11-list-only geometry matrix, commands, one fresh-context standard-capability reviewer identity/session/PASS record, and rollback note.
</output>
