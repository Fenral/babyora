---
phase: 02-outfit-truth-antrekkskart
plan: 09
type: execute
wave: 8
depends_on:
  - 02-08
files_modified:
  - e2e/fixtures/outfit-truth.html
  - e2e/fixtures/outfit-truth.tsx
  - e2e/outfit-truth.ts
  - src/App.tsx
  - src/screens/PaakledningScreen.tsx
  - src/screens/__tests__/PaakledningScreen.outfit-truth.test.tsx
  - src/lib/outfit/feature-flags.ts
  - scripts/outfit/verify-phase2-evidence.ts
  - scripts/outfit/run-phase2-evidence.ts
  - scripts/outfit/__tests__/verify-phase2-evidence.test.ts
autonomous: true
risk_lane: high
minimum_implementation_capability: high-implementation
minimum_reviewer_capability: high-verification
requirements:
  - OUTFIT-01
  - OUTFIT-02
must_haves:
  truths:
    - "Phase 2 ships the production Paakledning integration: the reusable panel consumes exact supported/list-only/unavailable bundles, forwards real Outfit-row registration, and is enabled only after every pre-activation gate passes"
    - "Serialized 02-09 is the sole initial App integration owner: both real current and planned open boundaries call `produceOutfitBundle`, retain that exact result in route state, and pass it with a stable RegisterOutfitRow and warm/cold callback into Phase-2-owned Paakledning"
    - "Plan 02-09 makes no further Hjem/Uke edit: it consumes the full seeds preserved by serialized 02-05; only after 02-09 PASS may Phase 3 extend App/Hjem for RegisterHomeAnchor/transition orchestration while preserving that seed flow and never editing Paakledning"
    - "Every locked semantic, presentation, avatar-occlusion, accessibility and honest 11-item fallback behavior passes component and browser gates on the enabled candidate"
    - "The final verifier exits nonzero for dirty state, wrong flag, nonlocal inventory evidence, mixed Phase-1 checksum spelling, mismatched SHA/PASS/ancestry tuples, or duplicate/unqualified reviews"
  artifacts:
    - path: src/screens/PaakledningScreen.tsx
      provides: enabled production OutfitTruthPanel integration and exact Phase-3 consumer props
    - path: src/App.tsx
      provides: real current/planned producer calls, route-state bundle propagation, App-lifetime Outfit-row registrar and warm/cold navigation callback
    - path: src/lib/outfit/feature-flags.ts
      provides: single compile-time activation constant kept false through pre-gates and true only on the final passing candidate
    - path: e2e/outfit-truth.ts
      provides: real panel plus production-screen DOM/layout/accessibility/live-prop validation without media
    - path: scripts/outfit/verify-phase2-evidence.ts
      provides: fail-fast clean-SHA, local-inventory, exact tuple, enabled-flag, ancestry and two-review gate
    - path: scripts/outfit/run-phase2-evidence.ts
      provides: path-safe checked-in evidence launcher using environment validation and direct argument arrays
  key_links:
    - from: src/screens/PaakledningScreen.tsx
      to: src/components/outfit/OutfitTruthPanel.tsx
      via: exact `OutfitBundleProducerResult` branch under `OUTFIT_TRUTH_V1_AVAILABLE`
    - from: src/screens/PaakledningScreen.tsx
      to: src/lib/outfit/outfit-transition-contract.ts
      via: unchanged `registerOutfitRow?: RegisterOutfitRow` forwarding to real semantic rows
    - from: src/App.tsx
      to: src/lib/outfit/outfit-bundle-producer.ts
      via: both explicit `Drill` current/planned open handlers call `produceOutfitBundle` from the context-owned seed
    - from: src/App.tsx
      to: src/screens/PaakledningScreen.tsx
      via: exact route-state bundle, stable row registrar, settled visual state and warm/cold guide callback
    - from: scripts/outfit/verify-phase2-evidence.ts
      to: .planning/phases/03-living-home-signature-transition/03-DEPENDENCY-CONTRACT.md
      via: exact enabled `phase2_candidate_sha`, public screen-prop interface, ancestry and review tuple
---

<objective>
Integrate and activate the complete Outfit truth experience across the real App current/planned route boundary and production `PaakledningScreen`, prove it, and publish the immutable Phase-3 dependency candidate.

Purpose: Complete the Phase-2 product goal without making further Hjem/Uke edits after 02-05, while giving Phase 3 an already-working App-to-Paak semantic flow and exact row-registration/visual-state consumer contract.
Output: Current/planned App production bootstrap, production screen integration, enabled flag, component and real-route browser gates, path-safe evidence verifier launcher, and exact `02-09-SUMMARY.md` handoff.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/01-planlegg-dagslinjen/01-18-SUMMARY.md
@.planning/phases/02-outfit-truth-antrekkskart/02-INTERFACE-CONTRACT.md
@.planning/phases/02-outfit-truth-antrekkskart/02-VALIDATION.md
@.planning/phases/02-outfit-truth-antrekkskart/02-COVERAGE.md
@.planning/phases/02-outfit-truth-antrekkskart/02-08-SUMMARY.md
@.planning/phases/03-living-home-signature-transition/03-DEPENDENCY-CONTRACT.md
@src/screens/PaakledningScreen.tsx
@e2e/planlegg.ts
</context>

<worktree_contract>
Continue from the exact PASS SHA of 02-08 in the clean integration worktree based on exact Phase-1 `phase1_candidate_sha`/`contract_sha256`/`pack_sha256`. This plan is the sole Phase-2 owner of `src/screens/PaakledningScreen.tsx` and serialized initial owner of the minimal `src/App.tsx` producer/prop bootstrap; Phase 3 starts only after this PASS, preserves that bootstrap, and must not edit Paakledning. This plan may edit only its ten declared paths and must not edit Hjem, Uke, existing `e2e/planlegg.ts`, Phase-3 files, packages, engine thresholds, global tokens or media. The accepted scope warning is cohesive: ten files form one production bootstrap plus executable evidence, split across tasks of 3/5/3 files (with `e2e/outfit-truth.ts` intentionally shared); splitting App propagation from the flag/screen gate would leave an enabled panel unreachable or unreviewed. Begin with the flag false. Tests must not capture screenshot/video/image/trace media. NOK 0; D-12 blocks actions over NOK 1000.
</worktree_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the complete no-media component-browser matrix</name>
  <files>e2e/fixtures/outfit-truth.html, e2e/fixtures/outfit-truth.tsx, e2e/outfit-truth.ts</files>
  <behavior>
    - Real component fixtures cover 1, 4, 5 and 10 semantic garments at 320/390/560 CSS pixels, 100%/200% text, light/dark × kald/mild/varm, normal/forced colors and normal/reduced motion.
    - Supported fixtures assert exact heading, spacious/compact mode, full node/row/connector counts/order, normal-flow caption, equipment separation, three-field interaction, persistent paired `aria-pressed`, no focus stealing, and stable geometry.
    - Computed inactive connector contrast is at least 3:1 against actual adjacent background; active connector also differs by width plus dash/pattern; forced colors resolves to system behavior.
    - The candidate-local inventory's exact 11-garment fixture renders all 11 ordered rows plus equipment and no map, connector, avatar, alternative, row registration or motion eligibility.
    - Canonical coverage/slot/rank/explicit-occlusion derivation passes every one of the 24 manifest rows plus hidden inner layer; duplicate occurrence/catalog ID, rank tie, null/unknown/ambiguous coverage, missing/extra item and pose mismatch are neutral.
  </behavior>
  <action>Create a dedicated Vite fixture that mounts real `OutfitTruthPanel` inputs without importing App/Hjem/Uke. Import the exact 11 fixture only from the tracked candidate's `scripts/outfit/inventory-v1.ts`. Assert DOM rectangles, computed styles/colors, ARIA, focus ownership, row lifecycle and text directly. Add exhaustive manifest-row/occlusion fixtures; do not accept the planning-only inventory, old level resolver, partial manifest match, screenshot, video, image snapshot or visual trace.</action>
  <verify>
    <automated>cmd.exe /d /s /c "npx tsx scripts/outfit/inventory-v1.ts --assert &amp;&amp; npm run build &amp;&amp; npx tsx e2e/outfit-truth.ts --case component-matrix"</automated>
  </verify>
  <done>The candidate-local real component passes every locked supported/list-only/state/contrast/avatar-occlusion branch without app wiring or media.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire and gate the real App-to-Paak current/planned flow</name>
  <files>src/App.tsx, src/screens/PaakledningScreen.tsx, src/screens/__tests__/PaakledningScreen.outfit-truth.test.tsx, src/lib/outfit/feature-flags.ts, e2e/outfit-truth.ts</files>
  <behavior>
    - `PaakledningScreen` accepts the exact additive public props `outfitBundle?: OutfitBundleProducerResult`, `registerOutfitRow?: RegisterOutfitRow`, and `transitionVisualState?: "settled" | "landing"`.
    - With the flag false, the new bundle branch cannot mount. With the flag true and supported bundle, the production screen renders `OutfitTruthPanel` and forwards the same registrar function unchanged to every real ordered row.
    - App's explicit current and planned `Drill` open handlers each call `produceOutfitBundle` once with `context.outfitProducerSeed` containing the preserved full normalized input/finalized Recommendation and the matching source discriminant, retain the result in route state, and never infer source from access capability.
    - Both production Paakledning branches receive the exact route-state bundle, a stable App-lifetime `RegisterOutfitRow`, `transitionVisualState="settled"`, and `() => setDrill({ kind: "guide", target: "varm-kald" })`; production never omits or directly injects the bundle.
    - Unsupported cardinality renders the complete 11-item list-only truth; unavailable/invalid bundle is neutral. Optional bundle absence preserves only the unchanged legacy/fixture fallback; the screen never recomputes.
    - `"landing"` preserves heading, list/map, controls, focus, registration and accessibility at T0; it is visual-only and never uses hidden/display-none/visibility-hidden/inert.
    - Hjem and Uke are unchanged; App has no Home registrar or generic anchor-kind callback in Phase 2.
  </behavior>
  <action>Keep `OUTFIT_TRUTH_V1_AVAILABLE` false while adding the additive typed Paak props, exact producer-result guard and production `OutfitTruthPanel` branch. In App, extend current/planned Drill variants with the exact bundle; call `produceOutfitBundle` in each real open handler from the context-owned seed plus explicit current/planned source, and retain the result through render/close/reopen. Add one App-lifetime itemId-to-element registry behind a stable callback, then pass bundle, callback, settled visual state and the exact warm/cold route callback into both Paak branches. Forward `registerOutfitRow` unchanged inside Paak. Do not edit Hjem/Uke or derive a bundle during render. First run focused tests, build, `e2e/planlegg.ts --case exact-context`, `e2e/planlegg.ts --case automatic-location`, and real App-route Outfit E2E with the flag false. Only after all pre-activation commands pass may the sole flag become true. Rerun all commands with supported current/planned, 11 list-only, invalid, registrar lifecycle, close/reopen/reset and guide navigation. App-route cases enter through real Hjem and Planlegg UI and may not mount Paak or inject props directly. Any later failure restores false before repair and restarts both stages.</action>
  <verify>
    <automated>cmd.exe /d /s /c "npx vitest run src/screens/__tests__/PaakledningScreen.outfit-truth.test.tsx src/components/outfit/__tests__/OutfitTruthPanel.test.tsx src/components/outfit/__tests__/OutfitExperience.test.tsx &amp;&amp; npm run build &amp;&amp; npx tsx e2e/planlegg.ts --case exact-context &amp;&amp; npx tsx e2e/planlegg.ts --case automatic-location &amp;&amp; npx tsx e2e/outfit-truth.ts --case production-app-routes"</automated>
  </verify>
  <done>The enabled real App current/planned routes produce and propagate exact bundles into Paak, forward row/guide/visual props, preserve legacy fallback only outside production, and leave Hjem/Uke untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bind final evidence and Phase-3 handoff to the enabled SHA</name>
  <files>scripts/outfit/verify-phase2-evidence.ts, scripts/outfit/run-phase2-evidence.ts, scripts/outfit/__tests__/verify-phase2-evidence.test.ts</files>
  <behavior>
    - Verifier requires a 40-hex candidate, existing commit, exact `HEAD === candidate`, byte-empty `git status --porcelain --untracked-files=all`, and `OUTFIT_TRUTH_V1_AVAILABLE === true`.
    - Candidate record uses only exact Phase-1 `phase1_candidate_sha` (40 hex), `contract_sha256` and `pack_sha256` (64 hex); any camelCase, mixed spelling, duplicate alias or ambiguity exits nonzero.
    - The launcher requires one 40-hex `PHASE2_CANDIDATE_SHA` and absolute existing `PHASE2_CANDIDATE_RECORD`, `PHASE2_REVIEW_A` and `PHASE2_REVIEW_B` paths, including paths containing spaces, then invokes the verifier through a direct argument array without shell interpolation.
    - The tracked candidate contains `scripts/outfit/inventory-v1.ts` and its test; the verifier runs that detached-worktree artifact and rejects a planning-workspace path or mismatched metrics.
    - Exact 02-01..02-08 SHAs exist and are ancestors; candidate record says PASS on the same SHA.
    - Two external review files have distinct reviewer/session IDs, fresh context, minimum high-verification capability, PASS, no unresolved P0/P1 and the same enabled candidate.
  </behavior>
  <action>Write adversarial verifier/launcher tests for dirty tracked/untracked state, wrong HEAD/flag, missing/nonlocal inventory, altered metrics, short/nonhex/nonancestor SHA, absent/wrong snake_case checksums, aliases/duplicates, FAIL, duplicate reviewer/session, stale/insufficient review, mismatched SHA, unresolved P0/P1, missing env, relative paths and absolute paths containing spaces. Export a verifier entry callable with a string argument array. Implement `run-phase2-evidence.ts` as the checked-in Node launcher: validate the four task-specific environment values, resolve/reject paths without shell parsing, and call or spawn the verifier with `["--candidate", sha, "--candidate-record", record, "--review-a", reviewA, "--review-b", reviewB]`. After the final enabled tree passes all component and real App-route E2E, scope/package/no-media scans, commit and review only a clean detached checkout. Dispatch two distinct high-verification fresh contexts. Any edit creates a new SHA and restarts both reviews. Set the four env values to quoted absolute paths (for example from `Resolve-Path`) and invoke only the launcher, then write the exact summary.</action>
  <verify>
    <automated>cmd.exe /d /s /c "npx vitest run scripts/outfit/__tests__/verify-phase2-evidence.test.ts &amp;&amp; npx tsx scripts/outfit/run-phase2-evidence.ts"</automated>
  </verify>
  <done>One clean enabled SHA contains the production screen, local inventory oracle, complete gates and two qualified PASS reviews; Phase 3 can consume it without editing Paakledning.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| exact bundle → production screen | Invalid/list-only/absent data must not trigger recomputation or graphical claims |
| Phase-3 callbacks → Phase-2 screen | Home and Outfit registration roles or visual state could be conflated |
| candidate-local oracle/evidence → final PASS | Untracked workspace evidence, mixed tuple labels or dirty state can spoof a reviewed candidate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-31 | Spoofing | production bundle branch | critical | mitigate | exact discriminated guard; supported/list-only/unavailable behavior; no recompute |
| T-02-32 | Elevation of Privilege | activation flag | critical | mitigate | false through pre-gates, single constant, full rerun after true, false rollback on failure |
| T-02-33 | Tampering | row/visual props | high | mitigate | exact additive props, registrar identity/lifecycle tests, semantic-at-T0 visual contract |
| T-02-34 | Tampering | final evidence | critical | mitigate | clean exact HEAD, candidate-local tracked oracle, strict snake_case tuple and ancestry |
| T-02-35 | Repudiation | two-key review | critical | mitigate | distinct reviewer/session IDs, fresh contexts, qualified same-SHA PASS |
| T-02-36 | Information Disclosure | media evidence | medium | mitigate | DOM/computed-style only and prohibited-media scan |
| T-02-37 | Tampering | evidence launcher/paths | high | mitigate | required absolute env paths, path-with-spaces tests and direct verifier argument arrays |
| T-02-SC | Tampering | npm supply chain | high | mitigate | unchanged manifests/lockfile; no installs |
</threat_model>

<failure_and_rollback>
Any failed pre-gate leaves the flag false. Any failure after activation first restores false, then repairs and repeats both stages and both reviews. Invalid bundles are neutral, 11+ remains complete list-only, and absent bundle preserves only the safe legacy/fixture fallback without recomputation; real App routes must always provide a result. Missing/mixed tuple fields, relative or shell-split evidence paths, nonlocal inventory evidence, dirty state, ancestry failure, screen-ownership collision, media/package drift or review defect is BLOCKED. Rollback is the exact 02-08 false-flag SHA plus accepted Phase-1 tuple.
</failure_and_rollback>

<verification>
Follow `02-VALIDATION.md` exactly. Require candidate-local inventory constants, all-manifest-row occlusion cases, focused/full suites, unchanged Phase-1 browser regression, component plus real current/planned App-route browser matrices, enabled flag, exact bundle/row/visual/guide prop contract, only the declared minimal App bootstrap and no Hjem/Uke edit, path-safe launcher tests, scope/package/no-media scans, clean exact SHA, strict Phase-1 source-field normalization, dependency ancestry and two distinct qualified fresh-context PASS verdicts.
</verification>

<success_criteria>
- Production `PaakledningScreen` renders the exact supported panel, honest 11-item list-only fallback and neutral invalid state under the enabled flag.
- Real current/planned App routes call the producer and propagate the exact result plus row/guide/visual props without direct E2E prop injection.
- Real rows register through Phase 2's exact callback; Phase 3 may later extend App/Hjem/Home/transition only after 02-09 and preserves this baseline.
- Exact heading/layout/state/contrast/recovery/alternatives and canonical avatar occlusion pass real browser gates.
- Final evidence is bound to tracked candidate-local inventory, exact snake_case Phase-1 tuple, clean ancestry and two qualified reviews.
</success_criteria>

<output>
Create `.planning/phases/02-outfit-truth-antrekkskart/02-09-SUMMARY.md` with line-addressable `status: PASS`, `phase2_candidate_sha: <40hex>` and `feature_flag: true`; `phase1_source_field: candidate_sha` plus normalized Phase-1 tuple; 02-01..02-08 dependency ancestry; tracked inventory metrics; full-input/full-finalized-Recommendation seed signature and 02-05 Hjem/Uke preservation audit; App current/planned producer calls; row/visual/guide/Paak signatures; component and real App-route matrices with no direct prop injection; launcher path-space cases; scope/cost manifests; two qualified PASS reviews; serialized Phase-2 02-05/02-09 versus Phase-3 later Hjem/App ownership; and rollback SHA.
</output>
