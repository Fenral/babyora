---
phase: 01-planlegg-dagslinjen
plan: "06"
type: execute
wave: 6
package_wave: 3
depends_on: ["01-05"]
files_modified:
  - src/lib/planning/planning-interaction.ts
  - src/lib/planning/__tests__/planning-interaction.test.ts
  - src/components/planning/PlanChangeRail.tsx
  - src/components/planning/PlanChangeRail.css
  - src/components/planning/__tests__/PlanChangeRail.test.tsx
  - src/components/controls/SegmentedControl.tsx
  - src/components/controls/SegmentedControl.css
  - src/screens/UkeScreen.tsx
  - src/App.tsx
  - e2e/planlegg.ts
autonomous: true
requirements: [UI-01, A11Y-01, EVID-01, GOV-04]
must_haves:
  truths:
    - "Dagslinjen is one controlled semantic ordered list; static spans are non-interactive, `onSelect` accepts `string | null`, and zero/one/many states never permit multiple expanded events."
    - "PlanChangeRail receives `onOpenOutfit(eventId, trigger)` only; it never receives/stores a DTO, transitionContextId or plannedContextId."
    - "The rail owns expansion/collapse intent: a newly expanded event requests one guarded light cue, while repeat and collapse request none; Uke owns only actual view/date selection cues and automatic refresh repair requests none."
    - "UkeScreen atomically builds canonical events/rows/context map, migrates the live rail, resolves local `onOpenOutfit(eventId, trigger)` fail-closed, then calls the trusted App `onOpenPlannedOutfit(dto, trigger)` callback in the same candidate."
    - "Only the preference-aware dispatcher returned by `useHapticSystem()` may execute a pure cue; native-adapter preference-off cardinality is proven later in 01-18."
    - "Browser evidence checks visible state, focus and keyboard only; haptic call counts are unit/component concerns and physical feel remains Pending."
  artifacts:
    - path: "src/lib/planning/planning-interaction.ts"
      provides: "Pure interaction decision plus dependency-injected dispatch seam"
      exports: ["decidePlanningInteraction", "dispatchPlanningInteraction"]
    - path: "src/components/planning/PlanChangeRail.tsx"
      provides: "Controlled semantic Dagslinjen"
      exports: ["PlanChangeRail"]
    - path: "src/components/planning/__tests__/PlanChangeRail.test.tsx"
      provides: "ReactDOM-server semantic/marker contracts without jsdom/testing-library"
  key_links:
    - from: "src/components/planning/PlanChangeRail.tsx"
      to: "src/lib/planning/planning-interaction.ts"
      via: "pure rail expansion/collapse decision and dependency-injected dispatch before guarded haptic fire/onSelect"
      pattern: "decidePlanningInteraction"
    - from: "src/components/planning/PlanChangeRail.tsx"
      to: "src/screens/UkeScreen.tsx"
      via: "eventId/currentTarget callback; Uke owns context map and trusted App handoff"
      pattern: "onOpenOutfit"
  prohibitions:
    - "MUST NOT add row-local React state, derive recommendations/events/access in the rail, pass a DTO/context ID to it, make required controlled props optional, call the unguarded module-level haptic `fire`, or add a haptic abstraction beyond the pure decision helper."
    - "MUST NOT instrument/mock haptic calls in browser E2E or claim physical tactile PASS."
    - "MUST NOT add packages/design system/API/schema/media."
---

<objective>
Implement the controlled semantic Dagslinjen and a testable interaction-decision seam.

Purpose: Make visible behavior and haptic intent deterministic without browser test hooks or sensitive context props.
Output: Pure interaction helper/tests, controlled rail, scoped CSS, compliant view control and non-media semantic E2E.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-planlegg-dagslinjen/01-05-SUMMARY.md
@.planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md
@src/components/planning/PlanChangeRail.tsx
@src/lib/planning/rail-rows.ts
@src/lib/planning/change-events.ts
@src/lib/haptics/system.ts
@src/components/controls/SegmentedControl.tsx
@src/data/garment-illustrations.ts
@src/styles/design-tokens.css
</context>

<delivery_contract>
- Risk/model: high exact-context/UI atomic migration; Claude Fable 5, Extra effort for the entire candidate. The source-grounded migration cannot safely split the live Uke map/handoff from the controlled rail without introducing an untrusted projection, so the standard UI slice is escalated rather than letting a standard executor own Task 3.
- Candidate commit: `feat: atomically wire exact Planlegg rail`.
- Reviewers: fresh Fable 5 Extra high-risk verifier plus a separate fresh Sonnet 5 High UI/accessibility verifier on the same immutable SHA; neither executor nor reviewer may waive the other lane.
- Evidence boundary: unit/component/DOM/keyboard checks only; no browser haptic count or media/device claim.
</delivery_contract>

<tasks>

<task type="auto">
  <name>Task 1: Establish expected old-contract RED safely</name>
  <files>src/lib/planning/__tests__/planning-interaction.test.ts, src/components/planning/__tests__/PlanChangeRail.test.tsx</files>
  <read_first>src/components/planning/PlanChangeRail.tsx, src/lib/planning/rail-rows.ts, src/lib/planning/change-events.ts, src/lib/planning/__tests__/planlegg-fixtures.ts, src/lib/planning/__tests__/planning-interaction.test.ts, .planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md</read_first>
  <behavior>
    - Pure tests separate Uke view/date selection from rail expansion: actual view/date change=selection, new event expansion=light, repeated same event=none, collapse=null+none and refresh repair=none.
    - Component contract named `RED_CONTROLLED_RAIL_CONTRACT` asserts `onSelect(eventId: string | null)`, one controlled expansion and exact `(eventId, trigger)` opener; its expected old-state failure uses message `OLD_RAIL_OWNS_LOCAL_OPEN_STATE`.
    - Pure dependency-injected dispatch tests prove exact `onSelect`/cue callback cardinality; ReactDOM server rendering covers only markup: one ol/li tree, static rows, time datetime, action/cause, max three safe previews, CTA availability and canonical marker kinds `add|remove|swap|rain|location|prep` (`prep` is presented as preparation/`Forbered`).
  </behavior>
  <action>Replace the pending tests before source edits. Use framework-free pure decision/dispatch tests with injected `onSelect`/cue callbacks for call ordering and cardinality. Use `react-dom/server`/`renderToStaticMarkup`, which already exists in the repository dependency graph, only for static markup/ARIA/marker assertions; it must not claim to execute click handlers. Do not introduce jsdom or testing-library. Use an assertion message exactly `OLD_RAIL_OWNS_LOCAL_OPEN_STATE` for the observable old component mismatch. The RED command must require nonzero plus both the named test and signature so syntax/import failures cannot pass.</action>
  <verify><automated>powershell -NoProfile -Command '$out = (& npx vitest run src/components/planning/__tests__/PlanChangeRail.test.tsx --reporter=verbose 2>&1 | Out-String); $code = $LASTEXITCODE; if ($code -eq 0) { throw "Expected RED" }; if ($out -notmatch "RED_CONTROLLED_RAIL_CONTRACT" -or $out -notmatch "OLD_RAIL_OWNS_LOCAL_OPEN_STATE") { $out; throw "Unexpected RED signature" }'</automated></verify>
  <acceptance_criteria>
    - RED cannot pass for unrelated compile/fixture errors.
    - Pure decision/dispatch tests specify Uke selection, rail light/repeat/collapse/null selection, refresh repair and exact callback cardinality; ReactDOM-server tests specify static semantics only.
    - Browser harness contains no haptic test hook/count expectation.
  </acceptance_criteria>
  <done>The intended controlled contract fails for the exact known old behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the controlled rail and native view control</name>
  <files>src/lib/planning/planning-interaction.ts, src/lib/planning/__tests__/planning-interaction.test.ts, src/components/planning/PlanChangeRail.tsx, src/components/planning/PlanChangeRail.css, src/components/planning/__tests__/PlanChangeRail.test.tsx, src/components/controls/SegmentedControl.tsx, src/components/controls/SegmentedControl.css</files>
  <read_first>src/components/planning/__tests__/PlanChangeRail.test.tsx, src/lib/planning/__tests__/planning-interaction.test.ts, src/lib/planning/change-events.ts, src/lib/planning/change-sentence.ts, src/lib/planning/rail-rows.ts, src/components/planning/PlanChangeRail.tsx, src/components/controls/SegmentedControl.tsx, src/screens/UkeScreen.tsx, src/lib/planning/plan-view-model.ts, src/lib/planning/planned-outfit-context.ts, src/App.tsx, src/lib/haptics/system.ts, src/data/garment-illustrations.ts, src/styles/design-tokens.css</read_first>
  <action>Implement pure interaction decisions with explicit ownership: the view/date request helper maps only a real user change to `selection`; the rail request helper maps a new expansion to `light`, repeat to no cue, and collapse to `onSelect(null)` plus no cue; refresh repair is represented separately as silent state repair. Add a small dependency-injected `dispatchPlanningInteraction(decision, { onSelect, onCue })` seam so Node tests can prove exact callback ordering/cardinality without a DOM. Rewrite rail props to required `rows`, `selectedEventId`, `onSelect(eventId: string | null)` and `onOpenOutfit(eventId, trigger)` only. The rail computes toggle intent, dispatches it once through that seam, and supplies the preference-aware hook dispatcher as `onCue`; it never owns local React state or calls the App boundary.

Render decoration outside direct ol children; each row is li, event disclosure is a native button with aria-expanded and time datetime. For every garment label call existing `garmentIdFor(label)` first and pass only that ID to `garmentPngSafe(id)`; use `GENERIC_GARMENT_SVG` on a missing/unapproved asset. Show max three decorative previews beside authoritative visible names. Render distinct shape/icon/text contracts for all six canonical marker kinds: `add`, `remove`, `swap`, `rain`, `location` and `prep`; present `prep` as preparation/`Forbered` without renaming the typed kind. Obtain the preference-aware dispatcher only through `useHapticSystem()` and pass it only for a pure non-null cue. Preserve SegmentedControl's fieldset, named/hidden legend, visually hidden radios and controlled value/onChange; move its styling into `SegmentedControl.css` and add 44px, focus-visible and forced-colors rules without embedded haptics or a div-button rewrite. Use existing tokens, 240/200ms motion and immediate reduced-motion state.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/lib/planning/__tests__/planning-interaction.test.ts src/components/planning/__tests__/PlanChangeRail.test.tsx && npm run lint && npm run build"</automated></verify>
  <acceptance_criteria>
    - Rail owns no open-state useState and accepts no context/DTO prop.
    - Helper tests prove view/date, new expansion, repeat, collapse and refresh-repair decisions; rail contains the only event-expansion cue site and Uke contains only view/date cues.
    - Direct ol children are li; targets are at least 44px and meaning is shape+text.
    - Each preview resolves `garmentIdFor(label)` then `garmentPngSafe(id)`; assets are decorative, fallback keeps the canonical visible name, and the six marker-type server-render matrix passes.
    - ReactDOM-server/pure tests preserve SegmentedControl fieldset/legend/hidden-radio/controlled-value semantics without a jsdom/testing-library dependency.
    - Dependency-injected dispatch tests prove a new expansion calls `onSelect` once and the guarded cue wrapper once, while repeat/collapse follow their exact selection semantics and call the cue wrapper zero times. Server-render tests do not claim interaction execution; native-adapter preference-off behavior remains 01-18 scope.
  </acceptance_criteria>
  <done>The ID-only rail and native view control are controlled, semantic and testable without unavailable DOM libraries.</done>
</task>

<task type="auto">
  <name>Task 3: Atomically migrate Uke/App and prove the live exact-context flow</name>
  <files>src/screens/UkeScreen.tsx, src/App.tsx, e2e/planlegg.ts</files>
  <read_first>src/screens/UkeScreen.tsx, src/App.tsx, src/lib/planning/plan-view-model.ts, src/lib/planning/change-events.ts, src/lib/planning/rail-rows.ts, src/lib/planning/planned-outfit-context.ts, src/lib/planning/planned-outfit-resolver.ts, src/components/planning/PlanChangeRail.tsx, e2e/planlegg.ts, e2e/fixtures/planlegg.ts</read_first>
  <action>In one compiling candidate, replace the live legacy projection with one memoized Uke evaluation that creates finalized points, canonical events, canonical rows and a newly allocated immutable `ReadonlyMap<string, PlannedOutfitContext>` from the exact same facts. Stop all runtime imports of the 01-03 deprecated adapters/projection while keeping their definitions/tests intact. Uke owns `selectedEventId: string | null`; its `onSelect(nextId)` only stores controlled rail state and emits no event haptic. Uke owns the view/date `selection` cue only for an actual user change; deterministic refresh repair selects the next valid ID or null without a cue. Define local `onOpenOutfit(eventId, trigger)` to call the pure 01-05 resolver against the currently rendered event set/map. Only an exact result calls App's trusted `onOpenPlannedOutfit(dto, trigger)` callback; a miss removes/refuses the CTA and performs no navigation or current fallback. App revalidates the DTO guard before placing it in the discriminated planned drill. Until Plan 01-12 replaces the configured-place seam with the privacy-safe resolver, every planned DTO records place source `configured-place`; source is part of the immutable identity and is never omitted or inferred later.

Implement `semantic-rail` for zero/one/many and the full visible/keyboard/geometry/fallback/six-marker matrix. Introduce `exact-context` with a future event whose date/place/activity/temperature/garments differ from current data; assert every opened Outfit dimension, title focus and exact origin/view/selection/scroll return. After refresh/removal, assert the CTA disappears and no navigation occurs; do not try to activate a detached stale element or infer a specific internal mismatch from browser output because pure resolver tests own all mismatch classes. Inspect no haptic calls/globals/native bridge and write no media.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/lib/planning/__tests__/planned-outfit-resolver.test.ts src/lib/planning/__tests__/planned-outfit-context.test.ts src/lib/planning/__tests__/planning-interaction.test.ts src/components/planning/__tests__/PlanChangeRail.test.tsx && npm run build && npx tsx e2e/planlegg.ts --case semantic-rail && npx tsx e2e/planlegg.ts --case exact-context"</automated></verify>
  <acceptance_criteria>
    - Visible state/focus/keyboard assertions pass for zero/one/many, all six marker kinds and safe garment fallback.
    - Uke creates events/rows/map in one pass; live runtime imports no legacy adapter/projection and the real callsite compiles with exact ID-only callbacks.
    - Only a resolver hit crosses Uke-to-App as a DTO; stale CTA disappears and browser navigation does not occur.
    - No browser haptic instrumentation or call-count assertion exists.
    - No screenshot/video/trace/file is produced.
  </acceptance_criteria>
  <done>Non-media browser evidence proves rail behavior while tactile quality remains a later human gate.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat | Mitigation / disposition |
|---|---|
| UI leaks sensitive DTO | Component receives event ID/availability only; parent map owns DTO. |
| Multiple disclosures conflict | Controlled single selectedEventId. |
| Untrusted labels/images inject | React escaping and local safe resolver. |
| Browser mocks falsely attest haptics | Haptic decisions unit-tested; browser checks visible state only. |
</threat_model>

<artifacts_this_phase_produces>
- New interaction decision helper/test, rail CSS/component test and controlled callback contract.
- No haptic-system change, package, API, schema or media artifact.
</artifacts_this_phase_produces>

<verification>
- Interaction/component suites, semantic-rail and exact-context E2E, lint/build and prior truth/context suites pass.
- Fresh high-risk exact-context/map and separate standard UI/accessibility reviewers verify the same exact SHA; physical haptic remains Pending.
</verification>

<rollback>
Revert `feat: atomically wire exact Planlegg rail` as one high-risk candidate; restore helper/component/control/tests together.
</rollback>

<success_criteria>
- Exact controlled callback, one-expanded semantics and interaction decisions pass.
- Browser has no haptic count mechanism.
- High-risk and standard UI regression PASS are exact-SHA bound; media/device/release remain Pending.
</success_criteria>

<output>
Create `01-06-SUMMARY.md` with RED signature, semantic/unit/decorative-media evidence, exact candidate SHA, both standard and high-risk map/context reviewer identities/lanes, each PASS/BLOCK verdict bound to that same SHA, legacy-adapter zero-runtime-import evidence and deferred standalone cleanup note, and all remaining Pending tactile/media/device/release gates.
</output>
