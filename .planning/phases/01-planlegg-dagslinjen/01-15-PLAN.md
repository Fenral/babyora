---
phase: 01-planlegg-dagslinjen
plan: "15"
type: execute
wave: 15
package_wave: 5
depends_on: ["01-14"]
files_modified:
  - src/components/planning/SnartPlan.tsx
  - src/components/planning/SnartPlan.css
  - src/components/planning/__tests__/SnartPlan.test.tsx
  - src/lib/planning/snart-session.ts
  - src/lib/planning/__tests__/snart-session.test.ts
  - src/lib/premium/products.ts
  - src/lib/premium/__tests__/products.test.ts
  - src/screens/UkeScreen.tsx
autonomous: true
requirements: [ACCESS-01, UI-02, A11Y-01, GOV-04]
must_haves:
  truths:
    - "The component renders exactly the model's `ready | empty | unavailable` discriminants and never locally filters items or synthesizes empty."
    - "`Har allerede` exists only for actionable items, emits a concept ID, stays session-only and never appears for `not_yet`."
    - "Mark-all with no `not_yet` renders authoritative empty; mark-all with remaining `not_yet` renders authoritative ready with only `not_yet`."
    - "Free/loading evaluates neither climate nor model and never retains paid advice; allowed evaluation uses only the active child's fixed-home projection."
    - "Automatic/effective display place changes cannot affect Snart input, climate profile, trace or result."
  artifacts:
    - path: "src/components/planning/SnartPlan.tsx"
      provides: "Accessible exhaustive presentation of model-owned states"
      exports: ["SnartPlan"]
    - path: "src/lib/planning/snart-session.ts"
      provides: "Access-first fixed-home projection and memoized evaluator"
      exports: ["projectSnartSession", "createSnartSessionEvaluator", "snartSessionBoundaryKey"]
    - path: "src/screens/UkeScreen.tsx"
      provides: "Disabled-capability-ready Snart view wiring without model leakage"
  key_links:
    - from: "src/screens/UkeScreen.tsx"
      to: "src/lib/planning/snart-session.ts"
      via: "access decision precedes climate lookup and model construction"
      pattern: "projectSnartSession"
    - from: "src/components/planning/SnartPlan.tsx"
      to: "src/lib/planning/snart.ts"
      via: "exhaustive discriminated result rendering"
      pattern: "ready|empty|unavailable"
---

<objective>
Build the accessible Snart presentation and access-first fixed-home session orchestration without enabling the capability.

Purpose: Make the independently approved model render honestly while proving no advice is calculated or retained for Free/loading paths.
Output: Exhaustive component, session-only orchestration, contextual purchase presentation and tests; capability remains off.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-planlegg-dagslinjen/01-14-SUMMARY.md
@.planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md
@.planning/phases/01-planlegg-dagslinjen/01-SNART-RULES.md
@src/lib/planning/snart.ts
@src/lib/premium/gating.ts
@src/lib/premium/plus-features.ts
@src/lib/premium/paywall-copy.ts
@src/state/location-pref-store.ts
@src/screens/UkeScreen.tsx
</context>

<delivery_contract>
- Risk/model: high advice/access/privacy presentation risk; fresh external no-leak/copy review on the candidate.
- Entry requires Plan 01-14's exact rules/model/climate hashes and independent PASS to remain valid.
- This plan does not flip `soon_preparation`, change pricing/RevenueCat, remove routes or create media.
</delivery_contract>

<tasks>

<task type="auto">
  <name>Task 1: Implement the exhaustive accessible Snart component</name>
  <files>src/components/planning/SnartPlan.tsx, src/components/planning/SnartPlan.css, src/components/planning/__tests__/SnartPlan.test.tsx</files>
  <read_first>.planning/phases/01-planlegg-dagslinjen/01-14-SUMMARY.md, .planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md, .planning/phases/01-planlegg-dagslinjen/01-SNART-RULES.md, src/lib/planning/snart.ts, src/styles/design-tokens.css</read_first>
  <action>Revalidate Plan 01-14's approved metadata, artifact hashes and exact candidate PASS before editing. Implement a semantic exhaustive renderer using existing tokens and natural page flow. Ready renders the approved period, historical-normal qualifier, uncertainty note and groups `Bør ha`, `Kjekt å ha`, `Ikke prioritert nå`. Empty renders only the model's valid empty copy. Unavailable is neutral, offers the approved recovery action/context and includes zero concepts or advisory copy. The component receives the authoritative result and emits `onMarkAlreadyHave(conceptId)` only for actionable rows; it owns no item filter or state transition. Tests must prove mark-all without visible `not_yet` renders supplied empty, while mark-all with `not_yet` renders supplied ready containing only `not_yet`; no mark control appears there. Use semantic buttons/lists/headings, 44px geometry, keyboard support, `:focus-visible`, forced-colors affordances and 200% wrapping. Add no nested scroll, generated examples, persistence, analytics or network import.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/lib/planning/__tests__/snart.test.ts src/components/planning/__tests__/SnartPlan.test.tsx && npm run lint"</automated></verify>
  <done>All approved model states, including both mark-all outcomes, have one accessible component presentation with no local model logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate fixed-home Snart evaluation before payload construction</name>
  <files>src/lib/planning/snart-session.ts, src/lib/planning/__tests__/snart-session.test.ts, src/lib/premium/products.ts, src/lib/premium/__tests__/products.test.ts, src/screens/UkeScreen.tsx</files>
  <read_first>src/lib/premium/gating.ts, src/lib/premium/plus-features.ts, src/lib/premium/paywall-copy.ts, src/lib/premium/products.ts, src/lib/planning/snart.ts, src/lib/planning/planning-interaction.ts, src/state/location-pref-store.ts, src/state/children-store.tsx, src/components/planning/SnartPlan.tsx, src/screens/UkeScreen.tsx</read_first>
  <behavior>
    - Loading/denied projection invokes neither climate lookup nor model builder and clears any cached paid result.
    - Stable allowed fixed-home/profile/window/ID-set input invokes dependencies once and memoizes the exact result.
    - Changing only ephemeral automatic/display place cannot change the boundary key, model input, profile, result or trace.
    - Changing child/fixed home/profile/window resets IDs and re-evaluates; downgrade clears output and later re-allow cannot reuse it.
    - Mark-all with no visible not-yet produces model empty; mark-all with visible not-yet produces model ready containing only not-yet.
  </behavior>
  <action>Create framework-free `projectSnartSession`, `snartSessionBoundaryKey` and `createSnartSessionEvaluator` with injected climate/model functions. Projection accepts the live access decision, active child's persisted fixed home, approved window/versions and immutable `alreadyHaveConceptIds`; no effective/automatic/display-place field is representable. Return before profile lookup/input construction for loading or denial, and purge cached paid data. Wire Uke to the seam with a session-only immutable ID set that resets on unmount or boundary change. Each mark re-evaluates through the model; never synthesize empty. Add the restrained Snart view choice and one contextual purchase action exactly `Se hva {barn} kan trenge snart`; loading stays neutral, Free contains no result, paywall focus returns to the trigger, and product copy names only the implemented four-to-six-week outcome. Preserve typed one-shot `requestedPlanView/requestToken` inputs for Plan 01-17 and consume each token at most once through the same access-first path. Do not enable `soon_preparation`, mutate prices/trials/RevenueCat, persist IDs, or derive Snart from `resolveEffectivePlace`, automatic coordinates, visible Uke city, `useWeather` coordinates or planned DTO place.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/lib/planning/__tests__/snart-session.test.ts src/lib/premium/__tests__/gating.test.ts src/lib/premium/__tests__/products.test.ts src/lib/premium/__tests__/paywall-copy.test.ts src/lib/planning/__tests__/planning-interaction.test.ts src/components/planning/__tests__/SnartPlan.test.tsx && npx tsc -b --pretty false"</automated></verify>
  <done>Snart evaluates only after allowed access against the fixed-home boundary, has correct model-owned mark-all behavior, and remains unavailable until the next plan flips the capability.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-15-01 | Information Disclosure | Free/loading Snart session | high | mitigate | Access-first projection and injected spy proof before payload construction. |
| T-01-15-02 | Tampering | Component state | high | mitigate | Model owns ready/empty/unavailable; component performs no filtering. |
| T-01-15-03 | Information Disclosure | Already-have IDs | high | mitigate | Immutable in-memory set only; reset boundaries; no storage/log/analytics. |
| T-01-15-04 | Spoofing | Fixed-home evidence | high | mitigate | Automatic/display place is absent from the session projection and key. |
</threat_model>

<verification>
- Model/component/session/access/paywall interaction suites and typecheck/lint pass.
- Source scans show no Snart evaluation before access, no automatic-place input, no persistence and no capability flip.
- No media is captured.
</verification>

<success_criteria>
- Component presentation is exhaustive, accessible and model-authoritative.
- Free/loading receives no computed or cached advice.
- Fixed-home input and session-only privacy boundaries are deterministic.
</success_criteria>

<output>
Create `01-15-SUMMARY.md` with governing Plan 01-14 hashes, component state matrix, S05/S18 mark-all evidence, injected-spy access-before-evaluation proof, fixed-home/automatic-place invariance, one-shot request contract and remaining capability/browser/media gates.
</output>
