---
phase: 01-planlegg-dagslinjen
plan: "05"
type: execute
wave: 5
package_wave: 2
depends_on: ["01-04"]
files_modified:
  - src/lib/planning/planned-outfit-resolver.ts
  - src/lib/planning/__tests__/planned-outfit-resolver.test.ts
  - src/App.tsx
  - src/screens/PaakledningScreen.tsx
autonomous: true
requirements: [CTXT-01, ACCESS-01, EVID-02, GOV-04]
must_haves:
  truths:
    - "A pure resolver accepts an event ID plus the current canonical event set/context map and returns a DTO only when membership, planningEventId and transitionContextId all match; every mismatch fails closed."
    - "The rail-facing contract remains ID-only; this plan adds no new live Dagslinje Outfit action and does not attempt to bridge a legacy row to canonical context."
    - "App/Outfit support a discriminated planned drill whose already validated DTO and focus origin remain in React memory; the live trusted UkeScreen-to-App callback is wired only in 01-06."
    - "Outfit renders all planned dimensions from the planned drill DTO and restores focus through the App state router on close."
  artifacts:
    - path: "src/lib/planning/planned-outfit-resolver.ts"
      provides: "Pure exact-ID/context resolver"
    - path: "src/App.tsx"
      provides: "Transient planned drill state and focus return"
    - path: "src/screens/PaakledningScreen.tsx"
      provides: "Authoritative planned-context branch"
  key_links:
    - from: "src/lib/planning/planned-outfit-resolver.ts"
      to: "src/lib/planning/change-events.ts"
      via: "pure resolver checks current event membership and exact identifiers"
      pattern: "resolvePlannedOutfitContext"
    - from: "src/App.tsx"
      to: "src/screens/PaakledningScreen.tsx"
      via: "plannedContext prop in transient drill state"
      pattern: "plannedContext"
  prohibitions:
    - "MUST NOT construct context inside a click handler, persist it, serialize it to URL, log/track it, or reuse transitionContextId as plannedContextId."
    - "MUST NOT perform a current-data fallback for a missing map key or recompute a planned recommendation at the destination."
    - "MUST NOT add a live rail opener, canonicalize a legacy row, or pass DTO/context identifiers through PlanChangeRail in this preparatory candidate."
    - "MUST NOT broaden access, change recommendations/pricing/RevenueCat/packages/APIs/schemas or capture media."
    - "MUST NOT claim or introduce the deferred Home-to-Outfit garment-node transfer, Antrekkskart connector system, or any other signature experience governed by the UX & Motion Bible."
---

<objective>
Prepare the fail-closed exact-context resolver and trusted App/Outfit transient boundary.

Purpose: Establish the trusted boundary without inventing a canonical bridge from the legacy rail before the atomic 01-06 migration.
Output: Pure resolver/tests plus a discriminated transient App/Outfit planned branch; no Uke/rail live action yet.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-planlegg-dagslinjen/01-04-SUMMARY.md
@.planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md
@docs/BABYORA-UX-MOTION-BIBLE.md
@src/lib/planning/planned-outfit-context.ts
@src/App.tsx
@src/screens/PaakledningScreen.tsx
@src/screens/HjemScreen.tsx
</context>

<delivery_contract>
- Risk/model: high exact-context/privacy/navigation risk; Claude Fable 5, Extra effort.
- Candidate commit: `fix: preserve exact planned outfit context`.
- Reviewer: fresh Fable 5 Extra or approved Opus plus external read-only review on immutable SHA.
- Evidence boundary: focused pure/context/recommendation tests and build only; the live exact-context browser case begins atomically in 01-06.
</delivery_contract>

<tasks>

<task type="auto">
  <name>Task 1: Add the pure fail-closed exact-context resolver</name>
  <files>src/lib/planning/planned-outfit-resolver.ts, src/lib/planning/__tests__/planned-outfit-resolver.test.ts</files>
  <read_first>src/lib/planning/planned-outfit-context.ts, src/lib/planning/__tests__/planned-outfit-context.test.ts, src/lib/planning/change-events.ts, src/lib/planning/rail-rows.ts</read_first>
  <behavior>
    - Exact current-event membership plus matching planningEventId and transitionContextId returns the validated immutable DTO.
    - Missing ID/map hit, invalid DTO, stale event membership, planning-event mismatch or transition mismatch returns null with no fallback.
    - The resolver performs no recommendation, time, weather, storage, navigation or logging work.
  </behavior>
  <action>Create pure `resolvePlannedOutfitContext(eventId, currentEvents, contexts)` around the validated DTO contract from 01-04. Require the requested ID to identify a member of `currentEvents`, require an own map hit that passes `isPlannedOutfitContext`, then require exact `planningEventId` and `transitionContextId` equality with that member. Return `null` for every mismatch. Add table tests for every guard plus a same-hour/different-date collision and distinct planned/transition IDs. Do not import React, construct a DTO, call the recommendation engine or choose a first/current fallback inside the resolver.</action>
  <verify><automated>npx vitest run src/lib/planning/__tests__/planned-outfit-resolver.test.ts src/lib/planning/__tests__/planned-outfit-context.test.ts</automated></verify>
  <acceptance_criteria>
    - Pure tests reject all named mismatch classes and prove the exact hit.
    - Resolver has no UI, current-weather, current-time, storage, analytics or recommendation dependency.
    - No legacy/canonical projection is added or changed.
  </acceptance_criteria>
  <done>The future-context trust decision is a deterministic fail-closed pure function ready for the atomic 01-06 callsite.</done>
</task>

<task type="auto">
  <name>Task 2: Prepare the transient App/Outfit planned branch</name>
  <files>src/App.tsx, src/screens/PaakledningScreen.tsx</files>
  <read_first>src/App.tsx, src/screens/PaakledningScreen.tsx, src/screens/HjemScreen.tsx, src/lib/planning/planned-outfit-context.ts, src/hooks/useWeather.ts</read_first>
  <action>Make App's paakledning drill a discriminated union so ordinary Hjem and planned contexts cannot coexist ambiguously. The planned variant carries an already validated `PlannedOutfitContext` plus originating HTMLElement in React memory only; 01-06 will create and wire the sole trusted UkeScreen-to-App `onOpenPlannedOutfit(dto, trigger)` callback. Pass the planned variant to PaakledningScreen and give the Outfit title a programmatic focus target. When planned context exists, derive date/time/place, activity/vogn, weather, cause, canvas axis and finalized recommendation exclusively from it; current `useWeather`, active child and current time remain only in the ordinary Hjem path. On close/cancel, clear the drill through the existing App state router and restore the connected origin or app main—do not add browser History behavior. Do not modify UkeScreen or expose a live Planlegg action in this plan.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/lib/planning/__tests__/planned-outfit-resolver.test.ts src/lib/planning/__tests__/planned-outfit-context.test.ts src/lib/recommendation/__tests__ && npm run build"</automated></verify>
  <acceptance_criteria>
    - App and Paakledning compile with the discriminated planned branch while Uke/legacy rail expose no new Outfit action.
    - Planned branch contains no current weather/time recomputation.
    - App uses no storage/URL/analytics/logger for the DTO.
    - Hjem current Outfit behavior remains intact.
    - No component other than Paakledning receives the DTO in this preparatory plan; no rail prop receives a DTO or context identifier.
  </acceptance_criteria>
  <done>The authoritative App/Outfit planned branch is ready for 01-06's sole trusted screen callback without a premature legacy-rail bridge.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat | Mitigation / disposition |
|---|---|
| Stale event opens wrong advice | Pure current-membership and identifier resolver; live wiring waits for atomic canonical migration. |
| Current network data overrides selection | Planned branch consumes the trusted DTO exclusively. |
| Sensitive DTO leaks | React-memory-only state and no payload logging/storage/URL. |
| Focus reference disconnects | isConnected check then app-main fallback. |
</threat_model>

<artifacts_this_phase_produces>
- Pure fail-closed resolver plus discriminated transient App/Outfit planned drill state; the trusted `onOpenPlannedOutfit(dto, trigger)` screen boundary is wired in 01-06.
- No live rail action, package/API/schema/media artifact.
</artifacts_this_phase_produces>

<verification>
- Context/recommendation tests and build pass; live exact-context E2E is owned by 01-06.
- Static privacy scan and fresh high-risk immutable-SHA review pass independently.
</verification>

<rollback>
Revert `fix: preserve exact planned outfit context`; no live rail action depends on this preparatory candidate.
</rollback>

<success_criteria>
- Exact trusted context can be preserved across Uke/App/Outfit once 01-06 supplies the canonical map.
- Pure stale/missing/mismatched lookup refuses rather than recomputes.
- Exact-SHA high-risk PASS exists; media/device/release stay Pending.
</success_criteria>

<output>
Create `01-05-SUMMARY.md` with resolver guard matrix, trusted-boundary invariant, proof that no live rail action was added, exact candidate SHA, each reviewer identity/lane, privacy and high-risk PASS/BLOCK verdicts bound to that SHA, and all remaining Pending gates.
</output>
