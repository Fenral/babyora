---
phase: 01-planlegg-dagslinjen
plan: "04"
type: tdd
wave: 4
package_wave: 2
depends_on: ["01-03"]
files_modified:
  - src/lib/planning/planned-outfit-context.ts
  - src/lib/planning/__tests__/planned-outfit-context.test.ts
autonomous: true
requirements: [CTXT-01, GOV-04]
must_haves:
  truths:
    - "PlannedOutfitContext is a recursively readonly cloned snapshot with child/age, exact ISO/timezone, place label/coordinates/source, situation, weather, finalized recommendation, planning event and access decision."
    - "`plannedContextId` identifies the DTO; `planningEventId` binds it to the event; the separate `transitionContextId` records the forecast transition identity; no generic `contextId` exists."
    - "Invalid/partial input is rejected and never defaults to current time/weather/place/recommendation."
    - "The module is pure and never persists, logs, tracks, serializes to URL or sends the payload."
  artifacts:
    - path: "src/lib/planning/planned-outfit-context.ts"
      provides: "Validated transient exact-context boundary"
      exports: ["PLAN_TIME_ZONE", "PlannedOutfitContext", "createPlannedOutfitContext", "isPlannedOutfitContext"]
  key_links:
    - from: "src/lib/planning/planned-outfit-context.ts"
      to: "src/lib/planning/change-events.ts"
      via: "planningEventId references a stable event while identifiers remain distinct"
      pattern: "planningEventId"
  prohibitions:
    - "MUST NOT use localStorage, sessionStorage, IndexedDB, URL/query, analytics, console or network for the payload."
    - "MUST NOT expose a generic `contextId`, copy `transitionContextId` into `plannedContextId`, or accept partial/defaulted data."
    - "MUST NOT change recommendations, access semantics, packages, APIs, schemas, pricing, RevenueCat or media."
---

<objective>
Create the pure immutable planned-Outfit DTO boundary.

Purpose: Prevent a selected future event from becoming a current-context recommendation.
Output: Strict constructor/type guard and adversarial tests; no UI wiring.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/01-planlegg-dagslinjen/01-03-SUMMARY.md
@.planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md
@src/lib/planning/change-events.ts
@src/lib/access/capabilities.ts
@src/lib/wool-layers/types.ts
@src/lib/planning/__tests__/planlegg-fixtures.ts
</context>

<delivery_contract>
- Risk/model: high exact-context/privacy risk; Claude Fable 5, Extra effort.
- Candidate commit: `feat: validate transient planned outfit context`.
- Reviewer: fresh Fable 5 Extra or approved Opus high-risk verifier plus read-only external review on exact SHA.
- Evidence boundary: unit/source privacy/full static checks; wiring belongs to 01-05; no media.
</delivery_contract>

<tasks>

<task type="auto">
  <name>Task 1: Establish the expected missing-contract RED safely</name>
  <files>src/lib/planning/__tests__/planned-outfit-context.test.ts</files>
  <read_first>src/lib/planning/__tests__/planned-outfit-context.test.ts, src/lib/planning/__tests__/planlegg-fixtures.ts, src/lib/planning/change-events.ts, src/lib/access/capabilities.ts, src/lib/wool-layers/types.ts</read_first>
  <behavior>
    - A verbose test named `RED_PLANNED_CONTEXT_CONTRACT` dynamically imports the future module; only the expected missing module is converted to exact sentinel `MISSING_PLANNED_OUTFIT_CONTEXT_CONTRACT`.
    - Complete fixture, invalid IDs/ISO/timezone/numbers/coordinates/place-source/situation/recommendation/access, immutability, stable identity and source-privacy assertions are real tests, not TODOs.
  </behavior>
  <action>Replace the TODO. Keep the dynamic import inside the named test so collection succeeds before the module exists: assign the exact relative module path to a string variable and call `import(/* @vite-ignore */ modulePath)`, preventing Vite from resolving the missing literal during collection. Catch only module-not-found for that exact path and throw the exact sentinel. Any syntax/fixture/other failure must retain its original output. Run the single-quoted PowerShell command below: it must observe nonzero and both the named test and sentinel, otherwise fail the RED gate.</action>
  <verify><automated>powershell -NoProfile -Command '$out = (& npx vitest run src/lib/planning/__tests__/planned-outfit-context.test.ts --reporter=verbose 2>&1 | Out-String); $code = $LASTEXITCODE; if ($code -eq 0) { throw "Expected RED" }; if ($out -notmatch "RED_PLANNED_CONTEXT_CONTRACT" -or $out -notmatch "MISSING_PLANNED_OUTFIT_CONTEXT_CONTRACT") { $out; throw "Unexpected RED signature" }'</automated></verify>
  <acceptance_criteria>
    - Command cannot pass on syntax/fixture/unrelated failures.
    - Output contains the exact named test and missing-contract sentinel.
    - TODO is removed before production source is added.
  </acceptance_criteria>
  <done>The planned DTO contract is executable RED for the intended missing implementation only.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the immutable non-persistent DTO</name>
  <files>src/lib/planning/planned-outfit-context.ts, src/lib/planning/__tests__/planned-outfit-context.test.ts</files>
  <read_first>src/lib/planning/__tests__/planned-outfit-context.test.ts, src/lib/planning/change-events.ts, src/lib/access/capabilities.ts, src/lib/wool-layers/types.ts, src/lib/clothing-engine-v2/validation.ts, src/state/child-profile.ts</read_first>
  <behavior>
    - Complete canonical input returns a deeply frozen clone with stable `plannedContextId`, separate `planningEventId` and separate `transitionContextId`.
    - Any invalid field throws a clear validation error; post-construction input mutation cannot alter output.
    - Same canonical input is byte-identical; changing any planned dimension, including place source, changes plannedContextId.
  </behavior>
  <action>Define `PLAN_TIME_ZONE='Europe/Oslo'`, readonly input/output, constructor and type guard. Follow the repository boundary split in `src/lib/clothing-engine-v2/validation.ts` and tolerant profile guards in `src/state/child-profile.ts`: the constructor validates strictly and throws a clear error for malformed creation input, while `isPlannedOutfitContext(unknown)` is a non-throwing tolerant guard that returns false for every unknown/partial/malformed value. Fields: schemaVersion, plannedContextId, planningEventId, transitionContextId, child id/name/ageMonths, plannedForIso/timeZone, place label/lat/lon/source, activity/vognMode, weather snapshot, finalized recommendation and evaluated access capability/allowed/reason. Validate source against the closed union `configured-place | fixed-home | automatic`: Plan 01-06 uses `configured-place` before the privacy-safe location cutover, while Plan 01-12 supplies the resolved `fixed-home | automatic` source. Validate, copy known keys into plain objects, canonicalize, derive plannedContextId, recursively freeze. The event and transition identifiers remain caller-supplied, validated and semantically distinct from the derived DTO identifier. This DTO is consumed only by the fail-closed resolver introduced in Plan 01-05 and then the trusted App boundary; no leaf component or click handler may consume or transport it directly. Remove the temporary missing-contract catch/sentinel while retaining every behavior test. Import no storage/network/analytics/logging module.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/lib/planning/__tests__/planned-outfit-context.test.ts && npx tsc -b --pretty false"</automated></verify>
  <acceptance_criteria>
    - Tests prove field completeness, coordinate boundaries, all three valid place-source values, invalid-source rejection, Unicode, immutability and identity changes when source alone changes.
    - Constructor throws on malformed creation input; `isPlannedOutfitContext` never throws and returns false for unknown/partial/malformed values.
    - Source contains no generic `contextId`, identifier aliasing, storage, URL, track, console or fetch call; `transitionContextId` is a separate validated field.
    - No current-data fallback exists.
  </acceptance_criteria>
  <done>A deterministic validated transient DTO is ready for exact wiring.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat | Mitigation / disposition |
|---|---|
| Partial/tampered DTO changes advice | Strict complete validation; no defaults. |
| Child/location/recommendation leaks | Pure module and executable source scan. |
| Caller mutation changes destination | Known-key clone plus recursive freeze. |
| Cause ID is confused with DTO ID | Three explicit, separately validated transitionContextId/plannedContextId/planningEventId contracts. |
</threat_model>

<artifacts_this_phase_produces>
- New planned context module and executable adversarial test suite.
- New DTO identifier `plannedContextId`; no persistence/API/schema/media artifact.
</artifacts_this_phase_produces>

<verification>
- Focused context suite, TypeScript, full test/lint/build and privacy scan pass.
- Fresh high-risk reviewer verifies the immutable SHA; executor cannot PASS it.
</verification>

<rollback>
Revert `feat: validate transient planned outfit context`; no data cleanup is required.
</rollback>

<success_criteria>
- Exact context is complete, immutable, deterministic and transient.
- Expected RED/green evidence is unambiguous.
- High-risk PASS is exact-SHA bound; media/device/release stay Pending.
</success_criteria>

<output>
Create `01-04-SUMMARY.md` with RED signature, rejected-field matrix, exact candidate SHA, reviewer identity/lane, privacy PASS/BLOCK verdict bound to that SHA, and all remaining Pending gates.
</output>
