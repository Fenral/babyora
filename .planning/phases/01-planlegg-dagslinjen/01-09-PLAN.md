---
phase: 01-planlegg-dagslinjen
plan: "09"
type: tdd
wave: 9
package_wave: 5
depends_on: ["01-08"]
files_modified:
  - src/lib/premium/use-access.ts
  - src/lib/premium/__tests__/use-access.test.ts
  - src/main.tsx
autonomous: true
requirements: [ACCESS-01, EVID-02, GOV-04]
must_haves:
  truths:
    - "On configured native startup and every resume, cached Plus becomes neutral synchronously before RevenueCat I/O; `useAccess()` exposes `isPremium:false, loading:true` until a fresh result settles."
    - "Concurrent startup/resume refresh requests share one in-flight promise and publish one result; a later resume after settlement starts a new refresh."
    - "Fresh false, refresh error and stale completion fail closed and never retain or reopen paid content."
    - "Web/dev without configured native RevenueCat preserves the explicit mock contract and does not mislabel cached state as a fresh store result."
  artifacts:
    - path: "src/lib/premium/use-access.ts"
      provides: "Fail-closed single-flight entitlement freshness boundary"
      exports: ["syncPremiumEntitlement", "useAccess"]
    - path: "src/lib/premium/__tests__/use-access.test.ts"
      provides: "Node-testable startup/resume/concurrency/error matrix"
  key_links:
    - from: "src/main.tsx"
      to: "src/lib/premium/use-access.ts"
      via: "startup begins the same single-flight freshness cycle used by resume"
      pattern: "syncPremiumEntitlement"
    - from: "src/lib/premium/use-access.ts"
      to: "src/lib/billing/revenuecat.ts"
      via: "one injected/fresh check publishes the effective access snapshot"
      pattern: "checkPremium"
  prohibitions:
    - "MUST NOT edit presentation/paywall/location, pricing, products, purchase/restore, RevenueCat semantics, packages, APIs, schemas or media."
    - "MUST NOT optimistically expose a persisted Plus value while configured-native freshness is unresolved or let concurrent/stale refreshes publish twice."
---

<objective>
Make live entitlement freshness fail closed before any Planlegg presentation consumes it.

Purpose: Prevent cached Plus from flashing or retaining paid content during configured-native startup/resume checks.
Output: Single-flight freshness state, hook wiring, startup integration and deterministic Node tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/01-planlegg-dagslinjen/01-08-SUMMARY.md
@src/lib/premium/use-access.ts
@src/lib/billing/revenuecat.ts
@src/state/subscription-store.ts
@src/main.tsx
</context>

<delivery_contract>
- Risk/model: high entitlement freshness/concurrency risk; Claude Fable 5, Extra effort.
- Candidate commit: `fix: fail closed during entitlement refresh`.
- Reviewer: fresh Fable 5 Extra or approved high-risk verifier plus read-only external review on exact SHA.
- Evidence boundary: Node/unit/type/build evidence only; no purchase sandbox, location, browser media or physical-device claim.
</delivery_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the single-flight freshness contract in RED</name>
  <files>src/lib/premium/__tests__/use-access.test.ts</files>
  <read_first>src/lib/premium/use-access.ts, src/lib/billing/revenuecat.ts, src/state/subscription-store.ts, src/main.tsx, package.json</read_first>
  <behavior>
    - Cached Plus on configured native enters `{isPremium:false, loading:true}` synchronously before the check promise resolves, then publishes fresh true or false exactly once.
    - Two concurrent startup/resume calls share one check and one promise; a resume after settlement starts a new check.
    - Error and superseded completion fail closed; stale results cannot overwrite a newer generation.
    - Web/dev unconfigured mode preserves the existing explicit mock behavior without a native freshness claim.
  </behavior>
  <action>Create Node Vitest tests around an injected/test-only controller seam exported from `use-access.ts`; mock Capacitor/RevenueCat before module import and use no jsdom/testing-library. Add exact RED signature `MISSING_SINGLE_FLIGHT_ENTITLEMENT_FRESHNESS`, require the expected contract failure rather than syntax/import noise, and cover subscriber publication cardinality plus promise identity.</action>
  <verify><automated>powershell -NoProfile -Command '$out = (& npx vitest run src/lib/premium/__tests__/use-access.test.ts --reporter=verbose 2>&1 | Out-String); $code = $LASTEXITCODE; if ($code -eq 0) { throw "Expected RED" }; if ($out -notmatch "MISSING_SINGLE_FLIGHT_ENTITLEMENT_FRESHNESS") { $out; throw "Unexpected RED signature" }'</automated></verify>
  <done>The old optimistic/call-per-event behavior fails for the precise freshness contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement and wire fail-closed startup/resume freshness</name>
  <files>src/lib/premium/use-access.ts, src/lib/premium/__tests__/use-access.test.ts, src/main.tsx</files>
  <read_first>src/lib/premium/__tests__/use-access.test.ts, src/lib/premium/use-access.ts, src/lib/billing/revenuecat.ts, src/state/subscription-store.ts, src/main.tsx</read_first>
  <behavior>
    - Configured-native refresh begins with a synchronous neutral publication, shares an in-flight promise, and commits only the latest fresh result.
    - Fresh false/error becomes fail-closed non-premium; no cached paid value survives.
    - Startup and the one registered resume listener call the same controller; repeated module/render registration cannot add listeners.
  </behavior>
  <action>Refactor `syncPremiumEntitlement` around one injected/testable module-level controller with explicit generation, current in-flight promise and subscriber state. On configured native, publish refreshing synchronously before `checkPremium`; while refreshing, `useAccess()` returns exactly `isPremium:false, loading:true` regardless of the persisted store. Deduplicate concurrent calls, ignore superseded completions, publish one fresh Boolean, and fail closed on error without restoring cached Plus. A later call after settlement starts a new generation. Keep exactly one native/web resume registration. For web/dev without configured native RevenueCat, preserve the existing mock value through an explicit ready-dev branch. Update main startup to use this same call path after RevenueCat initialization; add no second listener or optimistic read.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/lib/premium/__tests__/use-access.test.ts src/lib/access/__tests__/capabilities.test.ts && npx tsc -b --pretty false && npm run build"</automated></verify>
  <done>Every configured-native consumer sees neutral access until one fresh single-flight result becomes authoritative.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat | Mitigation / disposition |
|---|---|
| Persisted Plus flashes paid content | Synchronous neutral effective state before I/O. |
| Resume storms duplicate billing checks | Shared in-flight promise and single listener. |
| Stale result reopens paid access | Generation guard and fail-closed publication. |
| Test/dev loses mock workflow | Explicit unconfigured web/dev branch remains separate. |
</threat_model>

<verification>
- Freshness/concurrency/access tests, typecheck and build pass.
- Fresh high-risk reviewer PASS binds to the immutable entitlement candidate SHA.
</verification>

<rollback>
Revert `fix: fail closed during entitlement refresh`; dependent presentation/location capabilities remain disabled until a reviewed replacement exists.
</rollback>

<success_criteria>
- Cached configured-native Plus is never current authority during startup/resume refresh.
- Concurrent/stale/error paths fail closed deterministically.
- No presentation/location/media scope changed.
</success_criteria>

<output>
Create `01-09-SUMMARY.md` with freshness state table, concurrency/publication evidence, exact candidate SHA, reviewer identity/lane, PASS/BLOCK verdict bound to that SHA, and remaining Pending gates.
</output>
