---
phase: 01-planlegg-dagslinjen
plan: "14"
type: tdd
wave: 14
package_wave: 5
depends_on: ["01-13"]
files_modified:
  - .planning/phases/01-planlegg-dagslinjen/01-SNART-RULES.md
  - .planning/phases/01-planlegg-dagslinjen/01-SNART-CLIMATE-PROVENANCE.md
  - src/data/snart-climate-1991-2020-v1.json
  - scripts/validate-snart-climate.mjs
  - src/lib/planning/snart-climate.ts
  - src/lib/planning/snart.ts
  - src/lib/planning/__tests__/snart-climate.test.ts
  - src/lib/planning/__tests__/snart.test.ts
autonomous: true
requirements: [ACCESS-01, TRUTH-01, GOV-04, GOV-05, GOV-06]
must_haves:
  truths:
    - "Execution is unreachable until Plan 01-13 records all six approvals, approved-candidate rules metadata and matching climate artifact hashes."
    - "The offline climate decoder fails closed and never substitutes a nearby profile, current forecast, network result or generated value."
    - "The pure model returns exactly `ready | empty | unavailable`; invalid evidence is unavailable and contains no garment advice."
    - "Marking every actionable item returns `empty` only when no visible `not_yet` remains; otherwise it returns `ready` with only `not_yet`, and the UI never has to synthesize either state."
    - "Every visible result is deterministic, versioned and traceable to approved rule, climate and evidence identifiers."
  artifacts:
    - path: "scripts/validate-snart-climate.mjs"
      provides: "Deterministic integrity/provenance validator for the approved pack"
    - path: "src/lib/planning/snart-climate.ts"
      provides: "Strict decoder and exact fixed-home climate-profile lookup"
    - path: "src/lib/planning/snart.ts"
      provides: "Pure approved three-state Snart model"
      exports: ["buildSnartPlan"]
  key_links:
    - from: "src/lib/planning/snart.ts"
      to: "src/lib/planning/snart-climate.ts"
      via: "validated approved profile is the only long-horizon weather evidence"
      pattern: "snart-climate-1991-2020-v1"
    - from: "src/lib/planning/snart.ts"
      to: ".planning/phases/01-planlegg-dagslinjen/01-SNART-RULES.md"
      via: "approved rule IDs, group semantics and copy registry"
      pattern: "S18-owned-actionable-with-not-yet"
---

<objective>
Implement and independently freeze the approved offline Snart evidence/model candidate, while keeping the feature unavailable to users.

Purpose: Convert approved four-to-six-week guidance into a deterministic, fail-closed and traceable pure model before any UI or capability can expose it.
Output: Validated offline climate pack, strict decoder, pure three-state model, adversarial tests and an immutable independently reviewed model candidate.
</objective>

<execution_context>
@C:/Users/siver/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/siver/.codex/gsd-core/templates/summary.md
@C:/Users/siver/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-planlegg-dagslinjen/01-13-SUMMARY.md
@.planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md
@.planning/phases/01-planlegg-dagslinjen/01-SNART-RULES.md
@.planning/phases/01-planlegg-dagslinjen/01-SNART-CLIMATE-PROVENANCE.md
@src/lib/premium/plus-features.ts
@src/state/children-store.tsx
@src/state/location-pref-store.ts
</context>

<delivery_contract>
- Risk/model: high advice/evidence/privacy risk; implementation and data review are independent from the executor.
- Entry gate: exact `snart-v1.0-approved` / `approved_for_model_candidate`, six dated approvals, one Pending `Implementasjon` row, matching approved hashes and green independent validator.
- Candidate commit: `feat: add approved Snart evidence model`.
- Capability, UI, route, paywall and media remain untouched; `soon_preparation` remains false.
</delivery_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate and decode only the approved offline climate pack</name>
  <files>.planning/phases/01-planlegg-dagslinjen/01-SNART-CLIMATE-PROVENANCE.md, src/data/snart-climate-1991-2020-v1.json, scripts/validate-snart-climate.mjs, src/lib/planning/snart-climate.ts, src/lib/planning/__tests__/snart-climate.test.ts</files>
  <read_first>.planning/phases/01-planlegg-dagslinjen/01-13-SUMMARY.md, .planning/phases/01-planlegg-dagslinjen/01-SNART-RULES.md, .planning/phases/01-planlegg-dagslinjen/01-SNART-CLIMATE-PROVENANCE.md, scripts/validate-snart-climate.mjs, src/data/snart-climate-1991-2020-v1.json, src/state/location-pref-store.ts</read_first>
  <behavior>
    - Reject wrong version or normal period, missing license/source/method/hash, duplicate or missing local calendar days, invalid percentile ordering, probability outside 0..1, insufficient coverage and unsupported home profiles.
    - Return a deeply readonly exact profile or explicit unavailable result; never select a nearby profile silently.
    - Require the pack, provenance and approved evidence hashes and declared 1991–2020 period to agree exactly.
  </behavior>
  <action>Revalidate Plan 01-13's approval and hash gate before touching source. Commit the independently supplied pack, provenance and validator byte-for-byte; a difference blocks rather than being repaired by the executor. Write failing decoder/lookup tests, then implement only a strict pure decoder and exact `homePlaceKey` lookup. Reject unknown/malformed fields rather than coercing. Do not add runtime network access, a download step, forecast fallback, fuzzy location match or persistent child/location history.</action>
  <verify><automated>cmd.exe /d /s /c "node scripts/validate-snart-climate.mjs src/data/snart-climate-1991-2020-v1.json .planning/phases/01-planlegg-dagslinjen/01-SNART-CLIMATE-PROVENANCE.md && npx vitest run src/lib/planning/__tests__/snart-climate.test.ts"</automated></verify>
  <done>The runtime can consume only the exact independently approved, versioned and validated fixed-home climate profile.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the traceable three-state Snart model</name>
  <files>src/lib/planning/snart.ts, src/lib/planning/__tests__/snart.test.ts</files>
  <read_first>.planning/phases/01-planlegg-dagslinjen/01-SNART-RULES.md, .planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md, src/lib/planning/snart-climate.ts, src/lib/planning/__tests__/snart-climate.test.ts, src/lib/planning/__tests__/snart.test.ts</read_first>
  <behavior>
    - D0 is Europe/Oslo and the target window is 15 unique local dates D+28 through D+42 across DST.
    - Missing/invalid/stale evidence, unsupported home/profile, invalid input or unsupported 0–24-month age yields unavailable with no item/copy payload.
    - S05: after valid evidence, deduplication and `alreadyHaveConceptIds`, no visible item in any group yields empty.
    - S18: when all actionable winners are marked but a winning `not_yet` remains, return ready with only `not_yet`; it has no mark action.
    - A concept whose winning actionable result was marked cannot resurface via a lower-priority `not_yet`; a winning `not_yet` is never filtered by `alreadyHaveConceptIds`.
    - Every ready item carries ruleset, rule, concept, group, window, climate, evidence, uncertainty and optional size-note trace fields.
  </behavior>
  <action>Write failing tests for all approved S01–S18 fixtures and boundary/property invariants before implementation. Implement pure `buildSnartPlan` from the approved registry and copy only. Use local-calendar operations rather than millisecond-day arithmetic. Keep groups `must_have | nice_to_have | not_yet`, with visible wording delegated to the approved copy. Use birth date only to validate the approved age window, never to infer size or milestones; optional current size remains an opaque probabilistic note input. Apply approved priority and deduplication before session-only already-have filtering. Import no current forecast, network, storage, analytics, catalog mutation or generative text.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/lib/planning/__tests__/snart-climate.test.ts src/lib/planning/__tests__/snart.test.ts && npx tsc -b --pretty false"</automated></verify>
  <done>The pure model deterministically implements all approved states and corrected mark-all/not-yet semantics with complete traceability.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze and independently verify the immutable model candidate</name>
  <files>.planning/phases/01-planlegg-dagslinjen/01-SNART-RULES.md, .planning/phases/01-planlegg-dagslinjen/01-SNART-CLIMATE-PROVENANCE.md, src/data/snart-climate-1991-2020-v1.json, scripts/validate-snart-climate.mjs, src/lib/planning/snart-climate.ts, src/lib/planning/snart.ts, src/lib/planning/__tests__/snart-climate.test.ts, src/lib/planning/__tests__/snart.test.ts</files>
  <read_first>.planning/phases/01-planlegg-dagslinjen/01-13-SUMMARY.md, .planning/phases/01-planlegg-dagslinjen/01-SNART-RULES.md, .planning/phases/01-planlegg-dagslinjen/01-SNART-CLIMATE-PROVENANCE.md, src/lib/planning/snart-climate.ts, src/lib/planning/snart.ts, src/lib/premium/plus-features.ts</read_first>
  <action>Run the validator, focused tests, full test/lint/build and source scans. Freeze the exact candidate SHA and hashes for rules, pack, provenance, validator, decoder, model and tests. Obtain a fresh named data PASS and separate high-risk implementation PASS bound to the same immutable candidate; the executor cannot issue either verdict. Any code/data/rule/copy edit invalidates both. After both PASS verdicts, update only the seventh `Implementasjon` journal row to Approved with reviewer/date/SHA/evidence and record a separate documentation evidence commit; prove the reviewed implementation bytes did not change. Keep `soon_preparation` false and add no UI, route, paywall, network, persistence or media.</action>
  <verify><automated>cmd.exe /d /s /c "node scripts/validate-snart-climate.mjs src/data/snart-climate-1991-2020-v1.json .planning/phases/01-planlegg-dagslinjen/01-SNART-CLIMATE-PROVENANCE.md && npm test && npm run lint && npm run build"</automated></verify>
  <done>The exact evidence/model candidate has independent data and implementation PASS, the implementation approval records that SHA without changing reviewed bytes, and Snart remains disabled.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01-14-01 | Tampering | Climate pack/model | high | mitigate | Approved hashes, strict validator and immutable exact-SHA review. |
| T-01-14-02 | Information Disclosure | Child/location inputs | high | mitigate | Pure ephemeral inputs; no storage, analytics or network. |
| T-01-14-03 | Repudiation | Advice provenance | high | mitigate | Every item carries rule/evidence/climate/version trace fields. |
| T-01-14-04 | Elevation of Privilege | Capability availability | medium | mitigate | Keep `soon_preparation` false; no UI or route in this plan. |
</threat_model>

<verification>
- Approved artifact validator, all S01–S18 model fixtures, typecheck, full tests, lint and build pass.
- Independent data and high-risk implementation verdicts bind to the immutable candidate SHA.
- Capability/UI/paywall/route/media remain absent or disabled.
</verification>

<success_criteria>
- Invalid evidence never becomes guidance or empty reassurance.
- Corrected mark-all/not-yet behavior is model-authoritative and tested.
- The exact reviewed candidate is immutable, traceable and still unreachable to users.
</success_criteria>

<output>
Create `01-14-SUMMARY.md` with all six approval references, approved rules metadata/hash, pack/provenance/validator hashes, S01–S18 results, immutable candidate SHA, independent data and implementation verdicts, evidence-only journal update proof, and all remaining UI/capability/media/device/release gates.
</output>
