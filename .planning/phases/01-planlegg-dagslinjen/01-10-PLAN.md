---
phase: 01-planlegg-dagslinjen
plan: "10"
type: execute
wave: 10
package_wave: 5
depends_on: ["01-09"]
files_modified:
  - src/lib/premium/paywall-copy.ts
  - src/lib/premium/__tests__/paywall-copy.test.ts
  - src/lib/premium/products.ts
  - src/lib/premium/__tests__/products.test.ts
  - src/components/paywall/PlusExpansionPreview.tsx
  - src/components/PaywallDialog.tsx
  - src/App.tsx
  - src/screens/UkeScreen.tsx
  - e2e/planlegg.ts
autonomous: true
requirements: [ACCESS-01, UI-02, GOV-04]
must_haves:
  truths:
    - "Free keeps the complete supported Today experience at the currently configured child place and receives exactly one truthful future weather comparison when valid evidence exists—otherwise a neutral unavailable state—without future garment advice or an Outfit action; fixed-home semantics are accepted only after the split location plans."
    - "Paywall claims are built from currently enabled capabilities; disabled family, automatic-location and Snart capabilities never produce `sammen`, caregiver, automatic-place or soon-preparation promises."
    - "Entitlement loading is neutral and does not flash Plus content, a paywall or a locked state."
    - "On configured native RevenueCat, startup/resume refresh is single-flight and fail-closed: cached Plus becomes neutral until a fresh live result resolves; no concurrent refresh or stale completion can reopen paid content."
    - "Closing a contextual paywall returns focus to its connected trigger and preserves the selected Planlegg view/event/scroll."
    - "Live entitlement loss invalidates an already-open paid planned Outfit at the App/drill boundary, clears its transient DTO without recomputation, and restores a valid denied/Today state and focus."
    - "The live `PAYWALL_COPY.trustLine = 'Én Plus — alle som passer barnet'`, legacy trust constants and every semantic family/caregiver variant are removed or derived only from an enabled family capability rather than restyled."
  artifacts:
    - path: "src/lib/premium/paywall-copy.ts"
      provides: "Capability-derived claim builder"
    - path: "src/screens/UkeScreen.tsx"
      provides: "Policy-consumed Today/Uke presentation"
    - path: "e2e/planlegg.ts"
      provides: "Deterministic access and focus case"
  key_links:
    - from: "src/lib/premium/paywall-copy.ts"
      to: "src/lib/access/capabilities.ts"
      via: "enabled capability flags are the only source of benefit claims"
      pattern: "buildCapabilityPaywallCopy"
    - from: "src/screens/UkeScreen.tsx"
      to: "src/lib/premium/gating.ts"
      via: "resolvePlanningViewAccess/decideAccess presentation decisions"
      pattern: "resolvePlanningViewAccess"
    - from: "src/App.tsx"
      to: "src/lib/access/capabilities.ts"
      via: "live future-plan authorization invalidates an open paid planned drill"
      pattern: "decideAccess"
  prohibitions:
    - "MUST NOT change product identifiers, prices, trials, purchase/restore behavior, RevenueCat, packages, APIs or schemas."
    - "MUST NOT claim family sharing, caregivers, automatic location, everywhere, Snart or any disabled capability."
    - "MUST NOT let a Free future comparison expose garment advice, exact planned context, a hidden Outfit link or hidden Plus DOM."
    - "MUST NOT add browser haptic call-count assertions or create screenshots/video."
---

<objective>
Present the already-centralized access policy honestly across Today, Uke and the paywall.

Purpose: Preserve a useful Free product while selling only capabilities the build actually contains.
Output: Capability-derived copy, contextual paywall presentation, truthful Free/Plus states and deterministic no-media evidence.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-planlegg-dagslinjen/01-09-SUMMARY.md
@.planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md
@src/lib/access/capabilities.ts
@src/lib/premium/gating.ts
@src/lib/premium/paywall-copy.ts
@src/lib/premium/products.ts
@src/lib/premium/use-access.ts
@src/components/PaywallDialog.tsx
@src/App.tsx
@src/screens/PaakledningScreen.tsx
@src/screens/UkeScreen.tsx
@e2e/planlegg.ts
</context>

<delivery_contract>
- Risk/model: high entitlement/claim risk; Claude Fable 5, Extra effort.
- Candidate commit: `fix: align Planlegg access and paywall claims`.
- Reviewer: fresh Fable 5 Extra or approved high-risk verifier plus read-only external review on exact immutable SHA.
- Evidence boundary: policy/copy/component tests and deterministic browser state/focus checks; no purchase sandbox, media, physical-device or release claim.
</delivery_contract>

<tasks>

<task type="auto">
  <name>Task 1: Derive every paywall claim from enabled capabilities</name>
  <files>src/lib/premium/paywall-copy.ts, src/lib/premium/__tests__/paywall-copy.test.ts, src/lib/premium/products.ts, src/lib/premium/__tests__/products.test.ts, src/components/paywall/PlusExpansionPreview.tsx, src/components/PaywallDialog.tsx</files>
  <read_first>src/lib/access/capabilities.ts, src/lib/premium/gating.ts, src/lib/premium/paywall-copy.ts, src/lib/premium/__tests__/paywall-copy.test.ts, src/lib/premium/products.ts, src/lib/premium/__tests__/products.test.ts, src/components/paywall/PlusExpansionPreview.tsx, src/components/PaywallDialog.tsx</read_first>
  <behavior>
    - The claim builder returns only benefits whose backing capability is enabled.
    - With family sharing, automatic location and soon preparation disabled, no heading/body/trust line/preview contains `sammen`, `familie`, `begge foreldre`, `alle som passer barnet`, `omsorgsperson`, `automatisk sted`, `overalt` or `snart`.
    - Empty or partial capability sets still yield truthful generic copy and never invent a benefit.
  </behavior>
  <action>Replace static expansion promises with a pure `buildCapabilityPaywallCopy(flags)` boundary. Render its returned heading, body, optional trust line and preview items in both paywall components. Remove the live `PAYWALL_COPY.trustLine`, any legacy trust export and their expectations; do not replace them with a static or semantically equivalent family/caregiver promise. A trust line may exist only when returned from an enabled backing capability. Retain store-derived price/trial/purchase/restore code byte-for-byte except imports/rendering needed to remove the unsupported constant.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/lib/premium/__tests__/paywall-copy.test.ts src/lib/premium/__tests__/products.test.ts src/lib/premium/__tests__/gating.test.ts && npx tsc -b --pretty false"</automated></verify>
  <acceptance_criteria>
    - Tests enumerate enabled/disabled capability matrices and fail on unsupported words.
    - Source/copy tests reject the removed trust constant/value and every semantic family/caregiver phrase while family availability is false.
    - Product IDs, offering/package lookup, prices, trials, purchase and restore logic are unchanged.
  </acceptance_criteria>
  <done>Paywall language cannot outrun capability availability.</done>
</task>

<task type="auto">
  <name>Task 2: Render truthful Today/Uke states from fresh centralized access</name>
  <files>src/App.tsx, src/screens/UkeScreen.tsx</files>
  <read_first>src/App.tsx, src/screens/PaakledningScreen.tsx, src/screens/UkeScreen.tsx, src/lib/access/capabilities.ts, src/lib/premium/gating.ts, src/lib/planning/plan-view-model.ts, src/lib/planning/planned-outfit-context.ts, src/components/PaywallDialog.tsx, src/lib/premium/use-access.ts</read_first>
  <action>Consume the fail-closed freshness state from 01-09. Migrate UkeScreen from the 01-08 compatibility wrapper to `resolvePlanningViewAccess`/generic runtime decisions, then remove `shouldShowTenDayTeaser` only after its last runtime import is gone in this same task. Keep complete supported Today at the currently configured child place—do not claim or implement fixed-home behavior yet. Render exactly one evidence-backed future weather comparison for Free only with valid coverage; otherwise render neutral unavailable. It contains no garments, recommendation, DTO or Outfit action and uses `Se uke med Babyora Plus`. Denied actions open the contextual capability-derived paywall with trigger focus return. During entitlement refresh, preserve Today and render paid content neutrally with no paywall flash. App continuously resolves live future authorization separately from the DTO's captured historical fact; loading/denial atomically closes an open paid planned Outfit, clears DTO/origin, restores a valid Today/denied state and returns focus without recomputation. Current Hjem Outfit remains unaffected.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/lib/premium/__tests__/use-access.test.ts src/lib/premium/__tests__/gating.test.ts src/lib/planning/__tests__/plan-view-model.test.ts src/lib/premium/__tests__/paywall-copy.test.ts && npm run build"</automated></verify>
  <acceptance_criteria>
    - Free Today is complete at the currently configured place and has no artificial accuracy/activity restriction; fixed-home acceptance remains Pending for 01-11/01-12.
    - Free Uke has one real weather comparison when evidence is valid, otherwise neutral unavailable; neither state has advice/Outfit/context materialization.
    - Loading never exposes Plus content or triggers a paywall.
    - Plus shows only future capabilities enabled by the central registry.
    - Exact Free/Uke action copy is `Se uke med Babyora Plus`; a live downgrade closes and clears any already-open paid planned Outfit without using its captured access decision as current authority.
    - Cached Plus on configured native startup/resume is neutral until one fresh single-flight result; live false/error cannot leave paid payload visible.
  </acceptance_criteria>
  <done>Planlegg presents each access state without leaking or overstating value.</done>
</task>

<task type="auto">
  <name>Task 3: Prove access, downgrade and focus behavior without media</name>
  <files>e2e/planlegg.ts</files>
  <read_first>e2e/planlegg.ts, e2e/fixtures/planlegg.ts, src/App.tsx, src/screens/PaakledningScreen.tsx, src/screens/UkeScreen.tsx, src/components/PaywallDialog.tsx, src/lib/premium/paywall-copy.ts</read_first>
  <action>Implement `access` using deterministic Free-valid, Free-no-future-evidence, loading, Plus and downgrade entitlement fixtures. Assert complete Today for Free, exactly one weather-only comparison only for valid evidence, neutral unavailable for missing evidence, exact `Se uke med Babyora Plus` action copy, absence of advice/Outfit/hidden Plus DOM, neutral loading, capability-backed Plus content, contextual paywall text, Escape/close focus return, and safe downgrade to Today. Add the critical lifecycle: open a valid Plus future Outfit, confirm title focus, downgrade entitlement while it remains open, then assert the paid drill closes/refuses, transient planned payload is absent, a valid denied/Today state remains, and focus returns safely. Rerun `exact-context` and `semantic-rail` because this candidate edits App/Uke and access around those boundaries. Assert visible/focus/keyboard state only; write no image/video or haptic call-count artifact.</action>
  <verify><automated>cmd.exe /d /s /c "npm run build && npx tsx e2e/planlegg.ts --case access && npx tsx e2e/planlegg.ts --case exact-context && npx tsx e2e/planlegg.ts --case semantic-rail"</automated></verify>
  <acceptance_criteria>
    - The case fails on entitlement flash, hidden paid materialization, unsupported claim or focus loss.
    - Output records fixture IDs and visible assertions only; no personal payload/media.
    - Plus-Outfit-to-downgrade cleanup, exact-context and semantic-rail cases remain green.
  </acceptance_criteria>
  <done>Deterministic evidence covers the access/presentation boundary.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat | Mitigation / disposition |
|---|---|
| Marketing claim promises unavailable behavior | Pure capability-derived copy plus forbidden-word matrix. |
| Entitlement loading leaks Plus | Neutral unresolved state and no optimistic allow. |
| Free preview leaks actionable advice | Weather-only projection with no recommendation/DTO/Outfit action. |
| Paywall traps keyboard focus | Trigger capture, dialog focus trap and connected-trigger return. |
</threat_model>

<verification>
- Focused policy/copy/planning suites, access E2E and full test/lint/build pass.
- Fresh high-risk reviewer verifies the immutable SHA; executor cannot self-PASS.
</verification>

<rollback>
Revert `fix: align Planlegg access and paywall claims`; keep the pure access policy from 01-08 and default denied capabilities.
</rollback>

<success_criteria>
- Free equals complete Today at the currently configured place plus one non-advisory future weather comparison when valid evidence exists, otherwise a neutral unavailable state; fixed-home acceptance remains Pending.
- Plus/paywall exposes only enabled capabilities and restores focus.
- No price/RevenueCat/media change occurred; exact-SHA high-risk PASS exists.
</success_criteria>

<output>
Create `01-10-SUMMARY.md` with capability/claim matrix, configured-place Free/Plus/loading/downgrade evidence, exact candidate SHA, reviewer identity/lane, PASS/BLOCK verdict bound to that SHA, and all remaining Pending gates.
</output>
