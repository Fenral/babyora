---
phase: 01-planlegg-dagslinjen
plan: "03"
type: tdd
wave: 3
package_wave: 1
depends_on: ["01-02"]
files_modified:
  - src/lib/planning/change-events.ts
  - src/lib/planning/change-sentence.ts
  - src/lib/planning/rail-rows.ts
  - src/lib/planning/plan-view-model.ts
  - src/lib/planning/__tests__/change-events.test.ts
  - src/lib/planning/__tests__/change-sentence.test.ts
  - src/lib/planning/__tests__/rail-rows.test.ts
  - src/lib/planning/__tests__/plan-view-model.test.ts
autonomous: true
requirements: [TRUTH-01, EVID-02, GOV-04]
must_haves:
  truths:
    - "Only finalized recommendation changes or explicit action-bearing transitions create events; passive weather change creates none."
    - "Event identity uses absolute ISO time plus canonical content; `transitionContextId` names the event cause/transition and is never confused with the later DTO `plannedContextId`."
    - "Added and removed garments remain separate; swap preserves both sides; deterministic kind/order handles duplicate fingerprints, ties and DST."
    - "Copy is verb-led and coverage-aware rows/view states never invent unsupported advice."
    - "The new canonical contracts use `PlanningPoint`, `PlanningChangeEvent`, `PlanningRailRow`, `derivePlanningChangeEvents` and `buildPlanningRailRows`; the currently rendered legacy exports remain isolated deprecated compile adapters until 01-06 alone atomically migrates UkeScreen and PlanChangeRail, after which 01-18 proves zero runtime imports and deletion is deferred to a standalone high-risk cleanup rather than mixed into a UI slice."
  artifacts:
    - path: "src/lib/planning/change-events.ts"
      provides: "Stable recommendation-change derivation"
      exports: ["derivePlanningChangeEvents", "stablePlanningEventId"]
    - path: "src/lib/planning/plan-view-model.ts"
      provides: "Safe aggregate Planlegg model"
      exports: ["buildPlanViewModel"]
  key_links:
    - from: "src/lib/planning/plan-view-model.ts"
      to: "src/lib/planning/change-events.ts"
      via: "events from finalized evaluated points"
      pattern: "derivePlanningChangeEvents"
    - from: "src/lib/planning/rail-rows.ts"
      to: "src/lib/planning/coverage.ts"
      via: "unchanged-span wording requires assessed evidence"
      pattern: "ForecastCoverage"
  prohibitions:
    - "MUST NOT use the generic identifier `contextId` for event cause or future DTO identity."
    - "MUST NOT change recommendation thresholds/guardrails/material policy or create events from weather-only deltas."
    - "MUST NOT route any new view-model/context behavior through the deprecated legacy adapter, remove it before UkeScreen is migrated, or fabricate ISO/context fields for legacy hour-only inputs."
    - "MUST NOT add packages/APIs/schemas/analytics/location history/media."
---

<objective>
Derive truthful planning events, action copy, rows and view states as a separate high-risk candidate.

Purpose: Keep event semantics independently reviewable from forecast transport and later UI.
Output: Deterministic event/copy/row/view-model contracts over the approved forecast evidence.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/01-planlegg-dagslinjen/01-02-SUMMARY.md
@.planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md
@src/lib/planning/coverage.ts
@src/lib/planning/change-events.ts
@src/lib/planning/change-sentence.ts
@src/lib/planning/rail-rows.ts
@src/lib/recommendation/view.ts
</context>

<delivery_contract>
- Risk/model: high event/action/advice-presentational truth risk; Claude Fable 5, Extra effort.
- Candidate commit: `feat: derive truthful planning transitions`.
- Reviewer: fresh Fable 5 Extra or approved Opus high-risk verifier plus external read-only review on exact SHA.
- Evidence boundary: focused pure tests and unchanged motor suites; forecast candidate is read-only ancestry; no media.
</delivery_contract>

<tasks>

<task type="auto">
  <name>Task 1: TDD stable events and verb-led action grammar</name>
  <files>src/lib/planning/change-events.ts, src/lib/planning/change-sentence.ts, src/lib/planning/__tests__/change-events.test.ts, src/lib/planning/__tests__/change-sentence.test.ts</files>
  <read_first>src/lib/planning/change-events.ts, src/lib/planning/change-sentence.ts, src/lib/planning/__tests__/change-events.test.ts, src/lib/planning/__tests__/change-sentence.test.ts, src/lib/wool-layers/types.ts, .planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md</read_first>
  <behavior>
    - RED: zero/add/remove/true swap/rain/location/prep, duplicate fingerprints, same-instant ties, reordered input, Unicode garments and repeated DST hour.
    - GREEN: canonical `PlanningPoint` has `atIso`, finalized fingerprint, ordered garments/equipment, cause, `transitionContextId` and optional explicit transition; canonical `PlanningChangeEvent` has stable event `id`, `atIso`, kind, separate added/removed lists, cause and `transitionContextId`.
    - GREEN: existing hour-only `PlanPoint`/`ChangeEvent` plus `deriveChangeEvents`/`changeActionSentence` remain a separately typed deprecated compatibility surface for the unchanged current Uke/rail only; no fake `atIso`, event ID or transition identity is synthesized.
    - GREEN: sort by epoch, kind priority `location, prep, rain, swap, remove, add`, then canonical garment order; identical canonical inputs serialize identically.
  </behavior>
  <action>Add the canonical `PlanningPoint`/`PlanningChangeEvent`, `derivePlanningChangeEvents` and canonical planning-sentence path rather than mutating the shape consumed by the current screen mid-wave. Replace hour-only identity only in that canonical path. Preserve recommendation order when deriving set differences; true swap requires both removed and added. Rain/location/prep need explicit action-bearing transition; identical finalized content yields no event. Generate exact UI-SPEC verbs (`Ta på`, `Ta av`, `Bytt fra … til …`, `Ta med`, `Når dere kommer til …`, `Forbered`) and never lead with `+N til`. Retain the old `PlanPoint`/`ChangeEvent`/`deriveChangeEvents`/`changeActionSentence` exports as clearly deprecated, unchanged compile adapters for current Uke/PlanChangeRail only; add contract tests proving they do not synthesize canonical identity or feed the new model. Plan 01-05 prepares only the pure fail-closed resolver and trusted App/Outfit boundary; Plan 01-06 is the sole atomic UkeScreen/rail runtime-consumer migration. Plan 01-18 performs zero-import proof, while a standalone post-phase high-risk cleanup owns definition deletion. Do not introduce `plannedContextId` here.</action>
  <verify><automated>npx vitest run src/lib/planning/__tests__/change-events.test.ts src/lib/planning/__tests__/change-sentence.test.ts</automated></verify>
  <acceptance_criteria>
    - Canonical PlanningChangeKind is exactly add/remove/swap/rain/location/prep.
    - Every event has valid unique id/atIso/cause/transitionContextId; no generic contextId exists.
    - Weather-only changes with identical finalized fingerprint yield zero events.
    - Existing UkeScreen/PlanChangeRail consumers still type-check through only the deprecated legacy surface; canonical tests never use that adapter.
  </acceptance_criteria>
  <done>Event derivation is deterministic, unambiguous and action-bearing.</done>
</task>

<task type="auto">
  <name>Task 2: TDD coverage-aware rail rows</name>
  <files>src/lib/planning/rail-rows.ts, src/lib/planning/__tests__/rail-rows.test.ts</files>
  <read_first>src/lib/planning/rail-rows.ts, src/lib/planning/__tests__/rail-rows.test.ts, src/lib/planning/coverage.ts, src/lib/planning/change-events.ts, .planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md</read_first>
  <behavior>
    - Zero/one/many/adjacent events emit stable ordered row IDs.
    - Full-span copy appears only for fresh contiguous evidence; sampled/gapped/stale uses evaluated-point/partial wording.
    - Canonical change rows preserve event ID and expose only a boolean Outfit availability slot; they never carry or construct a planned DTO.
    - Existing `RailRow`/`buildRailRows(startHour,endHour,events)` remain a separately typed deprecated compatibility surface for the unchanged current screen; canonical rows are never projected into that lossy shape. Plan 01-06 replaces the legacy renderer and callsite atomically, while source deletion is deferred to a standalone high-risk cleanup after 01-18 zero-import proof.
  </behavior>
  <action>Implement canonical `PlanningRailRow` and `buildPlanningRailRows(ForecastCoverage, PlanningChangeEvent[])`; replace start/end-hour inference only in this canonical path. Merge only truly adjacent unchanged spans that share evaluated evidence. Keep event `id` and `transitionContextId`; reserve exact Outfit availability as parent-supplied data and never derive/persist context in the row model. Preserve existing `RailRow` and `buildRailRows(startHour,endHour,events)` as a clearly deprecated, behavior-frozen compile adapter only for the unchanged current Uke/PlanChangeRail. Do not create any canonical-to-legacy projection: the legacy shape cannot carry stable IDs, separate additions/removals, `prep`, causes or canonical action copy without information loss. Contract-test the two independent seams, forbid the legacy seam from `buildPlanViewModel`, require 01-06 to replace both legacy runtime consumers in one compiling task, require 01-18 to prove zero runtime imports, and defer physical source deletion to a standalone high-risk cleanup so risk lanes remain separate.</action>
  <verify><automated>npx vitest run src/lib/planning/__tests__/rail-rows.test.ts</automated></verify>
  <acceptance_criteria>
    - No unsupported `time for time`, `hele dagen`, `ut dagen` or `til kl.` copy exists.
    - Reordered canonical inputs yield identical rows.
    - Rows contain no `contextId` or `plannedContextId` payload.
    - The legacy rail adapter remains compile-safe for current rendering, is excluded from canonical view-model/context paths, and no canonical-to-legacy projection exists; it loses all runtime consumers in 01-06 and remains only as isolated deprecated source pending standalone high-risk deletion after 01-18 zero-import proof.
  </acceptance_criteria>
  <done>Rail rows encode only evidence-supported spans and event references.</done>
</task>

<task type="auto">
  <name>Task 3: TDD safe aggregate Planlegg states</name>
  <files>src/lib/planning/plan-view-model.ts, src/lib/planning/__tests__/plan-view-model.test.ts</files>
  <read_first>src/lib/planning/plan-view-model.ts, src/lib/planning/__tests__/plan-view-model.test.ts, src/lib/planning/coverage.ts, src/lib/planning/change-events.ts, src/lib/planning/rail-rows.ts, src/lib/planning/__tests__/planlegg-fixtures.ts, src/lib/recommendation/view.ts</read_first>
  <behavior>
    - Loading, no-cache error, cached offline, partial, empty and ready are discriminated states.
    - No input/invalid evidence produces no garment/action advice; offline/partial includes source limits.
    - Valid points return verdict, next action, events/rows and weather-only forecast without constructing future DTOs.
  </behavior>
  <action>Implement `buildPlanViewModel` as a pure aggregator over approved coverage, canonical finalized `PlanningPoint` values, `derivePlanningChangeEvents` and `buildPlanningRailRows`. Reuse the pure, explicit, typed and immutable presentation-view shape from `src/lib/recommendation/view.ts`; copy only that boundary pattern, never its inactive V2 recommendation rules or thresholds. Empty valid evaluation uses exact UI-SPEC empty copy; cached offline includes timestamp; forecast rows remain weather-only. Return selected-next-event candidates deterministically, but leave selection, access and exact-context map ownership to UkeScreen. Add a compile regression proving the untouched current UkeScreen and PlanChangeRail still consume only their independent deprecated adapters until the atomic 01-06 migration; canonical rows are not forced through those lossy types.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/lib/planning/__tests__/plan-view-model.test.ts && npx vitest run src/lib/planning/__tests__ && npm run lint && npm run build"</automated></verify>
  <acceptance_criteria>
    - Error/empty inputs expose no garments/actions.
    - View model neither imports access UI nor constructs PlannedOutfitContext.
    - Stable source points produce byte-identical events/rows/candidates.
    - Full build proves current consumers compile without weakening canonical fields or fabricating canonical identities; compatibility seams are isolated and later plans prove zero runtime consumption before standalone deletion.
  </acceptance_criteria>
  <done>The later UI receives one conservative deterministic presentation model.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat | Mitigation / disposition |
|---|---|
| Passive weather becomes clothing advice | Finalized fingerprint/action-bearing transition gate. |
| DST/tie collisions | Absolute ISO plus canonical kind/content identity. |
| Event cause confused with destination DTO | `transitionContextId` and `plannedContextId` are distinct contracts. |
| Invalid/empty evidence fabricates advice | Discriminated safe view states with no defaults. |
</threat_model>

<artifacts_this_phase_produces>
- Expanded event/copy/row contracts and new aggregate view-model module/tests.
- New event identifier `transitionContextId`; no future DTO, package, API, schema or media artifact.
</artifacts_this_phase_produces>

<verification>
- Focused planning suites, unchanged recommendation-engine suites and full test/lint/build pass.
- Fresh high-risk reviewer verifies this event candidate separately from forecast provenance.
</verification>

<rollback>
Revert `feat: derive truthful planning transitions` as one candidate; do not retain new copy/rows without event semantics.
</rollback>

<success_criteria>
- Event/copy/row/view states are deterministic and conservative.
- Event cause identity is unambiguous across later plans.
- Separate exact-SHA high-risk PASS exists; media/device/release remain Pending.
</success_criteria>

<output>
Create `01-03-SUMMARY.md` with RED/GREEN matrix, exact candidate SHA, reviewer identity/lane, PASS/BLOCK verdict bound to that SHA, and all remaining Pending gates.
</output>
