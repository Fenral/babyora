---
phase: 01-planlegg-dagslinjen
plan: "11"
type: tdd
wave: 11
package_wave: 5
depends_on: ["01-10"]
files_modified:
  - src/lib/location/cache-scope.ts
  - src/lib/met-no/client.ts
  - src/lib/met-no/__tests__/client.test.ts
  - src/lib/geocode/nominatim.ts
  - src/lib/geocode/__tests__/nominatim.test.ts
  - src/hooks/useWeather.ts
  - src/hooks/__tests__/useWeather.test.ts
  - src/state/location-pref-store.ts
  - src/state/__tests__/location-pref-store.test.ts
autonomous: true
requirements: [ACCESS-01, CTXT-01, EVID-02, GOV-04]
must_haves:
  truths:
    - "Fixed/manual forecast and reverse-geocode requests retain the reviewed persistent cache, while automatic coordinates use memory-only application caches plus network `cache:'no-store'` and create/read zero `metno:*` or `nominatim:*` localStorage keys."
    - "`useWeather` receives explicit cache/source scope, includes it in fetch identity and preserves Plan 01-02 provenance/currentness/latest-key behavior across scope changes."
    - "The stored child place is the fixed-home authority at cutover; only existing `mode` persists, while child-scoped automatic place, generation, entitlement and source remain memory-only."
    - "A pure resolver intersects the 01-08 runtime capability decision with mode and a valid matching ephemeral snapshot; loading/denied/flag-off/manual/missing/invalid/child-mismatch resolves fixed home with persistent scope."
    - "Automatic-location implementation availability remains false; this foundation performs no permission, GPS or UI activation work."
  artifacts:
    - path: "src/lib/location/cache-scope.ts"
      provides: "Shared explicit persistent versus memory-only request contract"
    - path: "src/lib/met-no/client.ts"
      provides: "Scope-aware forecast cache with automatic no-store path"
    - path: "src/lib/geocode/nominatim.ts"
      provides: "Scope-aware reverse-geocode cache with automatic no-store path"
    - path: "src/state/location-pref-store.ts"
      provides: "Fixed-home/ephemeral store boundary and pure effective-place resolver"
  key_links:
    - from: "src/hooks/useWeather.ts"
      to: "src/lib/met-no/client.ts"
      via: "explicit cacheScope participates in request identity and fetch options"
      pattern: "cacheScope"
    - from: "src/state/location-pref-store.ts"
      to: "src/lib/premium/gating.ts"
      via: "effective place consumes generic runtime capability result"
      pattern: "resolveRuntimeCapabilityAccess"
  prohibitions:
    - "MUST NOT edit App, Settings, Hjem, Uke, Outfit, availability flags, paywall, RevenueCat, products/prices, packages/APIs/schemas or media."
    - "MUST NOT let automatic coordinates touch persistent web/network caches, persist automatic place/generation/source/entitlement, or mutate Child city/lat/lon."
    - "MUST NOT weaken 01-02 forecast provenance, freshness, coverage, cancellation or latest-key guards."
---

<objective>
Establish the privacy-safe fixed-home/ephemeral and cache-scope foundation for automatic location.

Purpose: Make it impossible for automatic coordinates to leak into persistent weather/geocode cache or overwrite the fixed home before UI activation exists.
Output: Explicit cache scopes, scope-aware weather hook, session-only automatic-place state and pure fail-closed resolver/tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/01-planlegg-dagslinjen/01-10-SUMMARY.md
@.planning/phases/01-planlegg-dagslinjen/01-02-SUMMARY.md
@src/lib/met-no/client.ts
@src/lib/geocode/nominatim.ts
@src/hooks/useWeather.ts
@src/state/location-pref-store.ts
@src/state/children-store.tsx
@src/lib/premium/gating.ts
</context>

<delivery_contract>
- Risk/model: high location/privacy/cache correctness risk; Claude Fable 5, Extra effort.
- Candidate commit: `feat: isolate automatic location data in memory`.
- Reviewer: fresh Fable 5 Extra or approved high-risk verifier plus adversarial read-only privacy review on exact SHA.
- Evidence boundary: pure/client/hook/store tests and source/storage scans only; no permission UI, media or physical-device claim.
</delivery_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make forecast and reverse-geocode caching scope explicit</name>
  <files>src/lib/location/cache-scope.ts, src/lib/met-no/client.ts, src/lib/met-no/__tests__/client.test.ts, src/lib/geocode/nominatim.ts, src/lib/geocode/__tests__/nominatim.test.ts</files>
  <read_first>src/lib/met-no/client.ts, src/lib/met-no/__tests__/client.test.ts, src/lib/geocode/nominatim.ts, src/lib/access/capabilities.ts, src/lib/premium/gating.ts, .planning/phases/01-planlegg-dagslinjen/01-02-SUMMARY.md</read_first>
  <behavior>
    - `persistent` scope preserves valid localStorage cache read/write and normal network caching for fixed/manual coordinates.
    - `memory-only` scope reads/writes only a bounded module-memory cache, calls fetch with `cache:'no-store'`, and performs zero localStorage operations even when the same coordinates exist persistently.
    - Persistent and memory-only keys never cross-hit; expiry/error/404/null behavior remains deterministic and Plan 01-02 forecast evidence metadata is preserved.
  </behavior>
  <action>Create shared `LocationCacheScope = 'persistent' | 'memory-only'` and an explicit request options object. Extend the Plan 01-02 `fetchForecast` result contract and `reverseGeocode` with that option. Default existing fixed/manual callers to persistent for compatibility. Persistent follows the current reviewed cache paths. Memory-only uses a bounded in-process Map, never calls localStorage, and sends `RequestInit.cache='no-store'`; represent reverse-geocode null hits explicitly so null is cacheable without confusing a miss. Include scope in every memory key and keep the two stores disjoint. Tests spy on storage and fetch options for same-coordinate cross-scope cases, expiry/network/error/404 and prove zero automatic persistent `metno:`/`nominatim:` writes.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/lib/met-no/__tests__/client.test.ts src/lib/geocode/__tests__/nominatim.test.ts && npx tsc -b --pretty false"</automated></verify>
  <done>Automatic network lookups have an explicit memory-only/no-store path that cannot share persistent cache bytes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry cache scope atomically through useWeather</name>
  <files>src/hooks/useWeather.ts, src/hooks/__tests__/useWeather.test.ts</files>
  <read_first>src/hooks/useWeather.ts, src/hooks/__tests__/useWeather.test.ts, src/lib/met-no/client.ts, .planning/phases/01-planlegg-dagslinjen/01-02-SUMMARY.md</read_first>
  <behavior>
    - The hook accepts explicit cache/source options and includes scope in its request/latest-result identity.
    - A fixed→automatic or automatic→fixed change cannot publish the prior scope's late result.
    - Plan 01-02 provenance/currentness/coverage and stale/error semantics survive unchanged.
  </behavior>
  <action>Extend `useWeather` with an options parameter containing the explicit cache scope/source while preserving existing refHour behavior. Include coordinates, refHour and scope in the fetch key; pass scope into `fetchForecast`; retain the atomic `ForecastFetchResult` unwrap, cancellation and latest-key guard from 01-02. Add fake-promise tests resolving cross-scope requests out of order and assert only the current key publishes. Keep existing fixed/manual callers compiling through the persistent default until 01-12 wires explicit sources.</action>
  <verify><automated>npx vitest run src/hooks/__tests__/useWeather.test.ts src/lib/planning/__tests__/forecast-evidence.test.ts</automated></verify>
  <done>Weather provenance and latest-result safety remain atomic across location privacy scopes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Separate fixed home, persisted mode and ephemeral automatic place</name>
  <files>src/state/location-pref-store.ts, src/state/__tests__/location-pref-store.test.ts</files>
  <read_first>src/state/location-pref-store.ts, src/state/__tests__/location-pref-store.test.ts, src/state/children-store.tsx, src/lib/premium/gating.ts, src/lib/location/cache-scope.ts</read_first>
  <behavior>
    - Current valid Child id/city/lat/lon remains byte-identical fixed home; only `mode` persists under the existing key.
    - Auto resolves automatic only with allowed+implemented runtime decision and valid matching in-memory child snapshot; every other state resolves fixed home/persistent.
    - Reloaded legacy auto without memory, invalid/stale child, loading, Free, disabled flag and explicit manual all resolve fixed home without rewriting the stored preference except explicit user mode change.
  </behavior>
  <action>Extend the store with validated child-scoped in-memory `automaticPlace`, generation-aware set/clear actions and pure `resolveEffectivePlace(fixedHome, storedMode, runtimeDecision, automaticPlace)`. Return readonly child/city/coordinates/source plus `cacheScope`. Configure Zustand persist `partialize`/merge so only existing mode hydrates/persists under `babyora.location-pref`; automatic place, generation, entitlement, source and coordinates never serialize. Treat current stored child place as fixed home at cutover and do not reconstruct a former place. Reject blank labels, non-finite/out-of-range coordinates and child mismatch. Keep availability false and call no browser/native API.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/state/__tests__/location-pref-store.test.ts src/lib/premium/__tests__/gating.test.ts && npx tsc -b --pretty false"</automated></verify>
  <done>Fixed home, stored preference and automatic session data cannot overwrite or impersonate one another.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat | Mitigation / disposition |
|---|---|
| Automatic coordinates persist in cache | Memory-only/no-store scope with storage spies. |
| Same coordinates cross privacy scope | Scope-partitioned keys and hook identity. |
| Stored auto bypasses entitlement/availability | Generic runtime intersection plus pure resolver. |
| Legacy auto destroys home | Current Child snapshot remains fixed authority; no migration guess. |
</threat_model>

<verification>
- Cache/client/hook/store/gating tests and typecheck pass.
- Fresh high-risk privacy/location reviewer PASS binds to the immutable foundation SHA.
</verification>

<rollback>
Revert `feat: isolate automatic location data in memory`; automatic-location availability remains false, so no entry point can use the removed foundation.
</rollback>

<success_criteria>
- Automatic coordinates produce zero persistent forecast/geocode keys.
- Fixed/manual caching and Plan 01-02 evidence semantics remain intact.
- Fixed-home/ephemeral resolution fails closed and no UI capability is enabled.
</success_criteria>

<output>
Create `01-11-SUMMARY.md` with cache-scope/storage matrix, fixed-home resolver table, byte-equality evidence, exact candidate SHA, reviewer identity/lane and PASS/BLOCK verdict bound to that SHA; permission/UI/media gates remain Pending.
</output>
