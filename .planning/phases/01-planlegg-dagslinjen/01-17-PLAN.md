---
phase: 01-planlegg-dagslinjen
plan: "17"
type: execute
wave: 17
package_wave: 5
depends_on: ["01-16"]
files_modified:
  - src/App.tsx
  - src/screens/GuideHubScreen.tsx
  - src/data/vinterprogram.ts
  - src/screens/__tests__/guide-routing.test.tsx
  - e2e/planlegg.ts
autonomous: true
requirements: [ACCESS-01, UI-02, GOV-04]
must_haves:
  truths:
    - "No legacy Min garderobe entry is removed or redirected until Snart capability/UI/model has an exact-SHA independent PASS from 01-16."
    - "After that gate, the primary Guide entry opens Planlegg/Snart directly and returns focus/navigation coherently; it does not open a dead wardrobe screen."
    - "Both live entry paths—GuideHub `onOpenCard` and Vinterprogram `onOpenTarget`—flow through the same App-owned typed Snart helper; neither can fall through to a generic Guide drill."
    - "User-visible Guide/program CTAs no longer advertise `Min garderobe`, `Mine plagg` or an unavailable wardrobe-personalization flow."
    - "The orphaned legacy implementation is not broadly deleted in this route-only slice; unrelated catalog/engine behavior stays intact."
  artifacts:
    - path: "src/App.tsx"
      provides: "Typed Guide-to-Planlegg/Snart route handoff"
    - path: "src/screens/GuideHubScreen.tsx"
      provides: "Truthful Snart replacement entry"
    - path: "src/screens/__tests__/guide-routing.test.tsx"
      provides: "Route/claim regression contract"
    - path: "e2e/planlegg.ts"
      provides: "Cross-root migration evidence"
  key_links:
    - from: "src/screens/GuideHubScreen.tsx"
      to: "src/App.tsx"
      via: "typed `snart` target requests Planlegg Snart view"
      pattern: "snart"
    - from: "src/App.tsx"
      to: "src/screens/UkeScreen.tsx"
      via: "one-shot requested Planlegg view, consumed without stale replay"
      pattern: "requestedPlanView"
    - from: "src/screens/VinterprogramScreen.tsx"
      to: "src/App.tsx"
      via: "opaque onOpenTarget is bound to the same App dispatcher as GuideHub onOpenCard, including the typed Snart branch"
      pattern: "onOpenTarget"
  prohibitions:
    - "MUST NOT execute if 01-16 is blocked, capability false, hashes stale or high-risk PASS absent."
    - "MUST NOT change Snart rules/model/copy, access policy, pricing, RevenueCat, engine/catalog, packages/APIs/schemas or broad-delete legacy files/data."
    - "MUST NOT leave a misleading wardrobe CTA pointing to Snart or create screenshots/video."
---

<objective>
Move the user-visible blind-alley wardrobe entries to the now-verified Snart destination without broad legacy deletion.

Purpose: Make Guide navigation match the product direction only after the replacement is real.
Output: Typed cross-root route, honest Guide/program entry copy, route tests and deterministic no-media evidence.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-planlegg-dagslinjen/01-16-SUMMARY.md
@.planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md
@src/App.tsx
@src/screens/GuideHubScreen.tsx
@src/screens/UkeScreen.tsx
@src/screens/VinterprogramScreen.tsx
@src/data/vinterprogram.ts
@src/lib/premium/plus-features.ts
</context>

<delivery_contract>
- Risk/model: standard typed routing/content cleanup after high-risk Snart PASS; Claude Sonnet 5, High effort.
- Candidate commit: `refactor: route wardrobe entries to Snart`.
- Reviewers: fresh Sonnet 5 High standard verifier plus a separate fresh high-risk regression reviewer for App's exact-context/location/access handoffs and exact approved Snart-copy reuse on the exact SHA; reviewers confirm the 01-16 Uke/Snart source manifest was not modified.
- Evidence boundary: route/type/DOM/focus/browser checks only; no media/physical/release claim.
</delivery_contract>

<tasks>

<task type="auto">
  <name>Task 1: Atomically migrate every live entry to one typed Snart dispatcher</name>
  <files>src/App.tsx, src/screens/GuideHubScreen.tsx, src/data/vinterprogram.ts, src/screens/__tests__/guide-routing.test.tsx</files>
  <read_first>.planning/phases/01-planlegg-dagslinjen/01-16-SUMMARY.md, .planning/phases/01-planlegg-dagslinjen/01-SNART-RULES.md, src/lib/premium/plus-features.ts, src/App.tsx, src/screens/GuideHubScreen.tsx, src/screens/UkeScreen.tsx, src/screens/VinterprogramScreen.tsx, src/data/vinterprogram.ts, src/screens/MinGarderobeScreen.tsx</read_first>
  <action>First verify the exact 01-16 SHA/hashes/PASS, its recorded Uke/Snart source manifest and enabled `soon_preparation`; stop otherwise. Add typed `snart` alongside the still-temporary legacy target. In one compiling commit, replace the visible Guide wardrobe card with approved outcome copy/typed `snart`; change the material lesson's old wardrobe CTA to the existing `plaggbib` destination with an accurate comparison label; and preserve the week-8 lesson ID/order while replacing its unsupported wardrobe-personalization title/lead/sections/CTA with a short Snart explainer assembled only from the versioned approved 01-14 period, historical-normal qualifier, uncertainty and access copy, with target `snart`. Add one App dispatcher/helper that, for `snart`, clears any Guide drill, navigates to `plan`, and passes a new one-shot `requestedPlanView='snart'` plus request token through the verified 01-15 UkeScreen request contract; all other Guide targets retain their normal drill path. Pass this same dispatcher to both GuideHub `onOpenCard` and Vinterprogram `onOpenTarget`; do not pass generic `setDrill` to either. Do not edit UkeScreen, VinterprogramScreen or any Snart source. Retain the legacy `min-garderobe` union member/lazy branch only through this task so current typed data and every intermediate commit compile; after the data migration no current visible entry may emit it. Add integration tests that invoke both callbacks, prove both Snart requests use the same helper/single-consumption contract, and prove the material CTA still opens Plaggbibliotek.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/screens/__tests__/guide-routing.test.tsx src/lib/premium/__tests__/plus-features.test.ts src/lib/planning/__tests__/planning-interaction.test.ts && npx tsc -b --pretty false"</automated></verify>
  <acceptance_criteria>
    - Guide card and live Vinterprogram week-8 callback each open Planlegg with Snart selected exactly once through the same App helper; the material CTA opens Plaggbibliotek.
    - Current Guide/program exports emit no `min-garderobe`; its temporary typed route remains only for compile-safe removal in Task 2 and legacy implementation files remain untouched.
    - All eight lesson IDs/order remain stable and replacement prose is drawn only from approved versioned Snart presentation/uncertainty copy.
    - Existing Hjem/Planlegg/Guide/Familie root navigation still works and focus lands predictably.
  </acceptance_criteria>
  <done>Every current live entry is truthfully migrated through one typed, compiling, non-replaying Snart route while the old branch remains temporarily unreachable.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the now-unreachable legacy route and lock migrated content</name>
  <files>src/App.tsx, src/screens/GuideHubScreen.tsx, src/screens/__tests__/guide-routing.test.tsx</files>
  <read_first>src/App.tsx, src/screens/GuideHubScreen.tsx, src/data/vinterprogram.ts, src/screens/VinterprogramScreen.tsx, src/screens/MinGarderobeScreen.tsx, src/screens/__tests__/guide-routing.test.tsx, src/lib/premium/plus-features.ts, .planning/phases/01-planlegg-dagslinjen/01-SNART-RULES.md</read_first>
  <action>Only after Task 1 compiles and its live-callback tests pass, remove `min-garderobe` from `GuideHubTarget` and remove the lazy MinGarderobe import/branch from App's routable graph. Do not delete the legacy screen, catalog or engine modules. Strengthen assertions that current exported Guide/program content contains no `Min garderobe`, `Mine plagg`, unsupported wardrobe-personalization promise or `min-garderobe` target; all eight lesson IDs/order remain stable; every CTA label matches its actual target; the week-8 explainer is byte-for-byte sourced from approved Snart presentation/uncertainty copy; and both App callback bindings still use the shared dispatcher rather than generic `setDrill`. Add no new rule, threshold, health statement or garment guidance.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/screens/__tests__/guide-routing.test.tsx && npm run lint && npm run build"</automated></verify>
  <acceptance_criteria>
    - Every surviving CTA label matches its actual target.
    - Unsupported wardrobe-personalization marketing is absent from current Guide/program exports; the eight-week progress identity/order is preserved.
    - Replacement prose is byte-for-byte drawn from approved versioned Snart presentation/uncertainty copy; no rules, thresholds, health/advice or engine/catalog source changes.
    - App has no routable/lazy `min-garderobe` branch or generic callback fallthrough; the untouched legacy implementation is simply unreachable.
  </acceptance_criteria>
  <done>User-visible entry points no longer lead to or promise the abandoned workflow, and the unreachable legacy route is removed without breaking compilation.</done>
</task>

<task type="auto">
  <name>Task 3: Prove cross-root migration and regressions without media</name>
  <files>e2e/planlegg.ts</files>
  <read_first>.planning/phases/01-planlegg-dagslinjen/01-16-SUMMARY.md, e2e/planlegg.ts, e2e/fixtures/planlegg.ts, src/App.tsx, src/screens/GuideHubScreen.tsx, src/screens/UkeScreen.tsx, src/components/planning/SnartPlan.tsx, src/components/planning/SnartPlan.css, src/lib/planning/snart-session.ts, src/lib/planning/snart.ts, src/data/snart-climate-1991-2020-v1.json</read_first>
  <action>Implement `route-migration`. As a fail-fast preflight, parse the 01-16 summary source manifest and recompute SHA-256 for UkeScreen, SnartPlan TSX/CSS, Snart session orchestration, Snart model and climate pack; fail if any byte differs. In the live app, activate the Guide replacement card by pointer; assert Planlegg is the active bottom root, Snart is selected exactly once and content follows current entitlement. Leave through an explicit bottom-root action and return; the consumed `requestedPlanView` must not replay. Exercise the existing App-owned left-edge handler and assert its React state-router behavior rather than browser history. Then use deterministic Plus/program-start state to open `Første vinter`, navigate to the preserved week-8 lesson, activate its actual `Prøv selv` CTA by keyboard, and assert the same Planlegg/Snart single-selection/no-replay/focus contract. Close each drill through App's existing dismiss callback and prove focus returns coherently. Activate the material lesson CTA and prove it opens Plaggbibliotek rather than Snart or a dead route. Source-scan current exports for zero legacy wardrobe route/copy and assert no `window.history`, `history.back`, `pushState`, `popstate` or new router dependency was introduced. Rerun `exact-context`, `access`, `automatic-location` and `snart` because App routing changed across each high-risk boundary. Assert visible/focus/keyboard state only and write no media.</action>
  <verify><automated>cmd.exe /d /s /c "npm run build && npx tsx e2e/planlegg.ts --case route-migration && npx tsx e2e/planlegg.ts --case exact-context && npx tsx e2e/planlegg.ts --case access && npx tsx e2e/planlegg.ts --case automatic-location && npx tsx e2e/planlegg.ts --case snart"</automated></verify>
  <acceptance_criteria>
    - No accessible control points to `min-garderobe`.
    - Both the live Guide card and live week-8 Vinterprogram CTA use the shared Snart route for allowed/denied access without advice leakage or replay; the material CTA reaches Plaggbibliotek.
    - Four-root navigation, focus and one-scroll shell remain intact.
    - SHA-256 preflight proves the 01-16 Uke/Snart/session/model/climate sources are byte-identical, and all four impacted high-risk regression cases remain green.
    - In-app close, bottom-root and existing App left-edge behavior are coherent; no browser-history mechanism or new router is introduced.
  </acceptance_criteria>
  <done>The route migration is deterministic and does not destabilize shared navigation.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat | Mitigation / disposition |
|---|---|
| Replacement route ships before replacement | Hard dependency and exact-SHA/capability precheck. |
| Old label misrepresents new destination | CTA/content export assertions. |
| One-shot route keeps forcing view | Consumed request token and replay test. |
| Broad deletion breaks catalog/engine | Remove user-visible route only; legacy modules stay untouched. |
</threat_model>

<verification>
- Route/component/type tests, route-migration E2E and full test/lint/build pass.
- Fresh standard reviewer and separate high-risk App/Snart-copy regression reviewer PASS the exact SHA; Uke/Snart source remains byte-identical to 01-16.
</verification>

<rollback>
Revert `refactor: route wardrobe entries to Snart`; if Snart later becomes unavailable, disable its capability before restoring any entry.
</rollback>

<success_criteria>
- All visible legacy wardrobe entries have accurate verified destinations and no unsupported personalization claim.
- Guide-to-Snart navigation is typed, focus-safe and non-replaying.
- No broad deletion, product/access/rule or media change occurred.
</success_criteria>

<output>
Create `01-17-SUMMARY.md` with prerequisite SHA, removed/redirected entry inventory, in-app route evidence, exact candidate SHA, both the standard and separate high-risk App/Snart regression reviewer identities and lanes, each PASS/BLOCK verdict bound to that same SHA, and byte-equality proof that the approved 01-16 Uke/Snart sources were not modified, plus all remaining Pending gates.
</output>
