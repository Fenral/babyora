---
phase: 01-planlegg-dagslinjen
plan: "12"
type: execute
wave: 12
package_wave: 5
depends_on: ["01-11"]
files_modified:
  - src/App.tsx
  - src/hooks/useAutoLocationRefresh.ts
  - src/hooks/__tests__/useAutoLocationRefresh.test.ts
  - src/screens/InnstillingerScreen.tsx
  - src/screens/HjemScreen.tsx
  - src/screens/UkeScreen.tsx
  - src/screens/PaakledningScreen.tsx
  - src/lib/premium/plus-features.ts
  - src/lib/premium/__tests__/plus-features.test.ts
  - e2e/planlegg.ts
autonomous: true
requirements: [ACCESS-01, CTXT-01, EVID-02, GOV-04]
must_haves:
  truths:
    - "App startup/resume and Settings activation consume the generic runtime capability resolver; loading, denied, implementation-off and non-activation manual paths perform zero permission/GPS/geocode/forecast I/O."
    - "Settings supplies explicit activation intent while mode is manual; stable allowed auto startup/resume and one explicit allowed activation run one cancellable single-flight GPS→memory-only reverse-geocode pipeline."
    - "Settings persists `mode:'auto'` only after a successful in-memory resolution; no automatic path calls `updateChild`, and off/downgrade/loading/flag-off/switch/unmount/late completion clears or ignores ephemeral state."
    - "Hjem and Uke consume the same resolved place/source/cache scope; fixed/manual uses persistent cache and automatic uses memory-only/no-store."
    - "Current Hjem→App→Outfit transient context captures the exact evaluated place label/coordinates/source, weather snapshot, finalized recommendation and activity/vogn; Outfit does not reread child/weather when that context exists."
    - "Future Uke Outfit retains its exact evaluated place/weather/source; automatic-location availability flips true only after all focused integration checks pass."
  artifacts:
    - path: "src/hooks/useAutoLocationRefresh.ts"
      provides: "Intent-aware access-gated cancellable one-shot controller"
    - path: "src/screens/HjemScreen.tsx"
      provides: "Resolved place/weather and exact current Outfit context"
    - path: "src/screens/UkeScreen.tsx"
      provides: "Resolved place/weather and exact future DTO source"
    - path: "e2e/planlegg.ts"
      provides: "Storage/privacy/current+future Outfit location lifecycle evidence"
  key_links:
    - from: "src/App.tsx"
      to: "src/hooks/useAutoLocationRefresh.ts"
      via: "generic runtime decision plus mode/child drive startup and resume intent"
      pattern: "resolveRuntimeCapabilityAccess"
    - from: "src/screens/InnstillingerScreen.tsx"
      to: "src/hooks/useAutoLocationRefresh.ts"
      via: "explicit activation intent uses the same controller while manual"
      pattern: "activation"
    - from: "src/screens/HjemScreen.tsx"
      to: "src/screens/PaakledningScreen.tsx"
      via: "App-owned transient current Outfit context remains authoritative"
      pattern: "source"
  prohibitions:
    - "MUST NOT change prices/trials/purchase/restore/RevenueCat, packages/APIs/schemas or media."
    - "MUST NOT use watchPosition/background timers/history/tracking, persist automatic coordinates/city/source, call updateChild from automatic flow, or create persistent metno/nominatim keys for automatic coordinates."
    - "MUST NOT request permission or perform network/location I/O while entitlement refreshes, access is denied, implementation is off, or mode is manual without explicit Settings activation intent."
    - "MUST NOT redesign Home, add living weather ambience, or claim the deferred Home-to-Outfit signature transition while integrating location context."
---

<objective>
Integrate automatic location as a truthful, privacy-safe Plus capability across both entry points and both Outfit paths.

Purpose: Deliver “overalt” without overwriting home, leaking coordinates into persistence or mixing place B garments with place A weather.
Output: Intent-aware controller, App/Settings/Hjem/Uke wiring, exact current/future Outfit parity, capability enablement and deterministic no-media E2E.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-planlegg-dagslinjen/01-11-SUMMARY.md
@.planning/phases/01-planlegg-dagslinjen/01-10-SUMMARY.md
@docs/BABYORA-UX-MOTION-BIBLE.md
@src/lib/premium/gating.ts
@src/lib/premium/use-access.ts
@src/state/location-pref-store.ts
@src/hooks/useWeather.ts
@src/hooks/useAutoLocationRefresh.ts
@src/App.tsx
@src/screens/InnstillingerScreen.tsx
@src/screens/HjemScreen.tsx
@src/screens/UkeScreen.tsx
@src/screens/PaakledningScreen.tsx
</context>

<delivery_contract>
- Risk/model: high entitlement/location/privacy/exact-context race risk; Claude Fable 5, Extra effort.
- Candidate commit: `feat: enforce privacy-safe Plus automatic location`.
- Reviewer: fresh Fable 5 Extra or approved high-risk verifier plus adversarial read-only review on exact immutable SHA.
- Evidence boundary: store/hook/access/browser DOM+storage mocks only; native permission quality, media and physical device remain Pending.
</delivery_contract>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement one intent-aware cancellable controller</name>
  <files>src/hooks/useAutoLocationRefresh.ts, src/hooks/__tests__/useAutoLocationRefresh.test.ts, src/App.tsx</files>
  <read_first>src/hooks/useAutoLocationRefresh.ts, src/state/location-pref-store.ts, src/lib/premium/gating.ts, src/lib/premium/use-access.ts, src/lib/geocode/nominatim.ts, src/App.tsx</read_first>
  <behavior>
    - Startup/resume runs only for allowed+implemented access, stored auto mode and active child; Settings activation may run while manual only with explicit activation intent.
    - Loading/denied/flag-off/manual-without-intent performs zero permission, GPS and geocode calls and clears stale automatic state.
    - One stable request commits a validated matching in-memory place; concurrent/superseded/downgrade/off/switch/unmount/late results cannot commit.
  </behavior>
  <action>Refactor around one injected/testable controller accepting exact `{intent:'startup'|'resume'|'settings-activation', runtimeDecision, mode, childId}`. Intersect policy and implementation through 01-08's generic resolver before I/O. Only `settings-activation` may proceed while mode is manual; startup/resume require stored auto. Deduplicate one in-flight request, invalidate by generation/child/access/mode/unmount, recheck before each external step and before commit, and call `reverseGeocode` with memory-only scope. Commit only through set/clearAutomaticPlace; remove all updateChild/logged coordinate/raw error paths. App passes fresh 01-09 access and active child into startup/resume wiring. Typed outcomes expose no coordinates. Tests resolve GPS/geocode out of order across every invalidation and assert exact zero/one cardinality.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/hooks/__tests__/useAutoLocationRefresh.test.ts src/state/__tests__/location-pref-store.test.ts src/lib/premium/__tests__/use-access.test.ts src/lib/premium/__tests__/gating.test.ts && npx tsc -b --pretty false"</automated></verify>
  <done>The sole automatic-location controller is intent-aware, access-gated, single-flight and unable to mutate persisted home.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Settings and both weather consumers, then enable only the proven capability</name>
  <files>src/screens/InnstillingerScreen.tsx, src/screens/HjemScreen.tsx, src/screens/UkeScreen.tsx, src/lib/premium/plus-features.ts, src/lib/premium/__tests__/plus-features.test.ts</files>
  <read_first>src/screens/InnstillingerScreen.tsx, src/screens/HjemScreen.tsx, src/screens/UkeScreen.tsx, src/hooks/useAutoLocationRefresh.ts, src/hooks/useWeather.ts, src/state/location-pref-store.ts, src/lib/premium/gating.ts, src/lib/premium/use-access.ts, src/lib/premium/plus-features.ts, src/lib/premium/paywall-copy.ts</read_first>
  <action>Settings derives the generic automatic-location runtime decision. Loading/disabled does nothing and disables neutrally; Free/expired opens capability-derived paywall without location I/O and restores trigger focus. Allowed activation calls the shared controller with explicit settings-activation intent and persists only mode auto after successful in-memory resolution. Denial/error stays manual, clears memory and gives calm feedback. Turning off is always allowed and cancels/clears.

Hjem and Uke subscribe to the same fixed-home/mode/ephemeral state, call `resolveEffectivePlace`, and pass its coordinates plus cache scope to `useWeather`. Label exact `Fast sted · {sted}` or `Nåværende sted · {sted}`. Loading/Free/downgrade/reload synchronously resolves fixed home; future DTO captures the resolved place/source/weather from its evaluation. Snart is not wired here and remains fixed-home-only later. Only after controller/store/weather tests and build pass, set only automatic_location availability true; family/Snart/calibration remain false. The paywall may then derive automatic-place value from the enabled flag.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/hooks/__tests__/useWeather.test.ts src/hooks/__tests__/useAutoLocationRefresh.test.ts src/state/__tests__/location-pref-store.test.ts src/lib/premium/__tests__/plus-features.test.ts src/lib/premium/__tests__/paywall-copy.test.ts && npm run build"</automated></verify>
  <done>Settings, Hjem and Uke consume one entitlement/availability/place/cache contract and only the implemented automatic capability becomes claimable.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve exact current and future Outfit contexts and storage privacy</name>
  <files>src/App.tsx, src/screens/HjemScreen.tsx, src/screens/PaakledningScreen.tsx, e2e/planlegg.ts</files>
  <read_first>src/App.tsx, src/screens/HjemScreen.tsx, src/screens/PaakledningScreen.tsx, src/screens/UkeScreen.tsx, src/state/location-pref-store.ts, src/hooks/useWeather.ts, e2e/planlegg.ts, e2e/fixtures/planlegg.ts</read_first>
  <action>Extend Hjem's existing `onOpenSheet` transient payload to include the exact evaluated place label/coordinates/source, weather snapshot, finalized recommendation and activity/vogn from the same render. App stores that discriminated current payload in React memory and passes it to PaakledningScreen. When present, Paakledning renders exclusively from it and does not call `useWeather` or reread the active child; preview/test mounts without it may retain the explicit legacy fallback. Preserve the separate future PlannedOutfitContext branch from 01-06.

Implement `automatic-location` E2E with deterministic entitlement/GPS/geocode/weather fixtures and seeded fixed child. Prove no I/O while refresh/loading/flag-off/manual, explicit Settings activation, one stable success, stale/denied/downgrade/switch/unmount/reload behavior, exact source labels, and child JSON byte equality. Snapshot all localStorage keys/bytes: automatic paths create/read no `metno:*`/`nominatim:*` key, while a separate fixed/manual fixture still exercises persistent caching. Open current Hjem Outfit and future Uke Outfit after place B succeeds and assert each retains its exact evaluated place/weather/source/recommendation even if active display later changes to A. Rerun access/exact-context. Assert DOM/storage/call cardinality/focus only and write no media.</action>
  <verify><automated>cmd.exe /d /s /c "npm run build && npx tsx e2e/planlegg.ts --case automatic-location && npx tsx e2e/planlegg.ts --case access && npx tsx e2e/planlegg.ts --case exact-context"</automated></verify>
  <done>Automatic location is truthful end to end, persistent storage remains clean, and neither current nor future Outfit mixes evaluation contexts.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat | Mitigation / disposition |
|---|---|
| Cached preference bypasses entitlement/flag | Generic runtime resolver before every I/O entry. |
| Late result mutates another child/state | Generation, child and live-decision rechecks. |
| Automatic location becomes tracking | Explicit foreground one-shot intents only. |
| Coordinates leak through caches/storage | Memory-only/no-store path and byte/key assertions. |
| Outfit combines place B garments with place A weather | Same-render immutable current/future context payloads. |
</threat_model>

<verification>
- Access/store/hook/cache/capability/paywall tests, automatic-location/access/exact-context E2E and full test/lint/build pass.
- Fresh high-risk verifier PASS binds to exact immutable integration SHA; native permission/device/media gates remain Pending.
</verification>

<rollback>
Set automatic_location availability false first, then revert `feat: enforce privacy-safe Plus automatic location`; fixed/manual behavior from 01-11 remains valid and all entry points fail closed.
</rollback>

<success_criteria>
- Automatic location performs no unauthorized I/O or persistent coordinate/cache write.
- Hjem/Uke and current/future Outfit use exact consistent place/weather/source.
- Fixed home is the authoritative Free/loading/downgrade/restart fallback.
</success_criteria>

<output>
Create `01-12-SUMMARY.md` with intent/access/cancellation matrix, storage key+byte evidence, current/future Outfit parity, exact candidate SHA, reviewer identity/lane and PASS/BLOCK verdict bound to that SHA; native/media/device/release remain Pending.
</output>
