---
phase: 01-planlegg-dagslinjen
plan: "07"
type: execute
wave: 7
package_wave: 4
depends_on: ["01-06"]
files_modified:
  - src/components/planning/PlanleggStatusNotice.tsx
  - src/components/planning/ForecastDisclosure.tsx
  - src/screens/UkeScreen.tsx
  - src/screens/UkeScreen.css
  - e2e/planlegg.ts
autonomous: true
requirements: [UI-02, A11Y-01, EVID-01, GOV-04]
must_haves:
  truths:
    - "App owns the only main and vertical page scroll; Planlegg is one natural-height section."
    - "Order is title, child/place/currentness, view control, verdict/next action, dominant rail, secondary weather disclosure."
    - "UkeScreen preserves the same-pass ReadonlyMap and passes PlanChangeRail exactly `onOpenOutfit(eventId, trigger)`; stale/missing map entries never show a CTA."
    - "Loading/error/offline/partial/empty/ready states are truthful; duplicate rows/cards/pills/dead controls are removed."
    - "Browser checks visible interaction/focus/keyboard/reflow only, never haptic counts or media."
  artifacts:
    - path: "src/screens/UkeScreen.tsx"
      provides: "Single-flow Dagslinjen-led Planlegg composition"
    - path: "src/components/planning/PlanleggStatusNotice.tsx"
      provides: "Truthful status/recovery surface"
      exports: ["PlanleggStatusNotice"]
    - path: "src/components/planning/ForecastDisclosure.tsx"
      provides: "Secondary weather-only disclosure"
      exports: ["ForecastDisclosure"]
  key_links:
    - from: "src/screens/UkeScreen.tsx"
      to: "src/lib/planning/plan-view-model.ts"
      via: "one pure aggregate model plus same-pass context map"
      pattern: "buildPlanViewModel"
    - from: "src/screens/UkeScreen.tsx"
      to: "src/components/planning/PlanChangeRail.tsx"
      via: "controlled selection and event-ID opener"
      pattern: "onOpenOutfit"
  prohibitions:
    - "MUST NOT add nested main/100dvh/inner vertical scroll, duplicate garment timeline, burden pills, mega-cards, dead place/bell controls or generic CTA."
    - "MUST NOT show Snart before its approved implementation or change access/recommendation truth."
    - "MUST NOT add browser haptic instrumentation, packages/APIs/schemas/media or a new design system."
---

<objective>
Rebuild Planlegg around the semantic Dagslinjen while preserving exact-context map ownership.

Purpose: Replace the card-heavy duplicate layout with one calm native flow on Babyora's existing canvas.
Output: Status/forecast primitives, composed screen/CSS and non-media state/reflow evidence.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-planlegg-dagslinjen/01-06-SUMMARY.md
@.planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md
@src/App.tsx
@src/screens/UkeScreen.tsx
@src/components/planning/PlanChangeRail.tsx
@src/components/controls/SegmentedControl.tsx
@src/lib/planning/plan-view-model.ts
@src/lib/planning/planning-interaction.ts
@src/styles/design-tokens.css
</context>

<delivery_contract>
- Risk/model: standard screen composition/accessibility; Claude Sonnet 5, High effort.
- Candidate commit: `feat: rebuild Planlegg around Dagslinjen`.
- Reviewers: fresh Sonnet 5 High standard verifier plus a separate fresh high-risk exact-context/map regression reviewer on the same SHA because UkeScreen owns the guarded map; pure truth/context modules remain independently approved inputs.
- Evidence boundary: deterministic DOM/computed-style/keyboard/browser interaction only; no media/physical claim.
</delivery_contract>

<tasks>

<task type="auto">
  <name>Task 1: Build truthful status and weather-only disclosure primitives</name>
  <files>src/components/planning/PlanleggStatusNotice.tsx, src/components/planning/ForecastDisclosure.tsx, src/screens/UkeScreen.css</files>
  <read_first>.planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md, src/lib/planning/plan-view-model.ts, src/lib/met-no/types.ts, src/styles/design-tokens.css, src/components/Skeleton.tsx</read_first>
  <action>Create a pure discriminated PlanleggStatusNotice for static loading skeleton, no-cache error/retry, cached offline timestamp and partial evidence; ready returns null. It is the single Planlegg `aria-live="polite"`/status owner: announce each meaningful refresh, error or offline state once, never narrate every visual/temperature/rail transition, and avoid nested/competing live regions. Create controlled ForecastDisclosure with native aria-expanded, exact open/close copy and weather-only rows. Add scoped natural-flow CSS using existing tokens, 44px/focus-visible, forced colors, wrapping and reduced-motion.</action>
  <verify><automated>cmd.exe /d /s /c "npm run lint && npm run build"</automated></verify>
  <acceptance_criteria>
    - Copy matches UI-SPEC and no state fabricates advice.
    - Exactly one Planlegg live/status owner announces meaningful refresh/error/offline changes once; ordinary visual animation, temperature and selection transitions are not narrated.
    - Forecast contains no garments/recommendation counts.
    - CSS has no fixed content height, 100dvh, inner overflow-y or new palette/font.
  </acceptance_criteria>
  <done>Recovery/currentness and secondary forecast are ready for composition.</done>
</task>

<task type="auto">
  <name>Task 2: Compose the one-scroll hierarchy and preserve map lookup</name>
  <files>src/screens/UkeScreen.tsx, src/screens/UkeScreen.css</files>
  <read_first>src/screens/UkeScreen.tsx, src/screens/UkeScreen.css, src/App.tsx, src/lib/planning/plan-view-model.ts, src/lib/planning/planned-outfit-context.ts, src/lib/planning/planning-interaction.ts, src/components/planning/PlanChangeRail.tsx, src/components/planning/PlanleggStatusNotice.tsx, src/components/planning/ForecastDisclosure.tsx, src/components/controls/SegmentedControl.tsx</read_first>
  <action>Use a labelled section/div, not main. Render h1/context, current two-view control (I dag/Uke; Snart omitted), verdict/action, controlled rail, then forecast. Preserve the atomic canonical evaluated points/view model/ReadonlyMap and exact resolver-to-trusted-App callback from 01-06; do not create an alternate lookup. Select a deterministic valid event and, when refresh removes it, repair selection deterministically without moving current keyboard focus or emitting a cue. Pass rail `onSelect(nextId)` as a controlled state update with no Uke event haptic, and pass its existing local `onOpenOutfit(eventId, trigger)` resolver callback. Use the Uke interaction decision only for actual user-requested view/date changes and dispatch `selection` only when that value really changes; the rail continues to own expansion/collapse intent and its light cue. Remove old topbar, four-sample `Time for time`, phase cards/rows, burden bands, CTA and nested scroll/main. Preserve shared temperature axis using selected valid planned context or current valid weather.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/lib/planning/__tests__ src/components/planning/__tests__/PlanChangeRail.test.tsx && npm run lint && npm run build"</automated></verify>
  <acceptance_criteria>
    - Runtime has one app main, one h1, required order and one garment timeline.
    - Map and events share one evaluation pass; missing map entry removes/refuses CTA.
    - Refresh removal chooses the deterministic next valid event or null without moving keyboard focus or firing an interaction cue.
    - No generic contextId/DTO prop reaches PlanChangeRail.
    - Snart is absent until approved/implemented.
  </acceptance_criteria>
  <done>Dagslinjen is the dominant truthful composition with exact-context ownership intact.</done>
</task>

<task type="auto">
  <name>Task 3: Verify state matrix, focus, keyboard and reflow</name>
  <files>e2e/planlegg.ts</files>
  <read_first>e2e/planlegg.ts, e2e/fixtures/planlegg.ts, src/screens/UkeScreen.tsx, src/screens/UkeScreen.css, src/App.tsx</read_first>
  <action>Implement `composition` for ready zero/one/many, loading, error/retry, cached offline and partial. At 390x844 and 200% assert one main/scroll owner, required order, rail before forecast, obsolete UI absent, text wrapping/no horizontal overflow, keyboard focus and reduced-motion final state. Cover light and dark modes plus mild, warm, extreme heat, cold and extreme cold temperature themes and forced-colors; compute resolved foreground/background pairs for normal text, large text, controls, focus indicators and dominant action, and fail below WCAG AA rather than making a subjective visual-score claim. Assert exactly one Planlegg `aria-live="polite"`/status owner, one announcement per meaningful refresh/error/offline change, and no announcement for ordinary animation/temperature/selection transitions. Refresh away the selected event and assert deterministic replacement/null while the currently focused element remains focused. Visit all four roots for shell regression. Rerun `exact-context` because this candidate rewrites UkeScreen/map composition. Do not inspect haptic calls or persist media.</action>
  <verify><automated>cmd.exe /d /s /c "npm run build && npx tsx e2e/planlegg.ts --case composition && npx tsx e2e/planlegg.ts --case exact-context"</automated></verify>
  <acceptance_criteria>
    - All named states and four roots pass.
    - Live-region ownership and announcement suppression match the UI-SPEC contract.
    - Light/dark, mild/warm/extreme-heat/cold/extreme-cold and forced-colors computed text/control/focus/action contrast meet WCAG AA; subjective 90+ visual judgment remains Pending.
    - Refresh selection repair leaves keyboard focus unchanged, and the exact-context regression remains green.
    - Browser checks only visible DOM/state/focus/keyboard/geometry.
    - No media/file output occurs.
  </acceptance_criteria>
  <done>The rebuilt composition is structurally proven without a premature visual/tactile score.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat | Mitigation / disposition |
|---|---|
| UI overstates stale/partial data | Discriminated status from pure model. |
| Stale map CTA opens wrong advice | Current map membership controls availability and lookup. |
| Nested scroll traps input/AT | App main is sole scroll owner. |
| Standard styling alters high-risk truth | Approved truth/context modules remain read-only inputs. |
</threat_model>

<artifacts_this_phase_produces>
- New status/forecast components, scoped Planlegg CSS and rebuilt UkeScreen composition.
- E2E composition case; no access/Snart/package/API/schema/media artifact.
</artifacts_this_phase_produces>

<verification>
- Planning/component tests, composition E2E and full test/lint/build pass.
- Fresh standard review and separate high-risk exact-context/map regression review PASS the same exact SHA.
</verification>

<rollback>
Revert `feat: rebuild Planlegg around Dagslinjen` together; keep prior truth/context/rail candidates.
</rollback>

<success_criteria>
- One-scroll hierarchy, state matrix, exact lookup and reflow pass.
- Obsolete duplicate/card UI is absent.
- Standard PASS exists; access/Snart/media/device/release continue Pending.
</success_criteria>

<output>
Create `01-07-SUMMARY.md` with DOM/scroll/state/live-region evidence, exact candidate SHA, both the standard and separate high-risk map/context reviewer identities and lanes, each PASS/BLOCK verdict bound to that same SHA, Uke map/context invariants, the no-media result, and all remaining Pending gates.
</output>
