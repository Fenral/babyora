---
phase: 01-planlegg-dagslinjen
plan: "08"
type: tdd
wave: 8
package_wave: 5
depends_on: ["01-07"]
files_modified:
  - src/lib/access/capabilities.ts
  - src/lib/access/__tests__/capabilities.test.ts
  - src/lib/premium/gating.ts
  - src/lib/premium/__tests__/gating.test.ts
  - src/lib/premium/plus-features.ts
  - src/lib/premium/__tests__/plus-features.test.ts
  - src/hooks/useAutoLocationRefresh.ts
  - src/screens/InnstillingerScreen.tsx
  - e2e/fixtures/planlegg.ts
  - e2e/planlegg.ts
autonomous: true
requirements: [ACCESS-01, EVID-02, GOV-04]
must_haves:
  truths:
    - "`decideAccess` and `resolvePlanningViewAccess` become the authoritative pure policy/resolver contracts; loading is neutral and never emits a paywall trigger, while runtime Planlegg presentation wiring remains deferred to 01-10 and full automatic-location integration/enablement remains in 01-12."
    - "Today resolves full for Free; Week resolves teaser for Free/full for eligible Plus; Snart remains hidden/unavailable until approved implementation."
    - "Runtime claims are the intersection of access decision and implemented availability, never a screen-local isPremium boolean."
    - "A generic `resolveRuntimeCapabilityAccess` intersects `decideAccess` with runtime implementation availability so a rollback/disabled automatic-location flag denies before any entry point can perform permission, GPS, geocode, forecast or child-update I/O."
    - "`shouldShowTenDayTeaser` remains as a tested compatibility wrapper until UkeScreen migrates atomically in 01-10; it delegates to the new resolver and is not removed early."
    - "`automatic_location` availability remains false throughout this containment candidate and until Plan 01-12 safely integrates the full controller/place lifecycle and passes its own gate; family/calibration/Snart remain false."
    - "While `automatic_location` is false, the existing App-called refresh hook and Settings activation consume the same runtime decision: loading/denied/unavailable/manual startup paths perform zero location I/O, an already-auto preference can still be turned off, and manual cannot be activated to auto."
    - "A deterministic `location-containment` browser case seeds stored auto before startup, exercises Settings off and blocked reactivation, proves zero geolocation/geocode/forecast calls, and preserves the exact child-storage bytes without screenshot, video or trace output."
  artifacts:
    - path: "src/lib/premium/gating.ts"
      provides: "Pure capability-derived planning view resolver"
      exports: ["resolveRuntimeCapabilityAccess", "resolvePlanningViewAccess", "shouldShowTenDayTeaser"]
    - path: "src/lib/access/capabilities.ts"
      provides: "Central policy including reserved soon_preparation"
      exports: ["decideAccess"]
    - path: "src/hooks/useAutoLocationRefresh.ts"
      provides: "Minimal fail-closed containment for the existing App startup/resume entry point"
    - path: "src/screens/InnstillingerScreen.tsx"
      provides: "Same-decision containment for existing automatic-location activation while preserving auto-to-manual off"
    - path: "e2e/fixtures/planlegg.ts"
      provides: "Invented stored-auto, normalized child-byte and fresh fixed-forecast containment fixture"
      exports: ["PLANLEGG_E2E_FIXTURES"]
    - path: "e2e/planlegg.ts"
      provides: "Deterministic no-media location-containment browser case"
  key_links:
    - from: "src/lib/premium/gating.ts"
      to: "src/lib/access/capabilities.ts"
      via: "generic runtime resolver consumes AccessDecision and intersects implementation availability"
      pattern: "resolveRuntimeCapabilityAccess"
    - from: "src/lib/premium/gating.ts"
      to: "src/lib/premium/plus-features.ts"
      via: "access is intersected with implemented availability"
      pattern: "PlusFeatureAvailability"
    - from: "src/hooks/useAutoLocationRefresh.ts"
      to: "src/lib/premium/gating.ts"
      via: "the hook already called by App resolves automatic_location before its startup/resume effect can perform I/O"
      pattern: "resolveRuntimeCapabilityAccess"
    - from: "src/screens/InnstillingerScreen.tsx"
      to: "src/lib/premium/gating.ts"
      via: "manual-to-auto activation and confirmation recheck the same runtime decision while off remains unconditional"
      pattern: "resolveRuntimeCapabilityAccess"
    - from: "e2e/planlegg.ts"
      to: "e2e/fixtures/planlegg.ts"
      via: "location-containment seeds stored auto, fixed forecast state and exact child bytes before bootstrap"
      pattern: "location-containment"
  prohibitions:
    - "MUST NOT edit UkeScreen, PaywallDialog, App, Hjem/Uke weather consumers, location stores, products/prices, RevenueCat or presentation beyond the minimal Settings containment guard in this candidate."
    - "MUST NOT enable automatic_location, soon_preparation, family_sharing or personal_calibration before their implementation gates."
    - "MUST NOT request permission/GPS, geocode, forecast or call updateChild from either existing automatic-location entry point while access is loading/denied, implementation is unavailable, or startup mode is manual."
    - "MUST NOT implement the 01-12 controller, ephemeral place/cache pipeline, Hjem/Uke/Outfit integration or automatic_location enablement early."
    - "MUST NOT add packages/APIs/schemas/analytics/media."
---

<objective>
Define the centralized Planlegg capability policy and make its false automatic-location availability contain both pre-existing runtime entry points.

Purpose: Make access decisions independently reviewable before Uke/paywall consumes them, while ensuring disabled implementation availability is already an effective no-I/O boundary at App startup/resume and Settings.
Output: Expanded capability matrix, implementation availability, explicit view-access resolver/tests, minimal automatic-location containment and deterministic no-media browser evidence.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/01-planlegg-dagslinjen/01-07-SUMMARY.md
@.planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md
@src/lib/access/capabilities.ts
@src/lib/access/__tests__/capabilities.test.ts
@src/lib/premium/gating.ts
@src/lib/premium/__tests__/gating.test.ts
@src/lib/premium/plus-features.ts
@src/lib/premium/__tests__/plus-features.test.ts
@src/lib/premium/use-access.ts
@src/App.tsx
@src/hooks/useAutoLocationRefresh.ts
@src/screens/InnstillingerScreen.tsx
@src/state/location-pref-store.ts
@src/state/children-store.tsx
@e2e/fixtures/planlegg.ts
@e2e/planlegg.ts
</context>

<delivery_contract>
- Risk/model: high entitlement/access-policy/location-containment risk; Claude Fable 5, Extra effort.
- Candidate commit: `fix: define and enforce truthful Planlegg access policy`.
- Reviewer: fresh Fable 5 Extra or approved Opus plus external read-only review on exact SHA.
- Evidence boundary: pure policy tests plus source/DOM/storage/network-call containment at the two existing automatic-location entry points; full location functionality, RevenueCat, media and physical-device evidence remain untouched/Pending.
</delivery_contract>

<tasks>

<task type="auto">
  <name>Task 1: TDD the centralized capability decision matrix</name>
  <files>src/lib/access/capabilities.ts, src/lib/access/__tests__/capabilities.test.ts</files>
  <read_first>src/lib/access/capabilities.ts, src/lib/access/__tests__/capabilities.test.ts, src/lib/premium/use-access.ts, .planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md</read_first>
  <behavior>
    - Today-home remains free; future_plan, automatic_location and reserved soon_preparation require Plus.
    - Loading denies neutrally with no paywall trigger; Free denial includes its capability trigger; authenticated is not invented for non-auth capabilities.
    - Plus/downngrade/loading transitions are deterministic and do not modify RevenueCat/store state.
  </behavior>
  <action>Extend Capability with `soon_preparation`. Expand table tests for Free/Plus/loading/expired for today_home, future_plan, automatic_location and soon_preparation. Preserve family/auth policy as-is and do not infer authentication. Keep `decideAccess` pure; no store/React import.</action>
  <verify><automated>npx vitest run src/lib/access/__tests__/capabilities.test.ts</automated></verify>
  <acceptance_criteria>
    - Loading has no paywallTrigger; Today is complete for Free.
    - Automatic location and Snart require Plus but policy alone does not mark implementation available.
    - No RevenueCat/subscription mutation exists.
  </acceptance_criteria>
  <done>The central pure access decision covers every bounded capability state; runtime consumers remain unchanged and automatic location remains disabled pending 01-12.</done>
</task>

<task type="auto">
  <name>Task 2: TDD implemented availability and Planlegg view resolution</name>
  <files>src/lib/premium/gating.ts, src/lib/premium/__tests__/gating.test.ts, src/lib/premium/plus-features.ts, src/lib/premium/__tests__/plus-features.test.ts</files>
  <read_first>src/lib/premium/gating.ts, src/lib/premium/__tests__/gating.test.ts, src/lib/premium/plus-features.ts, src/lib/premium/__tests__/plus-features.test.ts, src/lib/access/capabilities.ts</read_first>
  <behavior>
    - `resolveRuntimeCapabilityAccess(capability, context, flags)` returns an allow/deny/neutral decision that cannot allow an unavailable implementation.
    - `resolvePlanningViewAccess(today|week|soon, context, flags)` returns exactly neutral/full/teaser/hidden plus capability/decision.
    - Today is full; Week loading neutral, Free teaser, Plus+future flag full; Soon hidden while flag false.
    - Claims/availability remain false for automatic_location until 01-12, soon until 01-16, family/calibration throughout this phase.
    - `shouldShowTenDayTeaser` remains callable with its current signature and delegates to the new view resolver until 01-10 migrates UkeScreen.
  </behavior>
  <action>Add pure generic `resolveRuntimeCapabilityAccess(capability, accessContext, availability)` that first obtains `decideAccess`, then fails closed when the capability implementation flag is absent/false, preserving neutral loading without a paywall trigger. Build `resolvePlanningViewAccess` on that generic seam. Retain `shouldShowTenDayTeaser` with its exact current signature as a compatibility wrapper that delegates to the new resolver until UkeScreen migrates in 01-10; do not remove or break the existing import in this policy-only candidate. Preserve unrelated child gating. Add `soon_preparation` to availability false and keep automatic_location false until its split foundation/integration candidates pass. Tests prove unavailable implementation wins over entitlement, rollback to false denies automatic location generically, and no resolver output is derived from only isPremium.</action>
  <verify><automated>npx vitest run src/lib/premium/__tests__/gating.test.ts src/lib/premium/__tests__/plus-features.test.ts</automated></verify>
  <acceptance_criteria>
    - Policy and implementation availability are separate inputs.
    - Disabled family/automatic-location/Snart claims cannot be returned.
    - Compatibility-wrapper tests remain green against the old Uke signature; its removal is owned by 01-10 after the live callsite migrates.
    - Child-switch behavior/tests remain unchanged.
  </acceptance_criteria>
  <done>Presentation can consume one pure truthful access contract without ad-hoc booleans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Contain both existing automatic-location entry points while availability is false</name>
  <files>src/hooks/useAutoLocationRefresh.ts, src/screens/InnstillingerScreen.tsx, e2e/fixtures/planlegg.ts, e2e/planlegg.ts</files>
  <read_first>src/App.tsx, src/hooks/useAutoLocationRefresh.ts, src/screens/InnstillingerScreen.tsx, src/lib/premium/gating.ts, src/lib/premium/use-access.ts, src/lib/premium/plus-features.ts, src/state/location-pref-store.ts, src/state/children-store.tsx, src/hooks/useWeather.ts, e2e/fixtures/planlegg.ts, e2e/planlegg.ts</read_first>
  <behavior>
    - The existing hook invocation in App remains unchanged; `useAutoLocationRefresh` consumes fresh `useAccess` state plus `resolveRuntimeCapabilityAccess('automatic_location', ..., PLUS_FEATURE_AVAILABILITY)` and returns before listener/permission/GPS/geocode/updateChild work when loading, denied, unavailable or mode is manual.
    - Settings derives that same runtime decision. An already-auto preference can always switch to manual, but a manual preference cannot open an enabled activation path, become auto or perform permission/GPS/geocode/updateChild/forecast I/O while availability is false; confirmation rechecks the decision before I/O.
    - `location-containment` seeds exact invented child bytes, stored auto mode and a fresh fixed-home forecast fixture before startup, then covers startup/resume, Settings off and blocked manual-to-auto attempts with zero permission/geolocation/geocode/forecast calls and byte-identical child storage.
  </behavior>
  <action>Write the failing no-media browser case first. In the App-called hook, derive an `AccessContext` from `useAccess()` without inventing authentication, resolve `automatic_location` through the generic runtime resolver and current availability object, and place the fail-closed check before effect listener registration or any call that can reach permission, `navigator.geolocation`, `reverseGeocode`, forecast refresh or `updateChild`. In Settings, derive the identical runtime decision, keep the `next === false` auto-to-manual branch before the denial guard, block manual-to-auto before opening/confirming the permission flow, and recheck immediately on confirmation before any I/O. Keep `automatic_location:false`; do not build the cancellable controller, memory-only place pipeline or enablement owned by 01-12.

Extend `PLANLEGG_E2E_FIXTURES` and the case dispatcher with `location-containment`. Install permission/geolocation and MET/Nominatim counters before app bootstrap, seed a fresh fixed-home forecast so normal Hjem rendering produces no unrelated network baseline, preserve a parser-normalized raw `babyora:children:v2` string in the provider's serialization order, and seed `babyora.location-pref` as auto. Assert startup plus a foreground/resume signal produce zero calls, navigate to Settings, prove auto-to-manual still works, attempt manual-to-auto/confirmation, and assert mode remains manual, every counter remains zero and the child string is exactly unchanged. Use invented data and DOM/storage/call-cardinality assertions only; create no screenshot, video, trace or other media artifact.</action>
  <verify><automated>cmd.exe /d /s /c "npm run build && npx tsx e2e/planlegg.ts --case location-containment"</automated></verify>
  <acceptance_criteria>
    - Both existing runtime entry points call `resolveRuntimeCapabilityAccess`; neither uses only `isPremium`, stored mode or a screen-local Boolean to authorize automatic location.
    - Seeded stored auto cannot bypass loading/denied/unavailable checks at startup or resume, and child JSON bytes never change.
    - Settings can turn auto off but cannot turn manual on while implementation availability is false; both the toggle and confirm paths fail before external I/O.
    - The deterministic case reports zero permission/geolocation/geocode/forecast calls and writes no screenshot, video, trace or other media.
  </acceptance_criteria>
  <done>False automatic-location availability is an enforced no-I/O runtime boundary now; Plan 01-12 still owns the controller, privacy-safe place pipeline, exact contexts and eventual enablement.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat | Mitigation / disposition |
|---|---|
| Entitlement loading flashes content | Neutral result with no paywall trigger. |
| Implemented claim outruns code | Explicit availability intersection and false-by-default flags. |
| Cached stored auto bypasses false availability at App startup/resume | The existing hook resolves runtime access before listeners or I/O; seeded-auto browser counters remain zero. |
| Settings opens a permission race after denial/rollback | Toggle and confirm use the same live decision; off is unconditional and activation fails before I/O. |
| Screen bypasses policy | Both existing entry points consume the single resolver and deterministic browser evidence checks effects. |
| Policy mutates billing | Pure modules; RevenueCat/useAccess read-only and out of files. |
</threat_model>

<artifacts_this_phase_produces>
- Reserved soon_preparation capability and pure resolvePlanningViewAccess contract/tests.
- Minimal fail-closed automatic-location containment in the existing hook and Settings plus one deterministic no-media browser case; no functional enablement or location-data model is added.
- No product/pricing, package, API, schema, analytics or media artifact.
</artifacts_this_phase_produces>

<verification>
- Access/gating/availability tests, exact `location-containment` build/browser command and full test/lint/build pass.
- Fresh high-risk reviewer verifies the exact policy-plus-containment candidate SHA; 01-12 requires its own later exact-SHA review and is not pre-passed here.
</verification>

<rollback>
Keep `automatic_location` false. Revert `fix: define and enforce truthful Planlegg access policy` only to a candidate that independently preserves an equivalent no-I/O guard at both existing entry points; never roll back the containment alone or restore the unsafe stored-auto startup/Settings path.
</rollback>

<success_criteria>
- Today/Week/Soon/loading/access/availability matrix is deterministic.
- Location/Snart/family remain unavailable until dedicated plans, and false automatic-location availability contains both existing entry points with zero I/O and unchanged child bytes.
- Exact-SHA high-risk PASS exists; media/device/release remain Pending.
</success_criteria>

<output>
Create `01-08-SUMMARY.md` with access matrix, false capability flags, both entry-point guards, `location-containment` permission/geolocation/geocode/forecast zero-call counts, child-byte equality, exact candidate SHA, reviewer identity/lane, PASS/BLOCK verdict bound to that SHA, and all remaining Pending gates including the full 01-12 location implementation.
</output>
