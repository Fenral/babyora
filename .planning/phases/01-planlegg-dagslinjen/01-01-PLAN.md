---
phase: 01-planlegg-dagslinjen
plan: "01"
type: execute
wave: 1
package_wave: 0
depends_on: []
files_modified:
  - src/lib/planning/__tests__/planlegg-fixtures.ts
  - src/lib/planning/__tests__/forecast-evidence.test.ts
  - src/lib/planning/__tests__/timezone.test.ts
  - src/lib/planning/__tests__/plan-view-model.test.ts
  - src/lib/planning/__tests__/planned-outfit-context.test.ts
  - src/lib/planning/__tests__/planning-interaction.test.ts
  - src/lib/planning/__tests__/snart.test.ts
  - e2e/fixtures/planlegg.ts
  - e2e/planlegg.ts
autonomous: true
requirements: [GOV-01, GOV-02, EVID-01, GOV-03, GOV-04]
must_haves:
  truths:
    - "Frozen 390x844, Europe/Oslo, clock, weather, child, entitlement and location fixtures are deterministic and contain no real user data."
    - "The exact Wave-0 forecast, timezone, view-model, planned-context, interaction and Snart contracts listed in Task 1 have named pending tests before their production work, but TODOs are never counted as PASS evidence; access/paywall and location-race contracts are established in their owning later plans."
    - "The browser harness is case-selectable and writes no screenshot, video, trace or other app media."
    - "Product Waves 0-6, standard/high-risk commits and fresh immutable-SHA review remain separate."
  artifacts:
    - path: "src/lib/planning/__tests__/planlegg-fixtures.ts"
      provides: "Frozen truth/context/access/Snart fixtures"
      exports: ["PLANLEGG_CLOCK_ISO", "PLANLEGG_TIME_ZONE", "planningForecastFixture", "planningChildFixture"]
    - path: "e2e/fixtures/planlegg.ts"
      provides: "Non-media browser fixture registry"
      exports: ["PLANLEGG_E2E_FIXTURES"]
    - path: "e2e/planlegg.ts"
      provides: "Case-selectable executable browser harness"
  key_links:
    - from: "e2e/planlegg.ts"
      to: "e2e/fixtures/planlegg.ts"
      via: "--case fixture lookup"
      pattern: "PLANLEGG_E2E_FIXTURES"
  prohibitions:
    - "MUST NOT modify production app code, package.json, dependencies, APIs, schemas, recommendations, pricing, RevenueCat, analytics or unrelated files."
    - "MUST NOT capture/persist screenshots, video, trace screenshots, store assets or marketing media while implementation changes."
    - "MUST NOT cite TODOs, harness boot or executor output as independent review PASS."
---

<objective>
Freeze deterministic tests and the no-media evidence boundary before production changes.

Purpose: Make every later TDD slice reproducible without crossing the deferred visual gate.
Output: Shared fixtures, named pending contracts and a no-write browser harness.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md
@.planning/phases/01-planlegg-dagslinjen/01-VALIDATION.md
@.planning/phases/01-planlegg-dagslinjen/01-EDGE-REQUIREMENTS.json
@docs/PROSESS-PLAN-TIL-KODE.md
@e2e/smoke.ts
@package.json
</context>

<delivery_contract>
- Risk/model: standard test infrastructure; Claude Sonnet 5, High effort.
- Candidate commit: `test: freeze Planlegg delivery contracts`.
- Reviewer: fresh Sonnet 5 High plus plan/UI checker on the immutable candidate SHA.
- Evidence boundary: fixture/unit/DOM interaction only; physical-device, media, 90+ and release gates stay `Pending`.
- Baseline source SHA: `e669afff0259750840393e8475f84afbc7937352`.
</delivery_contract>

<tasks>

<task type="auto">
  <name>Task 1: Freeze deterministic fixtures and pending contracts</name>
  <files>src/lib/planning/__tests__/planlegg-fixtures.ts, src/lib/planning/__tests__/forecast-evidence.test.ts, src/lib/planning/__tests__/timezone.test.ts, src/lib/planning/__tests__/plan-view-model.test.ts, src/lib/planning/__tests__/planned-outfit-context.test.ts, src/lib/planning/__tests__/planning-interaction.test.ts, src/lib/planning/__tests__/snart.test.ts</files>
  <read_first>.planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md, .planning/phases/01-planlegg-dagslinjen/01-VALIDATION.md, .planning/phases/01-planlegg-dagslinjen/01-EDGE-REQUIREMENTS.json, src/lib/met-no/types.ts, src/lib/planning/change-events.ts, src/state/children-store.tsx</read_first>
  <action>Create one UTF-8 frozen fixture module with normal-day and DST-boundary clocks, fresh/cached/stale/invalid forecast variants, TTL/TTL+1 timestamps, absent/invalid `updated_at`, long Norwegian/Unicode labels, finite coordinates, finalized recommendations and Free/Plus/loading access. Add named `it.todo` contracts for forecast evidence, atomic hook resolution, view states, exact context, interaction decisions and Snart rule tracing. Do not import missing production modules. Snart TODOs explicitly reference the future approved-rule gate and the exact `ready | empty | unavailable` states.</action>
  <verify><automated>npx vitest run src/lib/planning/__tests__/forecast-evidence.test.ts src/lib/planning/__tests__/timezone.test.ts src/lib/planning/__tests__/plan-view-model.test.ts src/lib/planning/__tests__/planned-outfit-context.test.ts src/lib/planning/__tests__/planning-interaction.test.ts src/lib/planning/__tests__/snart.test.ts</automated></verify>
  <acceptance_criteria>
    - Fixtures use explicit ISO instants and `Europe/Oslo`; no contract depends on `Date.now()`.
    - The seven listed Wave-0 test paths exist with named TODOs and invented data only; this plan makes no claim that every later access/location regression test already exists.
    - Snart fixtures contain no invented garment rules or unapproved rule IDs.
  </acceptance_criteria>
  <done>The named Wave-0 forecast/timezone/view/context/interaction/Snart contracts exist as deterministic pending tests without pretending implementation has passed; later access/location tests remain owned by their dedicated plans.</done>
</task>

<task type="auto">
  <name>Task 2: Add the case-selectable no-media browser harness</name>
  <files>e2e/fixtures/planlegg.ts, e2e/planlegg.ts</files>
  <read_first>e2e/smoke.ts, e2e/purchase-flow.ts, package.json, .planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md</read_first>
  <action>Follow the repository's executable TypeScript Playwright pattern: start `vite preview` on an isolated strict port, use 390x844 and Europe/Oslo, choose a stable fixture with `--case`, collect browser/console/network failures, and close browser/server in `finally`. Implement only `harness`: boot the demo seed, assert one app-owned `main` and Hjem/Planlegg/Guide/Familie, then print case/fixture IDs. Do not add npm scripts or any filesystem/media writer.</action>
  <verify><automated>cmd.exe /d /s /c "npm run build && npx tsx e2e/planlegg.ts --case harness"</automated></verify>
  <acceptance_criteria>
    - Unknown cases fail with the supported case names.
    - Harness success requires DOM assertions, not merely a successful boot.
    - Static search finds no screenshot, recordVideo, tracing, audit prepare/finalize or payload logging call.
  </acceptance_criteria>
  <done>A deterministic no-media browser harness is ready for later cases.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat | Mitigation / disposition |
|---|---|
| Fixtures leak child/location data | Invented values only; no user storage reads. |
| Harness persists forbidden media/secrets | No writer/media APIs or environment dump; static check. |
| TODO or executor output is called PASS | Explicit non-evidence rule and fresh immutable-SHA reviewer. |
| Work escapes scope | Exact file list and unchanged unrelated untracked files. |
</threat_model>

<artifacts_this_phase_produces>
- Nine new test/harness files listed above and their named fixture exports.
- No production export, persistence key, package, API, schema or media artifact.
</artifacts_this_phase_produces>

<verification>
- Focused TODO suites and `--case harness` pass without media output.
- `rg -n "screenshot|recordVideo|tracing|audit:prepare|audit:finalize" e2e/planlegg.ts e2e/fixtures/planlegg.ts` finds no executable capture call.
- Fresh standard reviewer verifies the exact candidate SHA; executor records no PASS.
</verification>

<rollback>
Revert only `test: freeze Planlegg delivery contracts`; it contains no production or data migration.
</rollback>

<success_criteria>
- Deterministic pending contracts and no-media harness exist.
- Product Waves 0-6 and risk lanes remain enforceable.
- Physical-device/media/90+/release gates remain explicitly Pending.
</success_criteria>

<output>
Create `01-01-SUMMARY.md` with fixture IDs/commands, exact candidate SHA, each reviewer/checker identity and lane, PASS/BLOCK verdict bound to that SHA, and all remaining Pending human/media/device/release gates.
</output>
