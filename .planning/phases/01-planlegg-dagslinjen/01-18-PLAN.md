---
phase: 01-planlegg-dagslinjen
plan: "18"
type: execute
wave: 18
package_wave: 6
depends_on: ["01-17"]
files_modified:
  - src/lib/haptics/system.ts
  - src/lib/haptics/__tests__/system.test.ts
  - src/components/BottomTabBar.tsx
  - src/components/BottomTabBar.css
  - src/components/__tests__/BottomTabBar.test.tsx
  - e2e/planlegg.ts
autonomous: true
requirements: [A11Y-01, UI-02, EVID-01, EVID-02, GOV-04, GOV-05, GOV-06]
must_haves:
  truths:
    - "Pure planning interaction tests own selection/light/repeat/collapse decisions; haptic-system unit tests own native mapping, preference-off, reduced-motion independence and web no-op."
    - "Reduced motion removes animation but does not silently disable an enabled native haptic preference; haptics-off is the independent tactile no-op."
    - "Web dispatch is a true no-op with no `navigator.vibrate` fallback; browser E2E never asserts haptic call counts."
    - "Bottom navigation uses native focus-visible behavior, filled/strong active icon and label, quiet mint pool and no persistent focus outline after pointer/touch."
    - "The final immutable candidate passes deterministic checks and fresh independent standard/high-risk review, while screenshot/video, physical-device, 90+ and owner release gates remain Pending."
  artifacts:
    - path: "src/lib/haptics/system.ts"
      provides: "Native-only preference-aware haptic dispatcher"
    - path: "src/lib/haptics/__tests__/system.test.ts"
      provides: "Platform/preference/motion mapping evidence"
    - path: "src/components/BottomTabBar.tsx"
      provides: "Shared native-feeling four-root navigation"
    - path: "e2e/planlegg.ts"
      provides: "Native-polish and complete deterministic no-media suite"
  key_links:
    - from: "src/lib/planning/planning-interaction.ts"
      to: "src/lib/haptics/system.ts"
      via: "pure cue decision feeds canonical native-only fire"
      pattern: "selection"
    - from: "src/components/BottomTabBar.tsx"
      to: "src/types/nav.ts"
      via: "four shared root definitions and aria-current"
      pattern: "TAB_DEFS"
  prohibitions:
    - "MUST NOT add browser haptic instrumentation/count assertions, navigator.vibrate, synthetic physical PASS or couple haptics-off to reduced motion."
    - "MUST NOT modify truth/context/access/location/Snart/recommendation behavior, pricing, RevenueCat, packages, APIs or schemas in this standard slice."
    - "MUST NOT run audit prepare/finalize, screenshot helpers or video/trace capture; the user explicitly deferred app media while it may change."
    - "MUST NOT mark physical-device, media-based 90+, final visual or release approval PASS."
    - "MUST NOT claim whole-app UX/Motion Bible completion; Antrekkskart, living Home, signature transfer and cross-surface convergence remain later roadmap phases."
---

<objective>
Finish the native interaction shell and produce the stable deterministic candidate without prematurely creating app media.

Purpose: Make tactile intent and shared navigation correct and independently testable, then verify the whole phase on one immutable SHA.
Output: Native-only haptic system, focus-visible bottom navigation, no-media browser matrix and exact-SHA review packet.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/gsd-core/references/verification-patterns.md
</execution_context>

<context>
@.planning/phases/01-planlegg-dagslinjen/01-17-SUMMARY.md
@.planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md
@.planning/phases/01-planlegg-dagslinjen/01-VALIDATION.md
@docs/BABYORA-UX-MOTION-BIBLE.md
@src/lib/planning/planning-interaction.ts
@src/lib/planning/__tests__/planning-interaction.test.ts
@src/lib/haptics/system.ts
@src/hooks/useNativeSettings.ts
@src/components/BottomTabBar.tsx
@src/types/nav.ts
@e2e/planlegg.ts
</context>

<delivery_contract>
- Risk/model: standard haptic adapter/shared navigation plus final cross-risk verification; Claude Sonnet 5 High for implementation, fresh Fable 5 Extra for final high-risk audit.
- Candidate commit: `fix: complete Planlegg native interaction shell`.
- Reviewers: fresh Sonnet 5 High standard verifier and separate fresh Fable 5 Extra/approved high-risk verifier on the same exact immutable final SHA; external read-only review does not self-fix.
- Evidence boundary: deterministic unit/DOM/browser/source checks only. Media, physical VoiceOver/TalkBack/haptic/text-scale/reach, 90+ and release stay Pending.
</delivery_contract>

<tasks>

<task type="auto">
  <name>Task 1: Make haptic policy native-only, independently configurable and unit-testable</name>
  <files>src/lib/haptics/system.ts, src/lib/haptics/__tests__/system.test.ts</files>
  <read_first>src/lib/haptics/system.ts, src/hooks/useNativeSettings.ts, src/lib/planning/planning-interaction.ts, src/lib/planning/__tests__/planning-interaction.test.ts, package.json</read_first>
  <behavior>
    - One semantic native selection cue maps to the Capacitor `selectionStart → selectionChanged → selectionEnd` sequence; light/medium/success map to the matching primitives.
    - User haptics-off produces zero native dispatch; enabled haptics still dispatch when reduced motion is true.
    - Web/SSR/plugin-unavailable/error paths resolve silently with zero vibrate/native calls.
    - prepare is idempotent and native-only.
  </behavior>
  <action>Refactor around a dependency-injected/testable dispatcher used by the public API. Remove `prefersReducedMotion`, `WEB_VIBRATE_PATTERNS`, `webVibrate` and every navigator vibration call. On non-native platforms return immediately. Map one discrete semantic selection cue to the Capacitor `selectionStart`, `selectionChanged`, `selectionEnd` sequence and other tiers to their existing native primitives; preserve silent failure and close the selection sequence in a finally-safe path. In `useHapticSystem`, guard only on `hapticsOn`; reduced motion remains an animation preference in callers, not a tactile preference. Do not add a browser hook/global. Add unit tests with mocked native adapter/platform/settings for mapping, sequence/cardinality, off, reduced-motion independence, web/SSR, error and prepare behavior. Retain the pure interaction tests from 01-06 for which cues should occur.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/lib/planning/__tests__/planning-interaction.test.ts src/lib/haptics/__tests__/system.test.ts && npx tsc -b --pretty false"</automated></verify>
  <acceptance_criteria>
    - Tests prove selection/light/repeat/collapse decisions separately from platform dispatch.
    - `rg "navigator\.vibrate|WEB_VIBRATE|prefersReducedMotion" src/lib/haptics/system.ts` returns no match.
    - Reduced-motion true plus haptics-on still dispatches one semantic cue with the exact native sequence; haptics-off produces zero native-adapter calls.
    - Web, SSR and plugin-unavailable paths produce zero native-adapter calls and no vibration fallback.
  </acceptance_criteria>
  <done>Haptic intent and delivery are independently correct without browser simulation.</done>
</task>

<task type="auto">
  <name>Task 2: Finish shared focus-visible and active navigation presentation</name>
  <files>src/components/BottomTabBar.tsx, src/components/BottomTabBar.css, src/components/__tests__/BottomTabBar.test.tsx</files>
  <read_first>src/components/BottomTabBar.tsx, src/types/nav.ts, src/styles/design-tokens.css, src/hooks/useNativeSettings.ts, src/lib/haptics/system.ts, .planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md</read_first>
  <action>Move bottom-tab styling to scoped CSS using existing tokens. Remove JavaScript `focused` state and use `:focus-visible` with forced-colors support so pointer/touch leaves no persistent outline. Preserve 44px targets, safe area, aria-current and four root labels. Give the active tab a quiet mint pool, stronger/filled icon treatment and stronger label without relying on color alone. Keep current reduced-motion static branch and selection fire only on actual root change. Export a pure root-change decision plus a dependency-injected callback dispatcher so Node tests prove repeated-current produces zero navigation/cue calls and an actual change produces exactly one of each. Use `renderToStaticMarkup` only for aria-current/four-root/static-indicator markup and inspect scoped CSS for focus-visible/forced-colors geometry; leave actual click, keyboard and focus behavior to the `native-polish` Playwright case. Haptic delivery count remains in system unit tests, not browser.</action>
  <verify><automated>cmd.exe /d /s /c "npx vitest run src/components/__tests__/BottomTabBar.test.tsx src/lib/haptics/__tests__/system.test.ts src/types/__tests__/nav.test.ts && npm run lint && npm run build"</automated></verify>
  <acceptance_criteria>
    - No onFocus/onBlur focus-state outline remains.
    - Active state has icon/weight/pool redundancy; all controls meet 44px and forced-colors focus remains visible.
    - Pure injected-callback tests prove repeated-current no-op and actual-change cardinality; server rendering proves only static markup, while Playwright owns real click/keyboard/focus behavior.
    - Reduced motion changes animation only, not the haptic preference path.
  </acceptance_criteria>
  <done>Shared navigation looks and behaves like a calm native root tab bar.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze and independently verify the complete non-media candidate</name>
  <files>e2e/planlegg.ts</files>
  <read_first>.planning/phases/01-planlegg-dagslinjen/01-VALIDATION.md, e2e/planlegg.ts, e2e/fixtures/planlegg.ts, src/components/BottomTabBar.tsx, src/lib/haptics/system.ts</read_first>
  <action>Implement `native-polish` with visible/keyboard/focus/geometry/reduced-motion assertions for all four roots and no haptic instrumentation. Add fail-fast source-boundary scans proving: the rail transports IDs and availability only; the trusted Uke-to-App `onOpenPlannedOutfit(dto, trigger)` callback is the only planned DTO transport; planned Outfit has no current-context fallback; no production runtime imports/calls the deprecated 01-03 legacy adapters or any canonical-to-legacy projection; no browser-history invention exists; no browser haptic hook or vibration fallback exists; and no unsupported capability/family claim exists. Make `--case all` invoke harness, exact-context, semantic-rail, composition, access, automatic-location, Snart, route-migration and native-polish without media or trace output.

  Run every focused suite, validator, full test/lint/build and all browser cases, then freeze the exact code-candidate SHA. Produce a deterministic text-only independent review packet containing: exact SHA; planned-path and changed-file manifest; complete command/results matrix; UI-SPEC/requirement-to-evidence matrix; the source-boundary scans above; DOM/browser state, semantic, focus, keyboard, 200%-reflow and route matrix; and a no-media artifact scan. `audit:prepare` and `audit:finalize` remain deferred because they create media-oriented audit artifacts. Playwright screenshot, video and trace recording stay off.

  Have a fresh standard UI/code verifier and separate fresh approved high-risk truth/context/access verifier return PASS/BLOCK reports bound to that same exact SHA; external review reports only. A BLOCK permits one documented scoped repair cycle, a new SHA and both fresh reviews. A repeated unresolved disagreement stops and escalates evidence, diff and verdicts to the owner.

  Only after both verdicts PASS, follow the repository `AGENTS.md` post-PASS routine: inspect status and the scoped diff; run diff hygiene and a staged sensitivity scan without printing secret values; leave unrelated user files unstaged; update `docs/CURRENT-HANDOFF.md` in a separate docs-only milestone only if the next action genuinely changes; then commit and push the meaningful approved milestone normally. Record the reviewed code SHA separately from any later summary/handoff commit. Never force-push, rewrite history or delete remote branches.</action>
  <verify><automated>cmd.exe /d /s /c "node scripts/validate-snart-climate.mjs src/data/snart-climate-1991-2020-v1.json .planning/phases/01-planlegg-dagslinjen/01-SNART-CLIMATE-PROVENANCE.md && npm test && npm run lint && npm run build && npx tsx e2e/planlegg.ts --case native-polish && npx tsx e2e/planlegg.ts --case all"</automated></verify>
  <acceptance_criteria>
    - All deterministic cases pass from a clean checkout of one exact SHA with zero media artifact, and the text-only review packet binds every result to that SHA.
    - Browser assertions contain no haptic call count/global/native-bridge hook.
    - Source scans prove the ID-only rail, sole trusted Uke-to-App DTO boundary, no current fallback, no runtime legacy projection, no browser-history invention, no browser haptic fallback and no unsupported capability/family claim.
    - Fresh standard and high-risk PASS both name the final SHA; no P0/P1/truth/access/safety/a11y finding remains.
    - Any repeated executor/verifier disagreement after one documented unsuccessful repair cycle is owner-escalated and leaves the phase unapproved.
    - App media, physical device, 90+ visual score and owner release approval remain explicitly Pending; `audit:prepare` and `audit:finalize` were not run.
    - After dual PASS, the AGENTS status/diff/sensitivity/handoff/meaningful-commit-and-push routine is recorded, with unrelated user files unstaged and no force/history rewrite.
  </acceptance_criteria>
  <done>A stable deterministic candidate exists without misrepresenting deferred human/media gates.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat | Mitigation / disposition |
|---|---|
| Browser vibration masquerades as native quality | Web hard no-op; physical gate remains Pending. |
| Reduced-motion silently disables chosen haptics | Independent settings and unit matrix. |
| Final polish mutates high-risk logic | Narrow file boundary plus fresh final high-risk review. |
| Prior PASS applies to a changed SHA | Both reviewers bind to final immutable SHA; any edit invalidates. |
| Media gate is accidentally run | Explicit prohibited commands and case harness with all recording off. |
</threat_model>

<verification>
- Planning-interaction/haptics/nav tests, all focused phase suites, climate validator, full test/lint/build and all no-media E2E pass.
- Fresh independent standard and high-risk reviewers PASS the same immutable final SHA.
- Physical-device/media-based 90+/owner release gates remain Pending and block final release completion.
</verification>

<rollback>
Revert `fix: complete Planlegg native interaction shell`; retain earlier independently reviewed candidates. If a final audit fails, do not roll forward or claim phase completion: allow one documented scoped repair on a new SHA with both reviews, then escalate the same unresolved disagreement to the owner rather than starting a second autonomous cycle.
</rollback>

<success_criteria>
- Native haptic policy, focus-visible navigation and deterministic browser state are correct.
- Entire phase is green on one exact SHA without app screenshot/video production.
- Deferred physical/media/90+/release gates are honestly Pending.
</success_criteria>

<output>
Create `01-18-SUMMARY.md` with exact reviewed code SHA; planned-path and changed-file manifest; complete deterministic command matrix; UI-SPEC/requirement evidence matrix; source-boundary scans; DOM/browser matrix; no-media artifact scan; both standard/high-risk reviewer identities and separate verdicts bound to that SHA; repair/escalation record if triggered; post-PASS AGENTS routine evidence and any separate documentation commit SHA; and explicit Pending media/device/90+/release gates.
</output>
