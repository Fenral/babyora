---
phase: 01-planlegg-dagslinjen
plan: "02"
type: tdd
wave: 2
package_wave: 1
depends_on: ["01-01"]
files_modified:
  - src/lib/met-no/types.ts
  - src/lib/met-no/client.ts
  - src/lib/met-no/__tests__/client.test.ts
  - src/hooks/useWeather.ts
  - src/hooks/__tests__/useWeather.test.ts
  - src/lib/planning/coverage.ts
  - src/lib/planning/__tests__/forecast-evidence.test.ts
  - src/lib/planning/__tests__/timezone.test.ts
autonomous: true
requirements: [TRUTH-01, EVID-02, GOV-04]
must_haves:
  truths:
    - "Forecast provenance/currentness is explicit for network, fresh cache and validated stale fallback; missing or invalid evidence never becomes current."
    - "The existing cache key and proxy remain; legacy valid cache entries are readable, corrupt entries are ignored, TTL is fresh and TTL+1 is stale."
    - "Only a validated stale cache may be used after network failure; invalid stale data yields unavailable/error."
    - "useWeather unwraps one ForecastFetchResult atomically and a cancelled/older request cannot overwrite a newer fetch key."
    - "Only fresh exact hourly adjacency supports full-span wording; sampled, gapped, stale or unavailable evidence narrows copy."
  artifacts:
    - path: "src/lib/met-no/types.ts"
      provides: "ForecastFetchMetadata and ForecastFetchResult"
      exports: ["ForecastFetchMetadata", "ForecastFetchResult"]
    - path: "src/lib/planning/coverage.ts"
      provides: "Coverage/currentness assessment"
      exports: ["assessForecastCoverage", "formatCoverageCopy"]
    - path: "src/hooks/__tests__/useWeather.test.ts"
      provides: "Atomic unwrap and cancellation contract"
  key_links:
    - from: "src/hooks/useWeather.ts"
      to: "src/lib/met-no/client.ts"
      via: "unwrap one ForecastFetchResult into one accepted request state"
      pattern: "ForecastFetchResult"
    - from: "src/lib/planning/coverage.ts"
      to: "src/lib/met-no/types.ts"
      via: "metadata plus absolute ISO points"
      pattern: "ForecastFetchMetadata"
  prohibitions:
    - "MUST NOT change recommendation rules, cache key, proxy endpoint, packages, APIs, persisted location/child schemas, analytics, pricing or RevenueCat."
    - "MUST NOT call sampled/gapped/stale evidence `time for time`, `hele dagen` or current."
    - "MUST NOT capture screenshots/video/traces."
---

<objective>
Create a separately reviewable truthful forecast-evidence boundary.

Purpose: Ensure later events and copy know exactly what weather evidence exists and how current it is.
Output: Backward-compatible cache/provenance result, atomic weather-hook state and conservative coverage model.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/01-planlegg-dagslinjen/01-01-SUMMARY.md
@.planning/phases/01-planlegg-dagslinjen/01-UI-SPEC.md
@src/lib/met-no/types.ts
@src/lib/met-no/client.ts
@src/lib/met-no/__tests__/client.test.ts
@src/hooks/useWeather.ts
@src/lib/met-no/fixed-hours.ts
@src/lib/met-no/waking-hours.ts
</context>

<delivery_contract>
- Risk/model: high forecast truth/currentness risk; Claude Fable 5, Extra effort.
- Candidate commit: `feat: expose truthful forecast evidence`.
- Reviewer: fresh Fable 5 Extra or approved Opus high-risk verifier plus external read-only review on exact SHA.
- Allowed-path amendment: only the eight listed weather/coverage paths may change to expose minimal metadata and safe stale recovery.
- Evidence boundary: focused unit/full static checks; no app media or self-PASS.
</delivery_contract>

<tasks>

<task type="auto">
  <name>Task 1: TDD backward-compatible cache provenance and stale recovery</name>
  <files>src/lib/met-no/types.ts, src/lib/met-no/client.ts, src/lib/met-no/__tests__/client.test.ts</files>
  <read_first>src/lib/met-no/types.ts, src/lib/met-no/client.ts, src/lib/met-no/__tests__/client.test.ts, src/lib/planning/__tests__/planlegg-fixtures.ts</read_first>
  <behavior>
    - RED: network success; versioned fresh cache; legacy `{ fetchedAt, data }` cache; exact TTL; TTL+1; corrupt JSON/shape; invalid forecast; absent/blank/malformed `properties.meta.updated_at`; network failure with valid/invalid stale cache.
    - GREEN: `fetchForecast` returns `{ forecast, metadata }`; metadata has source, nullable validated sourceUpdatedAt, fetchedAt, cache status and stale flag.
    - GREEN: read and validate fresh and stale candidates without changing `metno:{lat},{lon}` key; stale fallback occurs only after failed network and never for invalid data.
  </behavior>
  <action>Replace the focused TODOs with assertions and capture RED first. Validate cache envelope, finite `fetchedAt`, forecast meta/timeseries shape and every used numeric/time field before classifying it. Treat age `<= CACHE_TTL_MS` as fresh and `CACHE_TTL_MS + 1` as stale. Preserve valid legacy entries; a missing/invalid `updated_at` becomes `sourceUpdatedAt: null` and can never support a currentness claim. Network success writes a compatible validated envelope under the same key. Do not log cache contents.</action>
  <verify><automated>npx vitest run src/lib/met-no/__tests__/client.test.ts</automated></verify>
  <acceptance_criteria>
    - Tests freeze the clock and distinguish TTL from TTL+1 exactly.
    - Valid legacy cache reads; corrupt/invalid fresh or stale cache is ignored and never returned.
    - Stale fallback is visibly metadata-marked and occurs only after network failure.
    - Existing proxy URL and cache key are byte-unchanged.
  </acceptance_criteria>
  <done>Forecast retrieval exposes conservative provenance without stranding valid legacy cache or accepting corrupt evidence.</done>
</task>

<task type="auto">
  <name>Task 2: TDD atomic hook resolution and Europe/Oslo coverage</name>
  <files>src/hooks/useWeather.ts, src/hooks/__tests__/useWeather.test.ts, src/lib/planning/coverage.ts, src/lib/planning/__tests__/forecast-evidence.test.ts, src/lib/planning/__tests__/timezone.test.ts</files>
  <read_first>src/hooks/useWeather.ts, src/lib/met-no/client.ts, src/lib/met-no/types.ts, src/lib/planning/__tests__/forecast-evidence.test.ts, src/lib/planning/__tests__/timezone.test.ts, src/lib/planning/__tests__/planlegg-fixtures.ts</read_first>
  <behavior>
    - RED: result unwrap does not pass the result envelope to extractors; one accepted request publishes weather and matching evidence atomically; rejected/older/cancelled requests cannot overwrite the latest key.
    - GREEN: a pure request-state seam in `useWeather.ts` accepts `started/resolved/rejected` events by request ID; only the active request can publish one complete WeatherState.
    - GREEN: coverage sorts a copy by epoch, rejects invalid/duplicate points, distinguishes `complete-hourly | sampled | gapped | stale | unavailable`, preserves ISO identity and formats Europe/Oslo including repeated DST hour.
  </behavior>
  <action>Add Node-testable lifecycle/unwrap helpers in `useWeather.ts` and use them in the hook; no jsdom/package addition. Increment a request ID per fetch key, mark cleanup cancelled, and apply forecast plus evidence in one state transition only when current. Preserve existing WeatherState fields and add nullable evidence. Implement coverage with absolute instants and explicit `Intl.DateTimeFormat({ timeZone: 'Europe/Oslo' })`; never host-local `getHours()` for claims.</action>
  <verify><automated>npx vitest run src/hooks/__tests__/useWeather.test.ts src/lib/planning/__tests__/forecast-evidence.test.ts src/lib/planning/__tests__/timezone.test.ts</automated></verify>
  <acceptance_criteria>
    - Deliberately reversed promise completion leaves the newer request in state with its own evidence.
    - Extraction receives only `result.forecast`; no half-old/half-new WeatherState is observable.
    - Missing/duplicate/61-minute/stale points cannot produce full-span copy; DST instants remain distinct.
    - Existing consumers still receive now/hourly/daily/dailyAtHour/forecast/error/attribution.
  </acceptance_criteria>
  <done>Weather state and coverage are atomic, cancellable, timezone-explicit and conservative.</done>
</task>

</tasks>

<threat_model level="ASVS-L1">
| Threat | Mitigation / disposition |
|---|---|
| Corrupt cache becomes advice | Validate envelope and used forecast fields; invalid means network/unavailable. |
| Stale data masquerades as current | Explicit stale metadata and strong-copy denial. |
| Older async response overwrites new place | Request-ID/cancellation gate and atomic state transition tests. |
| Child/location payload leaks | No payload logging or new storage/network path. |
</threat_model>

<artifacts_this_phase_produces>
- Forecast metadata/result types, coverage module and focused hook tests.
- No new cache key, package, endpoint, user schema, analytics or media artifact.
</artifacts_this_phase_produces>

<verification>
- Focused weather/cache/hook/coverage/timezone suites and full test/lint/build pass.
- Fresh high-risk reviewer verifies the immutable forecast candidate SHA independently.
</verification>

<rollback>
Revert `feat: expose truthful forecast evidence` as one candidate; keep legacy cache readable and never retain strong copy without evidence.
</rollback>

<success_criteria>
- Legacy/fresh/stale/invalid cache and updated_at boundaries are deterministic.
- Hook resolution is atomic and cancellation-safe.
- Separate high-risk PASS exists; media/device/release gates remain Pending.
</success_criteria>

<output>
Create `01-02-SUMMARY.md` with RED/GREEN evidence, cache matrix, exact candidate SHA, reviewer identity/lane, PASS/BLOCK verdict bound to that SHA, and all remaining Pending gates.
</output>
